我们现在做的核心事情，是给改进后的 AEBS 控制系统训练并验证一个“安全证书”（SBC/barrier）。

通俗地说：

- Controller 负责开车和刹车。
- Barrier 像一个数学裁判，必须证明车辆从初始状态出发，在最坏感知误差下也不会进入危险区。
- Verifier 则逐点检查这个证明有没有漏洞。

目前不是代码一直“坏了”，而是验证过程不断暴露：之前对“要证明哪些状态安全、什么情况下算成功”的定义不完整。修这些是为了避免得到一个看似成功、实际上不成立的论文结论。

### 已经解决的部分

1. 语义感知模型已经跑通

距离预测：

- MAE：约 `0.10m`
- RMSE：约 `0.19m`
- 感知误差契约覆盖率：`97.5%`

2. 原 controller 在最坏感知误差下不安全

原 controller：

- exact：`0% unsafe`
- worst endpoint：`100% unsafe`

这意味着原控制器确实存在危险轨迹，所以不可能为它生成真实有效的安全证书。

3. 增加安全过滤器后，控制器安全测试通过

安全过滤器结果：

- exact：`0% unsafe`
- uniform：`0% unsafe`
- random boundary：`0% unsafe`
- worst endpoint：`0% unsafe`

所以 controller 层面已经基本解决。代价是部分场景 return 下降，说明它比较保守，但适合作为第一版可证 MVP。

4. 修正了 uncertainty 重复计算

以前把感知误差造成的动作变化又当作 process disturbance，而 barrier 中已经枚举了一遍感知误差，相当于同一个误差算了两次。

现在已经拆开：

```
感知误差 → semantic contract
动力学误差 → process disturbance
```

当前模拟器是确定性的，而且数据没有真实状态转移残差，所以 process disturbance 暂时正确地设为 0。

### 现在真正卡住的问题

我们此前要求 barrier 在整个状态矩形上都证明安全：

```
距离 5～16m
速度 0～3m/s
```

但这个矩形中包含很多物理上已经来不及刹车的状态。

例如：

```
距离障碍物只剩 6.1m
速度仍为 3m/s
```

即使立刻使用最大制动力，也可能来不及在进入危险区前降到安全速度。

这就像要求证明：

> 无论车辆离墙多近、速度多快，都一定能刹住。

物理上不成立。因此 barrier 一直出现 violation，并不一定是神经网络训练得不好，也可能是它在正确地告诉我们：“你要求证明的范围太大了。”

最新 `goal_guided` 版本失败得更明显：

- decrease violations：`4953/5704`
- initial、unsafe、goal 条件也被破坏
- worst state 转移到了 `d=10.43m, v=1.71m/s`

这说明继续简单增加 loss、epoch 或反例权重已经没有意义。

### 为什么前面一直修

因为我们先后发现并修正了不同层次的问题：

```
Controller 本身不安全
        ↓
增加 safety filter
        ↓
感知误差被重复计入
        ↓
拆分 semantic / process uncertainty
        ↓
终止状态也被要求严格下降
        ↓
修正 terminal / goal / unsafe domain
        ↓
遗漏 B(s) ≥ 0
        ↓
补全非负与区域验证
        ↓
发现证明范围包含物理不可恢复状态
```

这些不是为了把数字“调好看”，而是在逐步让实验和论文里的数学定义真正一致。

### 不修好能不能继续实验？

分两种情况。

如果目标只是展示：

- 感知模型效果；
- safety filter 的安全率；
- robust controller 与 baseline 的对比；
- semantic uncertainty 的影响；

那么这些实验可以继续，已经有一些有效结果。

但如果目标是论文的核心主张：

> 改进方法能够生成 robust SBC safety certificate。

那么这个问题必须解决。否则只能如实报告 `violated`，不能声称已经证明安全。

### 下一步应该怎么解决

下一步不再直接训练 barrier，而是先确定“真正可以证明的状态域”。

具体做法是：

1. 用最大制动力计算每个状态的最短停车距离：

\[ d_{\text{stop}}=\frac{v^2-v_{\text{safe}}^2}{2a_{\max}} \]

2. 把状态分成：

```
可恢复状态：现在开始最大刹车仍来得及
不可恢复状态：即使最大刹车也来不及
目标状态：已经低速、安全到达
危险状态：已经进入危险区域
```

3. 用 safety filter 在网格上进行 worst-endpoint rollout，检查：
    - 物理上可恢复的状态中，controller 是否真的都能保持安全；
    - 哪些状态属于初始区域能够到达的范围；
    - 物理判据与实际 rollout 是否一致。
4. 然后只在“可恢复且与初始区域相关”的不变域上训练和验证 barrier。

这不是为了删掉失败点，而是把论文主张改成物理上合理的形式：

> 对所有属于 robust recoverable domain 的初始状态，系统在感知契约内保持安全。

如果坚持证明整个 `5～16m × 0～3m/s` 矩形，就必须重新设计控制目标或物理系统，因为其中部分状态本身不具备可控安全性。

简而言之：controller 已经基本修好，现在卡的是“证明范围定义得过大”。下一步先求出可恢复安全域，再继续 barrier 实验，这是目前最关键、也最合理的一步。