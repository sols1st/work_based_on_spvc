# SafePVC 改进方案：可行性分析与详细实施计划

> 目标论文：*Provably Probabilistic Safe Controller Synthesis for Vision-Based Neural Network Control Systems*（SafePVC）
> 源码：`artical-F122/`（仅含 CARLA Emergency Braking 基准，代码目录名 `Aebs`）
> 改进思路来源：`论文改进思路.md`（"语义抽象 + 状态相关不确定性 + 分布鲁棒 SBC + 自适应反例驱动联合优化"）

---

## 0. 结论摘要（TL;DR）

**总体可行性：高。** 改进方案不仅可行，而且代码中已经存在"半成品"结构可以直接复用（感知网络已把 32×32 图像压缩成 1 维标量 `d̂`，控制器实际只消费 2 维状态）。改进的核心不是推倒重来，而是：

1. 把"图像→标量"的感知压缩，替换为"图像→**可验证的**语义集合"；
2. 把"手工指定的、与状态无关的均匀扰动盒"，替换为"**从数据估计的、与状态相关的、带置信度的**扰动分布族"；
3. 把"硬编码 1.2 的全局 Lipschitz 常数 + 均匀网格"，替换为"局部 Lipschitz + 显式统计误差 + 自适应剖分"。

**本文最重要的一个结论**：改进思路所指出的三个理论缺口，在代码里都能找到直接证据（见 §2）。这不是"鸡蛋里挑骨头"，而是方法真正的理论弱点，改进后确实能显著提升论文的严谨性与新颖度。

---

## 1. 原文与代码的对应关系（现状盘点）

### 1.1 代码结构总览

| 目录/文件 | 职责 | 与论文章节对应 |
|---|---|---|
| `Aebs/system/env.py` | 动力学 `d'=d-v·dt`，`v'=v-acc·dt`；状态/动作/初始/不安全集；**扰动盒 `noise_bounds`** | §2, §3.2 数据驱动扰动 |
| `Aebs/system/estimate.py` | 用不同 latent `z` 采样，统计真实扰动 Δs/Δv | Remark 1（但结果未接入验证） |
| `Aebs/system/combined.py` | 端到端仿真画轨迹 | §4 可视化 |
| `Aebs/cGAN/train_mlp.py` | 训练"cGAN"生成器（**实为监督 MSE 回归**） | §3.1 cGAN 感知 |
| `Aebs/controller/StateEstimate_train.py` | 训练 `state_net`：图像(1024)→估计距离 `d̂`(1维) | §3.1 MLP 蒸馏 |
| `Aebs/controller/Controller_train.py` | PPO 训练控制器（**观测为真实状态，非图像**） | §3.1 PPO 预训练 |
| `Combined_network/model.py` | `AebsEnd2EndNet = gen_net + state_net + controller_net` | §3.1 可验证闭环 VCLS |
| `Aebs/VT/utils.py` | `MLP`（barrier `B`）、`martingale_loss`、`triangular` 噪声 | §3.2 |
| `Aebs/VT/train.py` | `VTLearner`：barrier `l_model` 与 controller `p_net` 的训练 loss | §3.2.1/3.2.2 |
| `Aebs/VT/verify.py` | `VTVerifier`：均匀网格 + IBP + 局部 Lipschitz + 判定下降条件 | Theorem 3.1 |
| `Aebs/VT/loop.py` | 主循环：训 barrier → 验证 → 训 controller | Algorithm 1 |

### 1.2 原文主张 vs 代码实现对照表（关键差异）

| 论文主张 | 代码实际实现 | 影响 |
|---|---|---|
| cGAN 生成视觉观测 | `train_mlp.py` 仅用 `MSELoss` 监督回归，无判别器 | "两个误差源 g_real→G_cGAN→g_MLP" 实际塌缩为一个监督 MLP；论文也承认 mode collapse |
| MLP 蒸馏 + Lipschitz 正则 | `state_net`（SubNet [1024,256,64,1]）+ 谱范数 power-iteration 正则 | 蒸馏其实只有"图像→d̂"一步，且只恢复 1 维 |
| PPO 训练"视觉"控制器 | `Controller_train.py` 用 `AebsEnv`，观测 = `[d_norm, v]` **真实状态**，不看图像 | "vision-based" 名不副实；控制器本质只需 2 维语义 |
| 数据驱动扰动 Δs 估计 | `estimate.py` 算了 Δs/Δv 统计，但 `env.py` 里扰动 = **手工指定均匀盒** `(high-low)*0.01` | 数据驱动未被真正使用（见 §2.2） |
| Theorem 3.1 的 Lipschitz 界 K | `loop.py:112` 硬编码 `k_except_l=1.2`，`K = 1.2 * delta`；再用 autograd 局部梯度乘之 | 全局 `L_f,L_π,L_g` 从未被真正估计（见 §2.1） |
| 反例引导交替精化 | `verify.py:109` `refine_enabled=False`，反例缓冲从不填充；`train('p')` 在整张网格上训练 | "counterexample-guided" 在代码里是整网格重训（见 §2.4） |
| 训练扰动 | `train.py` 用 `triangular()` 三角分布 | 训练/验证噪声分布不一致（train 三角 vs verify 均匀盒） |

### 1.3 决定改进落点的关键实现细节

1. **状态是 2 维**：`[d_norm, v]`，其中 `d_norm = d/std1`。`d ∈ [5,16]`m，`v ∈ [0,3]`m/s，动作 `acc ∈ [-3,3]`，`dt=0.05`。
2. **感知链路**（`Combined_network/model.py:80-89`）：
   `(z∈R^4, d) → gen_net → 图像(1024) → state_net → d̂(1维) → concat([d̂, v]) → controller_net → acc`。
   注意：**只有距离 d 是从图像估计的，速度 v 是直接传入的**（假设来自里程计等非视觉传感器）。
3. **当前生成器 `gen_net(z, d)` 的输出只依赖 `(z, d)`，不含 `v`**。也就是说，现有合成图像**物理上不包含速度信息**。这直接限制了"语义 encoder 能从图像恢复哪些量"（见 §3 阶段一的风险）。
4. **Barrier `l_model` = MLP [2,16,8,1]，tanh + softplus 输出**（`utils.py:27-57`）。softplus 保证 `B(s)≥0`，自动满足 SBC 条件 (i)。
5. **验证用 auto_LiRPA 的 IBP**（`verify.py:309`），不是 α-β-CROWN（代码注释里有 CROWN 选项但默认 IBP）。`pmass` 网格 = 10^2 个均匀噪声单元。
6. **初始集 / 不安全集**（`env.py:155-162`）：init = `d∈[15,16], v∈[2.5,3]`；unsafe = `d∈[5,6], v∈[0.5,3]`。

---

## 2. 三个理论缺口的代码证据

改进思路的核心论断是原文存在三个理论缺口。下面逐一给出代码级证据，这同时构成"为什么值得改"的论证。

### 2.1 缺口一：视觉近似误差没有进入证书

**论文里**：Theorem 3.1 的界 `K = τ L_B (1 + L_f √(1 + (L_π L_g)²))` 声称把感知网络 `g` 的 Lipschitz `L_g` 纳入证书。

**代码里**（`loop.py:112` 与 `verify.py:410-413, 471`）：

```python
k_except_l = float(1.2)                 # loop.py:112
K = k_except_l * delta                   # verify.py:412，delta = 网格半径
...
decrease = e + lips_l_batch * K - l_batch   # verify.py:379，lips_l_batch 为 B 的局部梯度范数
```

即实际使用的界是 `lips_B(s) · 1.2 · delta`。那个 `1 + L_f√(1+(L_π L_g)²)` 被一个**魔法常数 1.2** 取代。`L_f`、`L_π`、`L_g` 从未被估计，感知误差 `g_real→g_MLP` 的量化从未进入验证。

**结论**：论文声称的"视觉近似误差进入证书"在实现里退化为一个硬编码常数，这既不可证明也不可解释。改进方案用**逐维 conformal 感知残差集合** `C_ξ` 显式量化感知误差，并把它作为独立的 `K^sem` 项写入证书，是对这一缺口的实质修复。

### 2.2 缺口二：扰动被错误简化为 state-independent 固定分布

**论文里**：Remark 1 与 §4.3 声称"数据驱动地估计状态扰动 Δs"，且 Assumption 1 假设 `Δs ~ μ` 与状态无关。

**代码里**：

```python
# env.py:171-177
self.noise_bounds = ((low - high) * factor, (high - low) * factor)  # factor=0.01
self.noise = (high - low) * factor
```

扰动是**对整个状态空间统一的均匀盒**（`factor=0.01` 手工指定），与状态无关、与 `z` 无关。而 `estimate.py` 里真正算出的、随状态变化的 Δs/Δv 统计量（均值/std）**从未被 `env.py` 或 `verify.py` 引用**。论文 §4.3 自己承认："these state disturbances are not derived from real-world environmental perturbations… directly modeled as uniformly distributed within manually specified bounds."

**结论**：改进方案的"state-dependent disturbance `w ~ μ(·|s)`"直接命中了这个最明显的偷懒之处。改为按状态区域 `C_i` 收集 `w_j` 并估计局部分布 `μ̂_i`，实现成本低、收益大（见阶段三）。

### 2.3 缺口三：有限样本估计没有统计置信度保证

**论文里**：期望 `E_{Δs~μ}[B(...)]` 通过 Monte Carlo 采样估计（`verify.py:349` 用 `pmass * ub` 加权求和），但没有任何关于"用 N 个样本估计期望"的置信区间。

**代码里**：`compute_expected_l`（`verify.py:319-349`）直接把离散化 pmass 当作真实概率，`integrate_noise`（`env.py:180-205`）假设均匀分布按体积比例给概率。这里既没有 Hoeffding/Bernstein 形式的统计裕量，也没有把有限样本误差计入证书。

**结论**：改进方案加入显式 `η_i(N, δ) = B_max √(log(2/δ)/(2N_i))` 统计裕量，使"有限样本"这个事实第一次进入证书，弥补缺口三。

### 2.4 额外发现的实现级问题（改进时一并修复）

1. **训练/验证噪声不一致**：`train.py:131` 用三角分布 `triangular()`，`verify.py` 用均匀盒。改进时统一为同一分布族。
2. **反例精化实为关闭**：`verify.py:109` `refine_enabled=False`，`check_dec_cond` 里 `if self.refine_enabled and v>0` 永不触发，`violation_buffer` 恒为 `None`；`train('p')` 退化为整网格重训。改进方案应真正启用并升级为联合反例。
3. **`reach_prob` 不一致**：learner 0.95（`loop.py:191`）vs verifier 0.9（`loop.py:203`）。
4. **`square_l_output` 名不副实**：实际是 softplus（`utils.py:53-55`），文档与代码注释易误导。

---

## 3. 逐条可行性分析

对 `论文改进思路.md` 的 25 个阶段逐条评估。评级：**高**（几乎可直接落地）/ **中**（需一定工程或理论处理）/ **低**（需新算法或实验困难）。"改动量"指相对现有代码的侵入程度。

| # | 改进点 | 现状 | 可行性 | 改动量 | 关键风险 / 说明 |
|---|---|---|---|---|---|
| 一 | 明确总体目标：`s' = f(s,π(ξ)) + w` | 已是 `s' = v_next(s,a) + noise` | 高 | 小 | 纯改写，去掉 `g(s,z)` |
| 二 | 删除 cGAN 图像生成 | `gen_net` 即监督 MLP，非真 cGAN | 高 | 小 | 直接删 `gen_net`，感知改为 encoder |
| 三 | 定义语义 latent ξ | 现有 `state_net` 只输出 1 维 `d̂` | 高 | 小 | **受限于现有图像不含 v**（见 §1.3-3），ξ 需务实定义 |
| 四 | 建立语义数据集 | 现有 `gen_aebs_images` 可无限造 (图像, 标签) | 高 | 小 | 可离线生成，无需人工标注 |
| 五 | 训练 semantic encoder | 复用 `StateEstimate_train.py` 改输出维 | 高 | 小 | 与 state_net 同构 |
| 六 | 检查控制充分性 | 控制器输入即语义，天然满足 | 高 | 小 | 加 `L_suff` 或 task-return 检验 |
| 七 | conformal 语义集合 | 全新，但算法简单（numpy 分位数） | 高 | 小 | 需要独立 calibration 集 |
| 八 | 状态相关语义集合 | 全新，可学习 `σ_ω(s,z)` | 中 | 中 | 需设计 uncertainty head |
| 九 | 语义控制器（输入 ξ） | 改 `controller_net` 输入维 2→k | 高 | 小 | PPO 重训或精调 |
| 十 | 语义不确定性增广 | 训练时对 ξ 加噪 | 高 | 小 | 直接复用现有 `y_pert` 扰动逻辑 |
| 十一 | 区分感知/过程两类不确定性 | 概念清晰化 | 高 | 小 | 纯模型重构 |
| 十二 | 状态相关扰动 | 替换 `env.noise` 为逐区域估计 | 高 | 中 | 复用 `estimate.py` 思路但真正接入 |
| 十三 | ambiguity set（不用 μ̂ 当真） | 全新 | 中 | 中 | DRO 子问题需选易解形式（见 §4.3） |
| 十四 | 分布鲁棒闭环 | 概念 | 高 | 小 | 模型统一 |
| 十五 | 鲁棒 SBC（sup over ξ,Q） | 现为单点期望 | 中 | 中 | sup over Q 的数值实现是难点 |
| 十六 | 保留 init/unsafe 约束 | 已存在（region loss） | 高 | 无 | 无需改 |
| 十七 | 重设计 SBC loss | 改造 `train_step_l` | 高 | 中 | 对齐 theorem 条件 |
| 十八 | 局部 Lipschitz 取代全局 | 代码已是局部梯度，删魔法 1.2 | 高 | 小 | 把 `1.2` 换成可证界的项 |
| 十九 | 显式统计误差 η | 全新 | 高 | 小 | Hoeffding 闭式，一行 |
| 二十 | 自适应状态剖分 | 现为 100×100 均匀网格 | 中 | 大 | 需重构 `verify.py` 网格逻辑为递归剖分 |
| 二十一 | 联合反例 (s,ξ,Q,w) | 现为单个状态 | 中 | 中 | 需在验证里解 argmax 子问题 |
| 二十二/三 | 反例更新 SBC/controller | 已有雏形但关闭 | 高 | 小 | 启用 `refine_enabled` 并喂联合反例 |
| 二十四 | controller trust region | 现为 MSE-to-teacher | 中 | 小 | KL/∞-norm 约束，PPO 已有 clip 机制 |
| 二十五 | 性能约束 J(π)≥J_min | 现为 MSE 近似 | 中 | 中 | 需额外任务回报估计 |
| 二十六 | 重估 ambiguity set | 闭环已有此意 | 高 | 中 | 随 π 更新重采样 |
| 二十七 | 更新语义抽象 | 全新 | 中 | 大 | 可选增强，非首轮必须 |

**总体判断**：25 项中约 15 项"高"可行性、8 项"中"、2 项"低/大改"（自适应剖分、DRO 数值求解）。**建议 MVP 范围**（首轮提交）优先覆盖：二~七、九~十三、十五~十九、二十一~二十四，把"自适应剖分"与"状态相关语义集合"列为增量。

---

## 4. 改进后的系统模型与原理

### 4.1 总体架构

改进后闭环从 `s' = f(s, π(g(s,z))) + Δs` 变为：

$$s_{t+1} = f\big(s_t, \pi(\xi_t)\big) + w_t,\qquad \xi_t\in\mathcal C_\xi(s_t,z_t),\qquad w_t\sim Q_t(\cdot|s_t),\ Q_t\in\mathcal P(s_t).$$

两个不确定性被**显式分离**：
- **感知不确定性**（set-bounded）：真实语义 `ξ^true ∈ C_ξ(s,z)`，由 conformal 校准保证覆盖率；
- **过程/环境扰动**（distributionally uncertain）：`w` 的分布只在 ambiguity set `P(s)` 内已知。

最终要证明的核心安全条件（改进版 SBC condition (iv)）：

$$B(s)-\sup_{\xi\in\mathcal C_\xi(s)}\sup_{Q\in\mathcal P(s)}\mathbb E_{w\sim Q}\big[B\big(f(s,\pi(\xi))+w\big)\big]\ge\epsilon.$$

当 `C_ξ={ξ̂}`、`P(s)={μ}` 退化为单点时，上式还原为原文 `B(s) ≥ E_μ[B(s')] + ε`。所以这是原文的严格推广。

### 4.2 原理一：语义抽象 + 符合校准（Conformal Prediction）

**动机**：原文用图像重建质量（Figure 2 的 Raw/Downsampled/Generated）作为感知精度的代理，但"像素重建好"≠"控制语义准"。改进后直接对控制真正需要的量做有保证的区间估计。

**Split Conformal**（对每个语义维度 `j`）：
1. 训练 encoder `E_φ: O→Ξ`，在 calibration 集上计算逐维残差 `r_{i,j} = |ξ_{i,j} - E_{φ,j}(o_i)|`；
2. 取 `1-α` 经验分位数 `q_j = Quantile_{1-α}({r_{i,j}})`；
3. 构造 `C_ξ(o) = Π_j [E_{φ,j}(o) - q_j, E_{φ,j}(o) + q_j]`。

**保证**（可交换性假设下）：`P(ξ^true ∈ C_ξ(o)) ≥ 1-α`。这正是改进方案中"第一层 guarantee"的来源。

**务实提醒（关键）**：如 §1.3-3 所述，现有 `gen_net(z,d)` 输出的图像不编码速度 `v`。因此对 CARLA 基准，第一版语义应定义为 **`ξ = (d, c_v)`**（`c_v` 为置信度，可用残差大小或图像熵代理），`v` 继续作为非视觉传感器输入直连控制器——这与当前代码结构完全一致，改动最小、且不夸大感知能力。若要对 `v_rel`、`TTC` 做视觉估计，需先升级生成器/数据（含运动信息的图像序列），作为后续增量。

### 4.3 原理二：分布鲁棒（Wasserstein DRO）

**动机**：`μ̂_s` 由有限样本估计，直接当真实分布会低估尾部风险。定义 ambiguity set：

$$\mathcal P(s)=\big\{Q:\ W(Q,\hat\mu_s)\le\rho_s\big\},$$

其中 `W` 为 Wasserstein 距离，半径 `ρ_s` 由样本量 `N_s` 与置信 `δ` 决定。由 Wasserstein 距离的浓度不等式，`P(μ_true(·|s) ∈ P(s)) ≥ 1-δ`。这构成"第二层 guarantee"。

**数值实现（重要工程决策）**：对二维扰动 `w`，直接解 `sup_{Q: W(Q,μ̂)≤ρ} E_Q[φ(w)]` 是凸优化，但逐网格点求解成本高。推荐三种**可落地的替代**，按实现难度递增：

- **(A) 区间/矩 ambiguity set（首选）**：`P(s) = {Q : supp(Q)⊆Box_s, E_Q[w]∈[a_s,b_s]}`。`Box_s` 为状态相关的支撑盒，`[a_s,b_s]` 为用 Hoeffding 从有限样本估计的均值置信区间。此时 `sup_Q E_Q[φ(w)]` 退化为**凸包极值**（对给定支撑盒，最坏分布是使 `φ` 最大的点质量，或对均值约束用拉格朗日对偶求解一个小线性规划）。这与现有 `pmass` 离散网格机制天然衔接。
- **(B) 离散 Wasserstein 球**：在现有 10×10 扰动网格上，把 `sup_Q` 写成小规模 LP（`max Σ q_j φ_j`，s.t. `q≥0, Σq=1, W(q,μ̂)≤ρ`），用 `cvxpy`/`scipy.linprog` 逐 cell 求解或预计算查表。
- **(C) KL/χ² 球（有闭式对偶）**：`sup_Q E_Q[φ] - ρ·D_φ(Q‖μ̂)` 的 DRO 有 CVaR 型闭式（风险敏感形式），最易微分、可嵌入训练 loss。

**建议**：训练 loss 用 (C)（可微分、平滑），验证阶段用 (A)（保守且可证明、能闭合到统计置信）。这样"训练/验证分布鲁棒"与"有限样本置信"同时落地。

### 4.4 原理三：鲁棒 SBC + 自适应验证 + 联合反例（CEGIS）

**鲁棒 SBC**：把原 martingale 损失中的 `E_μ[B(s')]` 替换为 `R_B(s)=sup_{ξ∈C_ξ(s)} sup_{Q∈P(s)} E_Q[B(f(s,π(ξ))+w)]`。训练损失：

$$L_B = \underbrace{E_s[\operatorname{ReLU}(R_B(s)-B(s)+ε)]}_{\text{下降条件}} + \lambda_I L_{init} + \lambda_U L_{unsafe} + \lambda_L L_{localLip}.$$

**自适应验证**：把均匀网格替换为递归剖分。对 cell `C_i` 计算 certified margin：

$$M_i = \inf_{s\in C_i} B(s) - \sup_{\xi,Q} E_Q[B(s')].$$

`M_i≥ε` → 认证该 cell；`M_i<0` 且有违反态 → 产出反例；`0≤M_i<ε` → 二分剖分 `C_i`。这比 100×100 均匀网格更 scalable（把算力集中到"难"区域）。

**联合反例**：反例从单个状态升级为：

$$c^* = (s^*,\ \xi^*,\ Q^*,\ w^*),\qquad \xi^*=\arg\max_{\xi\in C_ξ(s^*)} R_B,\quad Q^*=\arg\max_{Q\in P(s^*)} E_Q[B(s')].$$

它明确回答"哪个状态最危险、哪种感知误差最危险、哪类扰动分布最危险"，比原文的 `s_cex` 信息量大得多，也更能指导精化方向。

### 4.5 原理四：三层证书的合成（最终理论保证）

最终定理把三部分概率保证"拼接"起来：

$$P_{data}\big[\inf_{\xi\in\mathcal C_ξ,\,Q\in\mathcal P}P_Q(\mathrm{Safe})\ge p\big]\ \ge\ 1-\delta_{total},$$

其中 `δ_total` 由 `α`（感知覆盖失败）、`δ`（ambiguity 未覆盖）联合控制。相比原文"直接把 learned μ 当真"，这是质的提升。

**无限时域注意**：普通 split conformal 只给 **marginal** 覆盖 `P(ξ_t∈C_t)≥1-α`，不能直接推 `P(∀t, ξ_t∈C_t)≥1-α`。第一版建议**采用工程稳健契约**：把 `C_ξ` 定义为确定性 worst-case 感知集（适当放大分位数），SBC 对该集合做确定性 worst-case 验证，从而避免序列级 conformal 的复杂理论（与改进思路 §三十一 结论一致）。

---

## 5. 详细实施计划（分阶段，含代码改动点）

> 约定：新增文件放在 `Aebs/semantic/` 与 `Aebs/disturbance/`；对现有文件的修改用 `文件:函数` 标注。每阶段末尾给"验收标准"。

### 阶段 0：环境与基线复现（1 周）

**目标**：跑通现有 `python -m Aebs.VT.loop`，记录基线指标（certified probability、迭代次数、violation 数），作为后续对照。

- [ ] `conda env create -f environment.yml`，`cd auto_LiRPA && pip install .`
- [ ] 复现 `Controller_train.py`、`StateEstimate_train.py`、`train_mlp.py`、`loop.py`
- [ ] 记录基线：`max_reach_prob`、`hard_violations`、总迭代数、验证耗时

**验收**：能复现论文 Table 1 的量级（CARLA ~94.2% 交替训练）。

### 阶段 1：语义抽象 + 符合校准（1–2 周）★核心贡献点

**目标**：把 `state_net`（图像→1维 `d̂`）升级为 `E_φ`（图像→语义 ξ），并产出 certified 语义集合 `C_ξ`。

改动：
- 新增 `Aebs/semantic/encoder.py`：`SemanticEncoder`（结构复用 `SubNet`，输出维 = `dim(ξ)`）。
- 新增 `Aebs/semantic/data.py`：用现有 `gen_net` 离线采样 `(o_i, ξ_i)` 并切分 train/cal/test。
- 新增 `Aebs/semantic/conformal.py`：`split_conformal(residuals, alpha) -> q`，`build_set(E, o, q) -> C_ξ`。
- 修改 `Combined_network/model.py`：`AebsEnd2EndNet.forward` 中 `state_net` 换成 `E_φ`，输出 ξ；控制器输入 `concat([ξ, v])`。
- 定义 ξ：**CARLA 用 `ξ=(d)`（或 `(d, c_v)`）**，`v` 仍直连；X-Plane 若补数据可用 `ξ=(p,θ)`。

原理见 §4.2。**验收**：calibration 集上经验覆盖率 ≥ `1-α`，`C_ξ` 宽度显著小于图像空间维度（从 1024 维像素降到 1~2 维语义区间）。

### 阶段 2：语义控制器训练 + 不确定性增广（1 周）

**目标**：控制器直接在语义空间训练，并对语义不确定性鲁棒。

改动：
- 修改 `Aebs/controller/Controller_train.py`（或新增 `SemanticController_train.py`）：PPO 观测改为 `ξ`（真值），动作 `acc`。
- 修改 `Aebs/VT/train.py:train_step_p`：训练时对 ξ 注入 `e_ξ ∈ E_ξ(s,z)`（复用现有 `y_pert` 扰动逻辑），实现 worst-case 增广 `max_{e_ξ∈E_ξ} L_RL(π(ξ+e_ξ))`。

**验收**：`L_suff = E_o‖π_img(o) - π_ξ(E_φ(o))‖²` 低于阈值，或 `J(π_ξ) ≈ J(π_img)`；增广后控制器在 `C_ξ` 内扰动下 violation 下降。

### 阶段 3：状态相关扰动估计 + ambiguity set（1–2 周）★核心贡献点

**目标**：用数据估计 `w ~ μ(·|s)`，并构造带置信度的 `P(s)`，替换 `env.py` 的手工均匀盒。

改动：
- 新增 `Aebs/disturbance/estimate.py`：对每个状态 cell `C_i`（先用粗网格，后续接阶段 5 自适应剖分），采样 `{w_j : s_j∈C_i}`，得到 `μ̂_i`。
- 新增 `Aebs/disturbance/ambiguity.py`：实现 §4.3 的 ambiguity set——首选 (A) 区间/矩球：`supp ⊆ Box_i`（状态相关支撑）+ `E_Q[w] ∈ [a_i, b_i]`（Hoeffding 置信区间，`b_i - a_i = O(1/√N_i)`）。
- 修改 `Aebs/system/env.py`：删除全局 `noise_bounds`，暴露 `sample_w(s)` 与 `P(s)` 查询接口。
- 修改 `Aebs/VT/train.py`：训练噪声采样改用 `w ~ μ̂(·|s)`（**统一 train/verify 分布**，修复 §2.4-1）。

原理见 §4.3。**验收**：`P(μ_true(·|s) ∈ P(s)) ≥ 1-δ` 在仿真中经验成立；`P(s)` 半径随 `N_i` 增大单调收缩（展示有限样本收敛）。

### 阶段 4：分布鲁棒 SBC（1–2 周）★核心贡献点

**目标**：把 SBC 的下降条件改为对 `ξ∈C_ξ`、`Q∈P(s)` 的 worst-case。

改动：
- 修改 `Aebs/VT/utils.py`：新增 `robust_martingale_loss`（对 ξ 取 sup 用网格/最坏采样 + 局部 Lipschitz；对 Q 取 sup 用 §4.3 的闭式/凸极值）。
- 修改 `Aebs/VT/train.py:train_step_l`：`E_μ[B(s')]` → `R_B(s)`。
- 修改 `Aebs/VT/train.py`：loss 重组为 `L_B = L_Rdec + λ_I L_init + λ_U L_unsafe + λ_L L_localLip`（对齐 theorem 条件）。

原理见 §4.4。**验收**：在合成且已知 `μ_true` 的设定下，鲁棒 SBC 给出的安全下界 ≥ 朴素 SBC，且不因扰动分布微小变化而失效。

### 阶段 5：自适应验证 + 联合反例（2–3 周）★核心贡献点

**目标**：把均匀网格验证替换为自适应剖分，并产出联合反例 `(s*,ξ*,Q*,w*)`。

改动：
- 重构 `Aebs/VT/verify.py`：`get_unfiltered_grid`/`check_dec_cond` 的均匀网格 → 递归剖分结构（cell 用区间盒 `(lb,ub)` 表示，维护待验证队列）。
- 新增 `compute_certified_margin(C_i)`：用 IBP 求 `inf B(s)`，用阶段 4 的 `R_B` 求 `sup E_Q[B]`，判定 `M_i` 所属分支（认证/反例/剖分）。
- 启用 `refine_enabled=True`（修复 §2.4-2），`violation_buffer` 存联合反例而非纯状态。
- 修改 `Aebs/VT/train.py:train_step_p`：反例精化用 `L_B^cex = ReLU[B(s'*) - B(s*) + ε]`，并加 trust-region（`D_KL(π_{k+1}‖π_k)≤ρ` 或 ∞-范数，PPO clip 已近）。
- 修改 `Aebs/VT/loop.py`：删 `k_except_l=1.2`，改由 §2.1 的局部 Lipschitz + 阶段 3 的统计裕量 `η_i` 构成 `K_i^state + K_i^sem + η_i`。

原理见 §4.4、§2.1、§2.3。**验收**：达到相同安全下界所需 cell 数显著少于 100×100；反例能定位到具体状态/感知误差/扰动类型。

### 阶段 6：理论整合 + 实验（2–3 周）

**目标**：写出改进版定理（三层保证），并完成对照实验。

理论（可直接进论文，见 §9）：
- 定理 A（感知覆盖）：`P(ξ^true∈C_ξ) ≥ 1-α`（split conformal）。
- 定理 B（扰动覆盖）：`P(μ_true(·|s)∈P(s)) ≥ 1-δ`（Hoeffding/Wasserstein 浓度）。
- 定理 C（鲁棒安全）：在 A、B 成立下，鲁棒 SBC ⟹ `P(Safe) ≥ p`。
- 定理 D（合成）：`P_data[inf_{ξ,Q} P_Q(Safe) ≥ p] ≥ 1-δ_total`。

实验（对照改进思路 §三十二/三十三）：
1. cGAN-SafePVC vs Semantic-SafePVC（消融感知抽象）；
2. fixed μ vs state-dependent μ(·|s)；
3. empirical μ̂ vs distributionally-robust P(s)；
4. uniform grid vs adaptive verification。

指标：`p_certified`、`T_verify`、`N_cells`、`N_cex`、`J_task`、empirical violation rate，以及感知侧 `MAE(d)`、`Coverage(C_ξ)`、`Width(C_ξ)`（替换 Figure 2 的像素重建对比）。

---

## 6. 实验设计与评价指标

见阶段 6。补充三点实验设计建议：

1. **扰动强度的稳健性曲线**：论文 Figure 4 已按扰动幅度扫描；改进后应再扫 `ambiguity 半径 ρ` 与 `样本量 N`，展示"有限样本→更宽 P(s)→仍保安全下界"的**可信退化**，而非靠手工调盒。
2. **越界（violation）率对照**：在**真实环境扰动**（而非手工均匀盒）下重放，对比 naive SBC 与 robust SBC 的经验违反率——这直接验证缺口二的修复。
3. **感知覆盖率**：报告不同 fog/lighting/blur 下 `Coverage(C_ξ)` 是否仍 ≥ 1-α，支持"certified abstraction"主张。

---

## 7. 风险与缓解

| 风险 | 影响 | 缓解 |
|---|---|---|
| 现有合成图像不含速度，多维 ξ 无法从图像恢复 | 语义抽象维度受限 | 第一版 ξ=(d)，v 直连（与代码一致）；速度恢复列为增量 |
| DRO `sup_Q` 数值求解慢 | 验证/训练变慢 | 用区间/矩球（闭式凸极值）或 KL 球（可微），避免逐点 LP |
| 自适应剖分重构量大 | 工程风险高 | MVP 先保留均匀网格，自适应剖分作为第二版；或先做"1D 递归"验证 |
| 有限样本下 P(s) 过宽 → 安全下界过低 | 结果不亮眼 | 用更紧的浓度界（Bernstein/自归一化），或增大采样 |
| conformal 仅 marginal 覆盖，无限时域有理论裂缝 | 审稿质疑 | 采用确定性 worst-case 感知契约（§4.5），不声称序列级覆盖 |
| 更换感知模块导致旧权重失效 | 需重训 | 阶段 1 用 gen_net 在线造数据，无需旧 cGAN 权重 |

---

## 8. 里程碑与工作量估算

| 里程碑 | 内容 | 预估 | 依赖 |
|---|---|---|---|
| M0 | 基线复现 | 1 周 | — |
| M1 | 语义抽象 + conformal（阶段 1–2） | 2–3 周 | M0 |
| M2 | 状态相关扰动 + ambiguity（阶段 3） | 1–2 周 | M1 |
| M3 | 鲁棒 SBC（阶段 4） | 1–2 周 | M2 |
| M4 | 自适应验证 + 联合反例（阶段 5） | 2–3 周 | M3 |
| M5 | 理论 + 实验 + 论文（阶段 6） | 2–3 周 | M4 |
| **合计（MVP）** | 覆盖全部"高+中"可行性项 | **9–14 周** | |

**可并行**：阶段 3 与阶段 4 可在阶段 1 完成后并行；阶段 6 的理论部分可与阶段 2–5 同步撰写。

---

## 9. 附：可直接写入论文的关键定理与公式

**改进后闭环**：

$$s_{t+1}=f\big(s_t,\pi(\xi_t)\big)+w_t,\quad \xi_t\in\mathcal C_\xi(s_t,z_t),\quad w_t\sim Q_t(\cdot|s_t)\in\mathcal P(s_t).$$

**鲁棒 SBC（改进版 Theorem 2.2 条件 iv）**：

$$B(s)-\sup_{\xi\in\mathcal C_\xi(s)}\sup_{Q\in\mathcal P(s)}\mathbb E_{w\sim Q}\big[B\big(f(s,\pi(\xi))+w\big)\big]\ge\epsilon.$$

**感知覆盖（split conformal）**：

$$P_{data}\big[\xi^{true}\in\mathcal C_\xi(o)\big]\ge 1-\alpha,\qquad \mathcal C_\xi(o)=\prod_{j=1}^{k}\big[E_{\phi,j}(o)-q_j,\ E_{\phi,j}(o)+q_j\big].$$

**扰动覆盖（Wasserstein / Hoeffding）**：

$$P_{data}\big[\mu_{true}(\cdot|s)\in\mathcal P(s)\big]\ge 1-\delta,\qquad \mathcal P(s)=\{Q:W(Q,\hat\mu_s)\le\rho_s\}.$$

**有限样本统计裕量（bounded case）**：

$$\eta_i = B_{\max}\sqrt{\frac{\log(2/\delta)}{2N_i}}.$$

**验证判定（含全部误差项）**：

$$B(\tilde s_i)-\hat{\mathcal R}_B(\tilde s_i)-K_i^{state}-K_i^{sem}-\eta_i\ge\epsilon.$$

**最终三层保证**：

$$P_{data}\Big[\inf_{\xi\in\mathcal C_\xi,\,Q\in\mathcal P}P_Q(\mathrm{Safe})\ge p\Big]\ \ge\ 1-\delta_{total}.$$

---

## 10. 一句话总结

**这个改进的本质，是把 SafePVC 的证明对象从"学到的图像代理"升级为"经过认证的语义抽象 + 分布鲁棒的随机闭环"**；代码结构已经为这次升级留好了接口（图像→标量的压缩、2 维语义状态、局部 Lipschitz 与 pmass 网格），三个理论缺口在代码里都有明确证据，因此**可行性高、论文贡献点清晰、工程量可控（MVP 约 9–14 周）**。
