# SafePVC 改进工作计划与进展

> 本文档是项目的主计划与进度台账。实验的逐次运行结果、日志位置和指标明细记录在《改进实验记录.md》中。

## 1. 项目目标

改进论文 *Provably Probabilistic Safe Controller Synthesis for Vision-Based Neural Network Control Systems* 及其 CARLA Emergency Braking 实现，将原方法重构为：

$$
s_{t+1}=f(s_t,\pi(\xi_t))+w_t,
\qquad
\xi_t\in\mathcal C_\xi(o_t),
\qquad
Q(w_t\mid s_t)\in\mathcal P(s_t).
$$

最终交付应同时包含：

- 可运行、可复现的 Semantic-DR-SafePVC 代码；
- 原始方法与各改进阶段的消融实验；
- 语义感知、任务性能、经验安全性和形式验证指标；
- 与实现一致的论文方法、理论说明、算法和实验章节；
- 完整实验日志、配置、随机种子、模型权重和汇总表。

## 2. 当前范围与约束

- 当前源码只提供 CARLA Emergency Braking（`Aebs`）基准；论文中的 X-Plane 源码未提供，因此第一阶段以 CARLA 为主。
- 数据集只有 400 张 `32x32` 灰度图像及距离标签，第一版语义变量取距离 $\xi=d$；速度 $v$ 继续作为已知状态直接输入控制器。
- 普通 split conformal 只提供边际覆盖率。无限时域安全论证中，将 $\mathcal C_\xi$ 作为确定性 worst-case 感知契约，不把单步边际覆盖率直接外推到无限时间。
- 第一版分布鲁棒模型采用“状态分箱 + 支撑区间/矩信息 + 统计置信裕量”，先保证实现透明和可验证，再考虑 Wasserstein/KL/χ² 等更复杂集合。
- 所有正式实验在服务器 `/root/work_based_on_spvc/artical-F122`、Conda 环境 `vt` 中运行。

## 3. 已完成工作

### 3.1 论文与源码审计

- [x] 阅读并检查原论文 7 页 PDF 的完整内容与版面。
- [x] 将《论文改进思路》整理为可执行的阶段路线。
- [x] 确认关键实现链路：`gen_net(z,d) -> image -> state_net -> d_hat -> controller`。
- [x] 确认生成图像不编码速度，故第一版语义变量只取距离。
- [x] 定位三项核心理论/实现缺口：
  - 感知误差未进入安全证书；
  - 扰动被实现为状态无关的手工均匀盒；
  - 有限样本期望没有统计置信裕量。
- [x] 定位额外实现问题：训练/验证扰动分布不一致、反例精化关闭、`reach_prob` 不一致、Lipschitz 系数硬编码等。

### 3.2 服务器与环境核验

- [x] SSH 连接正常，服务器 GPU 为 NVIDIA RTX 4090，显存约 24 GB。
- [x] `vt` 环境可用：Python 3.8.20、PyTorch 2.3.1、CUDA 可用。
- [x] 本地与服务器关键源码 SHA-256 一致。
- [x] 原始入口 `python -m Aebs.VT.loop` 可运行。

### 3.3 阶段 0 原始基线

- [x] 完成一次原始代码 101 轮运行。
- [x] 运行耗时约 107 秒，最小/最终硬违反数均达到 0/10000。
- [x] 日志中出现的最佳安全概率下界为 80.563%。
- [x] 已实现原始日志解析器 `Aebs/experiments/parse_baseline.py`。

注意：该基线尚未固定全局随机种子，且安全界仍使用硬编码离散化系数和经验均匀扰动，因此只能作为“原实现复现值”，不能作为最终严格对照。

### 3.4 阶段 1 语义抽象初版

- [x] 新建 `Aebs/semantic/`，实现语义编码器，输出距离点估计与图像相关尺度。
- [x] 实现有限样本 split-conformal 校准。
- [x] 实现按预测距离分组的 Mondrian conformal。
- [x] 保存固定的 train/calibration/test 划分、模型、校准参数和 `metrics.json`。
- [x] 实现控制充分性指标：比较真实距离输入与语义距离输入产生的 PPO 动作。
- [x] 实现多随机种子汇总脚本 `Aebs/semantic/aggregate.py`。
- [x] 单元测试已覆盖网络输出形状、正尺度、split-conformal 阶数统计量和 Mondrian 分箱逻辑。

首个随机种子（seed 7）的初步结果：

| 指标 | 旧 `state_net` | 新语义编码器 |
|---|---:|---:|
| 真实图像距离 MAE | 2.862 m | 0.100 m |
| 真实图像距离 RMSE | 3.454 m | 0.193 m |
| 最大绝对误差 | 6.725 m | 1.268 m |

其他结果：全局 conformal 测试覆盖率 86.25%，目标为 90%；平均区间宽度 0.298 m；控制动作 MAE 0.044。远距离分箱覆盖率只有 65%，因此阶段 1 还不能验收，已加入 Mondrian conformal，等待多种子复核。

## 4. 分阶段实施计划

### 阶段 0：建立可信、可复现的对照基线

状态：进行中。

任务：

- [x] 跑通原始 101 轮流程并保存日志。
- [x] 自动解析迭代数、最佳安全界、硬违反数和耗时。
- [ ] 增加统一随机种子、命令行配置和结构化逐轮日志。
- [ ] 分别运行 fixed-controller 与 alternating-update 基线，每组至少 5 个种子。
- [ ] 增加 Monte Carlo 闭环评估：成功率、碰撞/不安全率、回报、制动距离和最终速度。

验收标准：相同种子结果可复现；所有对照方法输出同一套指标与文件结构。

### 阶段 1：真实图像语义抽象与校准

状态：进行中。

任务：

- [x] 用真实图像训练 $E_\phi(o)\to(\hat d,\hat\sigma)$。
- [x] 严格分离训练、校准和测试数据。
- [x] 实现全局 split-conformal 和预测距离条件的 Mondrian conformal。
- [ ] 完成至少 5 个随机种子的重复实验并汇总均值、标准差、最小值和最大值。
- [ ] 检查每个距离分箱的 coverage/width，若远距离持续欠覆盖，比较绝对残差校准、更多分箱/更少分箱和保守尺度下限。
- [ ] 绘制预测散点图、残差图、coverage-width 图和分箱结果图。
- [ ] 将校准后的语义契约接入组合网络接口。

验收标准：点估计明显优于旧 `state_net`；多种子平均边际覆盖率接近或不低于目标；报告覆盖率不足的分箱，不隐藏失败结果。

### 阶段 2：语义控制器与感知不确定性增广

状态：未开始。

任务：

- [ ] 将控制器输入显式改为 `[d_semantic, v]`，移除验证路径中的 cGAN 图像生成器。
- [ ] 建立三种训练对照：真实语义、点估计语义、集合增广语义。
- [ ] 从 $\mathcal C_\xi(o)$ 随机采样或采用端点/PGD worst-case 语义误差训练控制器。
- [ ] 引入相对上一轮策略的 trust-region 约束。
- [ ] 评估任务回报、经验不安全率、动作偏差和鲁棒性。

验收标准：集合增广控制器在保持任务性能的同时，对语义误差的最坏情况更稳健。

### 阶段 3：状态相关扰动估计与 ambiguity set

状态：未开始。

任务：

- [ ] 重写 `estimate.py`，用批处理生成 $(s,w)$ 数据，避免原实现一次构造 5000×10000 样本导致内存风险。
- [ ] 按距离/速度状态分箱估计 $\hat\mu(w\mid s)$。
- [ ] 保存每个状态格的样本数、均值、方差、分位数和支撑区间。
- [ ] 用留出数据检验状态相关性，并与全局固定分布比较对数似然/校准误差。
- [ ] 构造带置信度的支撑区间/矩 ambiguity set $\mathcal P(s)$。
- [ ] 在策略更新后重新估计扰动模型。

验收标准：统计检验或预测指标表明状态相关模型优于全局模型；每个 ambiguity set 都记录样本量和置信参数。

### 阶段 4：分布鲁棒 SBC 训练与验证

状态：未开始。

任务：

- [ ] 将训练和验证统一为同一扰动模型，消除三角分布/均匀分布不一致。
- [ ] 将下降损失改为
  $$
  \operatorname{ReLU}\!\left(
  \sup_{\xi\in\mathcal C_\xi(s)}\sup_{Q\in\mathcal P(s)}
  \mathbb E_Q[B(f(s,\pi(\xi))+w)]-B(s)+\epsilon
  \right).
  $$
- [ ] 第一版用语义区间端点与扰动支撑盒/矩界计算 worst-case 上界。
- [ ] 加入 Hoeffding 或 empirical Bernstein 有限样本裕量 $\eta_i(N_i,\delta)$。
- [ ] 修复 learner/verifier 的 `reach_prob` 不一致。
- [ ] 用实际局部常数替换 `k_except_l=1.2` 硬编码。

验收标准：训练目标与验证条件一致；每个安全界可追溯到语义裕量、状态离散裕量和统计裕量。

### 阶段 5：自适应验证与联合反例

状态：未开始。

任务：

- [ ] 启用并修复反例 buffer，仅在困难区域重点训练。
- [ ] 将 uniform grid 改为 certify / counterexample / split 的自适应分区。
- [ ] 产生联合反例 $(s^*,\xi^*,Q^*,w^*)$。
- [ ] 固定策略更新 SBC，再固定 SBC 对策略做 trust-region 更新。
- [ ] 记录验证 cell 数、split 数、反例数、峰值显存和验证时间。

验收标准：在相同验证精度下，自适应方法减少 cell 数或验证时间；反例能够稳定推动 margin 改善。

### 阶段 6：完整消融、统计分析与论文更新

状态：未开始。

至少完成以下对照：

1. cGAN-SafePVC vs Semantic-SafePVC；
2. 精确语义 vs 点估计语义 vs conformal 集合；
3. 固定全局扰动 vs 状态相关扰动；
4. 经验分布 vs ambiguity set；
5. 无统计裕量 vs 有统计裕量；
6. uniform grid vs adaptive partition；
7. fixed controller vs joint update vs trust-region alternating update。

每组至少报告：`p_certified`、经验不安全率、任务回报、验证时间、cell 数、反例数、感知 MAE/RMSE、coverage、区间宽度，并运行多个随机种子。

论文更新任务：

- [ ] 重写问题定义和假设，明确区分感知不确定性与过程扰动。
- [ ] 写出感知契约、ambiguity contract 和 robust SBC 三层保证。
- [ ] 更新总体框架图与 Algorithm 1。
- [ ] 重写实验设置、主结果、消融、局限性和威胁有效性。
- [ ] 确保论文中的每个理论量都能在代码与实验配置中找到对应实现。

## 5. 实验文件与命名规范

建议目录：

```text
artical-F122/results/
├── baseline_original/
│   ├── seed_<n>/config.json
│   ├── seed_<n>/train.log
│   └── seed_<n>/metrics.json
├── semantic_stage1/
├── robust_policy_stage2/
├── disturbance_stage3/
├── robust_sbc_stage4/
└── ablations/
```

每次实验必须保存：

- Git commit 或关键源码哈希；
- 命令行与完整配置；
- 随机种子、设备和依赖版本；
- 原始日志与 `metrics.json`；
- 最优与最终 checkpoint；
- 失败原因、异常和人工决策。

禁止只记录最佳结果；多种子汇总必须同时报告均值、标准差、最小值和最大值。

## 6. 风险与应对

| 风险 | 影响 | 应对 |
|---|---|---|
| 仅 400 张真实图像 | coverage 方差大、容易过拟合 | 固定独立校准/测试集，多种子重复，必要时补采数据 |
| 远距离分箱欠覆盖 | 条件安全契约不可靠 | Mondrian/绝对残差校准、保守宽度、补充远距离样本 |
| X-Plane 代码缺失 | 无法完整复现论文双基准 | 先完成 CARLA；将 X-Plane 列为外部依赖，不伪造结果 |
| DRO 过度保守 | 无法获得非平凡证书 | 从透明的区间/矩集合开始，逐步调半径并报告性能-保守性曲线 |
| 自适应验证仍爆炸 | 运行时间过长 | 优先分裂 margin 小且界宽的 cell，设置资源上限并记录 UNKNOWN |
| 基线本身不确定/有 bug | 对照不公平 | 同时保留“原实现基线”和“修复但未改方法基线” |

## 7. 最近下一步

按优先级执行：

1. 完成语义模块的 5 个随机种子实验，汇总 global 与 Mondrian coverage。
2. 将原始基线改为可设种子、可设迭代数并输出逐轮 JSON，再跑 5 个种子。
3. 实现统一闭环 Monte Carlo 评估器，作为所有后续阶段的固定性能/安全评价工具。
4. 根据多种子 coverage 结果决定阶段 1 是否验收；未达标则先修正校准，不提前进入形式证书集成。
5. 阶段 1 验收后，将语义区间接入控制器并开展阶段 2 三组对照。

## 8. 进度摘要

| 阶段 | 状态 | 当前结论 |
|---|---|---|
| 论文/源码审计 | 已完成 | 三个理论缺口与多项实现问题均已定位 |
| 服务器环境 | 已完成 | `vt` + RTX 4090 可正常运行 |
| 阶段 0 基线 | 进行中 | 单次原实现跑通；待确定性、多种子和闭环评估 |
| 阶段 1 语义抽象 | 进行中 | 点估计显著改善；coverage 尚未稳定达标 |
| 阶段 2 鲁棒语义控制器 | 未开始 | 依赖阶段 1 验收 |
| 阶段 3 状态相关扰动 | 未开始 | 可与阶段 2 后半并行设计 |
| 阶段 4 分布鲁棒 SBC | 未开始 | 依赖阶段 1-3 接口稳定 |
| 阶段 5 自适应 CEGIS | 未开始 | 依赖阶段 4 基础验证器 |
| 阶段 6 实验与论文 | 未开始 | 持续积累素材，最终统一完成 |
