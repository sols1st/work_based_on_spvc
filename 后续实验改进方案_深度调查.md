# SafePVC AEBS 后续实验改进方案：代码审查、失败诊断与最小可行路线

> 这份文档回答三个问题：现在为什么一直失败、还有没有可能做成、下一步最少应该做什么。
>
> 结论依据分为四类：`[实验事实]` 已保存的运行结果；`[代码事实]` 当前仓库实现；`[论文条件]` 原论文定理；`[分析判断]` 根据前三类证据作出的推断。
>
> 本文只制定方案，没有启动新训练、没有做多随机种子实验，也没有修改服务器上的实验结果。

## 0. 一页结论

实验还能继续，也有完成的可能，但不应该继续用“换激活函数、加 loss 权重、再训练一版 barrier”的方式推进。

当前最重要的新结论是：**已经保存的实验同时混有“评估定义问题”和“证书求解问题”**。在继续判断方法成败前，应先把二者拆开。

1. `[代码事实]` AEBS 环境到达“距离不超过 6m 且速度不超过 0.5m/s”的成功状态后，只加奖励，没有结束回合。只要车辆停在成功区域附近，就可能在后续每一步重复获得成功奖励。
2. `[实验事实]` 安全过滤器虽然把四种测试模式的 `unsafe rate` 都降到 0%，但过滤后的 `exact` 和 `uniform` 平均都跑满 400 步；`random_boundary` 的成功率为 0%，也跑满 400 步。也就是说，“没有进入危险区”不等于“完成了停车任务”，过滤器很可能在部分场景中安全地停滞。
3. `[代码事实]` 安全过滤器始终使用四个感知区间中最大的 `2.7757m` 误差半径，而不是当前观测对应的局部半径。这解释了为什么它非常保守。
4. `[论文条件]` 原论文 Theorem 2.2 只要求在安全状态且 `B(s) <= 1/(1-p)` 时满足下降条件。当前代码却在几乎所有非终止、非危险状态上强制下降。这是比论文更强的条件，因此当前 `violated` 不能直接推出“原论文条件无解”。[^1]
5. `[代码事实]` 当前验证只检查 `80×80` 网格和 3 个感知误差点 `{-1,0,+1}`，没有覆盖网格单元内部，也没有证明感知误差区间内部的最坏值一定出现在端点。因此即使某次输出 `verified`，也只能叫“固定网格通过”，还不能叫连续状态空间的形式化证书。
6. `[分析判断]` 这个系统只有距离和速度两个状态，而且安全过滤器输出非负制动力。在运行区域内，速度不增加、距离持续减小，状态演化近似是单向的。已有离散可恢复性分析还显示“离散可恢复但最终碰撞”的点为 0。这些证据说明：**一个网格上的排序函数或 barrier 很可能存在；现在首先要判断的是它是否存在，而不是继续猜神经网络结构。**

因此，推荐的最短路线是：

```text
修正成功终止与结果分类
          ↓
只重新评估一次 baseline 和 safety filter
          ↓
用线性规划直接判断网格证书是否有解
          ↓
     ┌────┴────┐
   有解         无解
     ↓           ↓
用表格证书/NN拟合  修改控制器或放宽命题
     ↓
最后才做连续域形式化验证
```

这条路线包含两个明确的停止点：

- 修正评估后，如果过滤器仍大量超时或停在目标区外，先改过滤器，不训练 barrier。
- 如果线性规划证明当前控制器、当前状态域和当前下降条件不可行，停止训练 barrier，直接修改控制器或证书命题。

## 1. 当前实验已经真正完成了什么

### 1.1 已完成的工程链路

`[实验事实]` 当前代码已经串通了如下模块：

```text
真实图像 → 距离估计 → 感知误差区间
        → 控制器/安全过滤器 → 状态相关扰动接口
        → barrier 训练 → 网格验证 → 最坏反例报告
```

这是有价值的工程结果。特别是，失败不是程序没有运行，而是程序确实给出了可定位的反例。

### 1.2 感知模块的正向结果

| 指标 | 当前结果 | 正确解读 |
|---|---:|---|
| MAE | 0.1004m | 测试图像的平均绝对距离误差约 10cm |
| RMSE | 0.1934m | 少量大误差使其高于 MAE |
| 最大绝对误差 | 1.2678m | 测试集中最差的单个点 |
| 状态条件契约覆盖率 | 97.5% | 80 个测试样本中约 78 个被区间覆盖 |
| 目标覆盖率 | 95% | 总体测试覆盖率达到目标，但样本很少 |
| 四段半径 | 0.0834 / 2.7757 / 0.1649 / 0.4160m | 第二段异常宽，主导了后续保守性 |

Conformal prediction 的优势是，在数据可交换等前提成立时，可以用独立校准集给出有限样本的边际覆盖保证；它并不要求误差服从高斯分布。[^2] 但“总体覆盖率达到 95%”不自动等于“每个距离段、每种天气、每种速度都达到 95%”。这一区别在本项目中非常重要。

### 1.3 控制器与安全过滤器结果

原始 PPO 在最坏感知端点下不安全：

| 模式 | 原始 PPO unsafe | 重新训练 PPO unsafe | 结论 |
|---|---:|---:|---|
| exact | 0.0% | 0.0% | 精确感知下都安全 |
| uniform | 1.0% | 21.5% | 重新训练明显恶化 |
| random boundary | 17.0% | 28.0% | 重新训练明显恶化 |
| worst endpoint | 100.0% | 100.0% | 重新训练没有解决核心问题 |

因此，重新训练的 PPO 不应作为最终控制器。当前采用的是解析安全过滤器：先获得原 PPO 动作，再用停车距离公式计算最低制动力，取二者较大值。

安全过滤器已有结果：

| 模式 | unsafe：原始 → 过滤 | success：过滤后 | 平均步数：过滤后 | 正确解读 |
|---|---:|---:|---:|---|
| exact | 0.0% → 0.0% | 100.0% | 400.0 | 安全，但没有按成功条件及时结束 |
| uniform | 4.0% → 0.0% | 79.5% | 400.0 | 无碰撞不等于全部成功 |
| random boundary | 12.5% → 0.0% | 0.0% | 400.0 | 典型的“安全停滞/超时” |
| worst endpoint | 100.0% → 0.0% | 100.0% | 339.04 | 最坏端点安全性改善明显 |

这里最值得保留的结果是“已测试回合中没有进入定义的危险区”，而不是当前的 return。由于成功状态没有终止且成功奖励可能重复累加，`405 → 372` 或 `404 → 116` 这些 return 不能可靠表示控制性能。

### 1.4 扰动模型结果

`[实验事实]` 修正后的 process uncertainty 来自确定性仿真器残差，16 个 cell 的上下界全部为 0。这意味着：

- 当前仿真动力学是确定性的；
- 代码已经把“感知误差”和“过程误差”分开；
- 但不能据此声称现实车辆的过程扰动为 0。

原 `03_uncertainty` 的非零项来自感知误差引起的动作变化。如果在 barrier 中已经枚举感知误差端点，再把这部分当作过程扰动加入下一状态，会把同一不确定性计算两遍。因此旧结果只能保留为定义错误的历史负例。

### 1.5 barrier 结果的最好与最终状态

尝试过的主要改动包括：取消强制正输出、分阶段训练、网格点混入训练、最大损失、top-k、完整证书域、目标引导、可恢复域、终止值、epsilon 敏感性、低速 curriculum 和 softplus-square。

目前最值得比较的两版是：

| 版本 | 主 violations | epsilon=0 violations | min margin | 区域条件 | 判断 |
|---|---:|---:|---:|---|---|
| low-speed curriculum | 2576/4483 | 1379/4483 | -0.001793 | init/unsafe/nonnegative 全通过 | 当前神经 barrier 中最接近可用的一版 |
| softplus-square | 2556/4483 | 2089/4483 | -0.005527 | init 仍有 1 点失败 | 主计数只少 20，但真实上升点多 710，整体恶化 |

其中 `epsilon=0` 仍失败，说明问题不仅是严格余量设得太大。当前最好的模型中，真正的上升主要集中在速度 `1–2m/s` 的中速区域；这比继续全域盲调超参数更有诊断价值。

## 2. 为什么之前改了很多次仍然失败

失败并不是单一原因，而是四层问题叠加。

### 2.1 第一层：优化目标只能“鼓励”，不能保证所有点通过

神经网络训练最小化的是平均 loss、top-k loss 或批次最大 loss。它们可以把多数点的误差变小，却不能保证连续状态域中的每一点都满足不等式。

这正是历次结果出现以下现象的原因：

- violation count 降了，最坏 margin 却变差；
- 最坏 margin 变好了，init/unsafe 区域又失败；
- 训练 violation 降到 3.47%，验证仍有 1096 个失败点；
- softplus-square 改善了主计数，却增加了 epsilon=0 的真实上升。

相关工作通常不会把采样训练本身当作证明，而是在训练后使用 bound propagation、线性规划、SMT 或 branch-and-bound 对完整区域验证，再把反例加入训练。[^5][^6][^7]

### 2.2 第二层：当前下降条件比原论文更强

原论文在 `p=0.9` 时的门槛是：

\[
\frac{1}{1-p}=10.
\]

Theorem 2.2 的下降条件只适用于：

\[
s\notin X_u,\qquad B(s)\le 10.
\]

当前 `certificate_masks()` 返回的下降域是：

```python
decrease = ~(terminal | expanded_unsafe)
```

它没有检查 `B(s) <= 10`。因此它要求所有可恢复、非终止状态都严格下降。这个加强条件可以作为更强的工程目标，但不能把其失败等同于原论文定理失败。

这里也不能简单把 `B>10` 的安全点全部删掉，否则网络可以通过把大量困难点抬到 10 以上来逃避检查。正确做法是同时报告：

1. **论文原条件结果**：只在 `B(s)<=10` 的安全点上检查下降；
2. **非退化结果**：统计多少安全/可达点被推到了 `B(s)>10`；
3. **加强条件结果**：如果要求整个可恢复安全域 `B(s)<=10` 且下降，单独标成 strengthened certificate。

### 2.3 第三层：固定网格不是连续域证明

当前验证器在 `80×80` 个状态点上计算数值。如果全部点通过，代码会将状态写成 `verified`。但相邻两个网格点之间仍有无限多个状态，神经网络和控制器可能在中间产生更差值。

原论文 Theorem 3.1 正是为此引入 Lipschitz 常数和离散化补偿项 `K`；论文还说明使用区间传播工具验证区域条件。[^1] 当前 MVP 没有计算该 `K`，也没有用区间上下界覆盖每个网格 cell，所以状态字段更准确的命名应为：

- `grid_pass`：所有抽样网格点通过；
- `grid_violated`：至少一个网格点失败；
- `formally_verified`：只有每个连续 cell 都得到可靠界时才使用。

文献中的可行做法是：对每个状态盒传播神经网络界，再用 branch-and-bound 不断细分无法判定的盒子；有限样本只负责找到候选，完整区域验证负责给出证明。[^5][^6]

### 2.4 第四层：端点枚举未必覆盖连续感知区间

当前对感知误差只计算 `-radius`、`0`、`+radius` 三点。只有当下面的复合映射对感知距离具有可证明的单调性或凸性时，端点最大值才一定等于区间最大值：

\[
\hat d\longmapsto \pi_{filter}(\hat d,v)
\longmapsto f(s,\pi_{filter})
\longmapsto B(s').
\]

这里既包含 PPO，又包含 `max(PPO action, required braking)`，最后还包含神经 barrier；一般不能直接假定整个复合函数单调。于是存在一种可能：两个端点和中点都通过，但区间内部某一点失败。

最小诊断不是立即上形式化工具，而是先在每个反例状态的感知区间内密集取 21 或 41 个点，检查最坏值是否总在端点。如果不是，就必须把连续感知区间纳入区间传播或 branch-and-bound。

## 3. 新发现的评估问题：必须先修，成本最低、影响最大

### 3.1 成功状态没有结束回合

`Aebs/system/env.py` 当前逻辑是：

```python
if d_next <= SAFETY_DIST and v_next <= SAFETY_SPEED:
    reward += 2

if d_next >= 16.0 or d_next <= 5.0 or v_next <= 0.0:
    done = True
```

也就是说，`d<=6` 且 `v<=0.5` 被注释为 success，却没有设置 `done=True`。`robust_controller.evaluate()` 也沿用了相同逻辑：把 `success=True`，但继续最多运行 400 步。

这会产生三个后果：

1. 成功奖励可能在多个时间步重复累积，return 被放大；
2. “已成功但没有停止”和“安全地卡住直到超时”混在一起；
3. 使用这个环境训练 PPO 时，策略优化的是错误的长期奖励结构。

因此，之前 robust PPO 的失败不能全部归因于鲁棒训练思想；它至少部分是在一个终止/奖励定义不一致的环境中训练出来的。

### 3.2 最小修正方式

首先不要改奖励权重，只统一事件定义。建议每一步按以下优先级判定：

```text
1. unsafe：d ≤ 6m 且 v > 0.5m/s
2. success：5m ≤ d ≤ 6m 且 v ≤ 0.5m/s
3. stopped_safe_outside_goal：v = 0 且 d > 6m
4. out_of_domain：d < 5m 或 d > 16m
5. timeout：达到 400 步
```

注意边界必须全项目一致，例如 unsafe 使用 `v > 0.5` 还是 `v >= 0.5`，goal 使用 `v <= 0.5` 还是 `<0.5`，只能选一套。建议让 `v=0.5` 属于成功侧，以避免 success 和 unsafe 重叠。

成功奖励只在第一次进入 success 时加一次，随后立即终止。Gymnasium 中时间上限应使用 `truncated=True`，任务事件使用 `terminated=True`。

### 3.3 修正后必须报告的指标

主表不要再只有 unsafe 和 return，至少加入：

| 指标 | 含义 | 为什么需要 |
|---|---|---|
| success rate | 到目标附近并降到安全速度 | 判断任务是否完成 |
| unsafe rate | 高速进入危险距离 | 判断碰撞风险 |
| stopped-safe-outside-goal rate | 在目标区外提前停死 | 识别过度保守 |
| timeout rate | 400 步仍未结束 | 识别停滞或振荡 |
| time to success | 成功所需时间 | 衡量效率 |
| minimum distance | 轨迹中的最小距离 | 衡量安全余量 |
| filter intervention rate | 过滤器覆盖 PPO 动作的步数比例 | 衡量过滤器有多保守 |
| mean extra braking | 过滤器额外增加的制动力 | 衡量性能代价 |
| return | 修正奖励后的辅助指标 | 不能再作为唯一性能指标 |

## 4. 感知误差契约为什么会出现 2.7757m，以及怎样改

### 4.1 不是简单的“模型很差”，而是小样本分箱放大了极值

总数据只有 400 张：240 train、80 calibration、80 test。状态条件契约的四个校准 bin 分别只有：

```text
24 / 22 / 19 / 15 个样本
```

当前 split-conformal 使用精确顺序统计量：

\[
k=\min\left(\left\lceil(n+1)(1-\alpha)\right\rceil,n\right),
\qquad \alpha=0.05.
\]

对 `n=15,19,22,24`，`k` 都等于 `n`，所以每个 bin 的 95% 半径就是该 bin 中**最大的校准残差**。只要某个 bin 有一个异常样本，整个 bin 都会继承这个最大误差。

要让 95% 分位数不再等于最大值，单个 bin 至少需要 39 个校准样本。四个大致均衡的 bin 至少需要 156 个校准样本，而当前只有 80 个。

因此，第二个 bin 的 2.7757m 很可能是“真实大误差 + 分箱样本太少”共同导致的，而不是应该通过手工删异常点解决。

### 4.2 推荐的最小改法

按成本从低到高：

1. **先检查异常样本本身。** 显示第二个 bin 最大残差对应的图像、标签、预测值和样本索引，确认是标注错误、图像损坏，还是模型真实失败。只有确认数据错误才修标签；不能为了缩小半径删除正常难例。
2. **MVP 改为 2 个可观测分箱。** 80 个 calibration 样本大致每箱 40 个，刚好避免 95% 分位数总是最大值。分箱变量应优先使用预测距离 `hat d`，而不是真实距离，因为部署时不知道真实距离。
3. **保留全局契约作基线。** 同时报告 global、2-bin 和旧 4-bin 的覆盖率、平均宽度、最大宽度，而不是只选最窄的一版。
4. **下一阶段增加独立校准数据。** 如果要保留 4 bin，至少先把 calibration 扩充到 156 个以上；考虑 train/test 后，实际总样本应显著超过这个下限。
5. **数据增加后再考虑 CQR。** Conformalized Quantile Regression 可以让区间宽度适应不同输入区域的异方差，并保持 conformal 的边际覆盖性质；但 400 张图像下直接增加两个分位数网络，未必比 2-bin 更稳定。[^3]

Mondrian conformal regression 的思想是按类别或难度组分别校准，从而获得更有条件性的区间；代价是每组有效样本更少。[^4] 这正说明当前不能继续增加 bin 数。

### 4.3 真实状态分箱与在线部署分箱要分开

当前 `StateConditionalErrorContract.radius(distance_m)` 在验证中用真实 `distance_m` 查 bin。这可用于描述“如果真实状态在这个区域，观测误差属于什么集合”的离线抽象，但真实车辆的 safety filter 并不知道真值。

因此需要明确两个对象：

- **验证用 observation relation**：给定真实状态，所有允许的观测是什么；
- **部署用 prediction interval**：给定图像或预测距离，真实距离可能是什么。

如果要让 safety filter 在线自适应，就应使用按 `hat d`、模型预测 scale 或图像难度校准的区间，不能直接查询真实距离 bin。当前过滤器为了回避这个问题，取全局最大半径，结果虽然保守，却造成明显停滞。

## 5. 安全过滤器怎样从“零危险但会卡住”改成“安全且能完成”

### 5.1 当前过度保守的直接原因

当前代码使用：

\[
d_{conservative}=\hat d-2.7757-0.25.
\]

也就是说，不论当前图像落在哪个误差区间，都先减去 2.7757m，再额外减 0.25m。对于只有 5–16m 的场景，这个扣减非常大，容易要求过早强制刹车。

### 5.2 最小改进顺序

1. 先修评估，不改过滤器参数，获得可信基线。
2. 将全局最大半径替换为可部署的局部置信下界 `d_lower`。
3. 仍保留解析停车距离约束，不重新训练 PPO。
4. 把过滤器目标从“尽量刹车”改成“满足下一步可恢复性所需的最小干预”。

新的动作可以写成：

\[
a_{final}=\max\{a_{PPO},a_{required}(d_{lower},v)\}.
\]

这里结构不变，只把 `d_lower` 从“预测值减全局最大误差”改为“当前图像对应的校准下界”。

安全过滤器研究通常强调把性能控制器与安全约束分开：性能控制器给出期望动作，过滤器只在必要时修改动作。[^9] 这与本项目的解析过滤器方向一致，不需要先回到复杂的 PPO 鲁棒训练。

### 5.3 过滤器的通过标准

同一批固定评估初始条件下：

- 四种模式 `unsafe rate = 0`；
- exact 成功率接近 100%；
- uniform、random-boundary 和 worst-endpoint 的成功率都要单独报告；
- `timeout rate` 和 `stopped-safe-outside-goal rate` 不能被 success 淹没；
- 相比“全局最大半径过滤器”，干预率或额外制动力至少有一项明显下降，且 unsafe 不反弹。

不要先规定一个好看的 return 门槛。先把事件定义修正，再用“安全、成功、超时、干预”四类指标做决策。

## 6. 下一步最关键实验：不用神经网络，直接判断证书有没有解

### 6.1 为什么要做网格线性规划

现在的问题是：

> 是证书本身不存在，还是神经网络训练没有找到它？

继续训练无法回答这个问题。由于系统只有二维状态，完全可以在现有 `80×80` 网格上，把每个网格点的 barrier 值直接当成变量，解一个线性可行性问题。

这一步不需要训练神经网络，也不需要多 seed。它只回答可行性，是当前信息增益最高的实验。

### 6.2 变量和约束

对每个网格点 `i` 定义一个变量 `b_i=B(s_i)`，大约 6400 个变量。

基本约束：

\[
b_i\ge0,\qquad
b_i\le1\quad(s_i\in S_0),\qquad
b_i\ge10\quad(s_i\in X_u\cup X_{inevitable}).
\]

安全终止点固定为：

\[
b_i=0.
\]

对每个需下降状态和每个不确定后继 `s'_{ij}`：

\[
b_i\ge \operatorname{Interp}(B,s'_{ij})+\epsilon.
\]

`Interp` 使用后继所在网格 cell 四个角点的双线性插值。对固定后继位置，插值是四个 `b` 变量的线性组合，因此全部条件可以交给 `scipy.optimize.linprog`、HiGHS、CVXPY 或 Gurobi。

### 6.3 先做一个纯 LP，再按结果决定是否需要 MILP

原论文条件包含逻辑蕴含：只有当 `B(s)<=10` 时才要求下降。这个“要么 `B>10`，要么满足下降”的条件不能直接写成普通线性规划；在网格上精确表达它需要二进制变量和 big-M，属于混合整数线性规划（MILP）。

为了保持最简，第一轮只解一个**非退化加强条件 LP**：

- 对全部可恢复、非终止安全网格点再加 `B<=10`；
- 对这些点都检查 robust decrease。

它对应当前代码真正想达到的更强目标。如果这个纯 LP 可行，就没有必要先引入 MILP；可以直接获得可解释的网格解。

如果纯 LP 不可行，再解一套**论文条件 MILP**：

- init `B<=1`；
- unsafe/inevitable `B>=10`；
- 全域 nonnegative；
- 用二进制变量表达每个安全网格点“`B>10` 或满足下降”；
- 另外统计从初始集 robust 可达的安全点中有多少被置为 `B>10`，防止用高 barrier 值逃避下降检查。

若 MILP 可行而加强 LP 不可行，就能明确写成：“原论文的带门槛 SBC 条件在网格上可行，但全可恢复域严格下降的加强条件过强。”

### 6.4 epsilon 的最小测试

不要扫描很多值。固定顺序：

1. `epsilon=0`：判断是否至少存在非增函数；
2. 若可行，直接最大化公共 `epsilon`；
3. 报告求得的 `epsilon_max`，再判断是否达到 `1e-4`、`5e-4` 或 `1e-3` 的量级。

最大化 epsilon 比手工反复运行 0.001、0.005、0.01 更直接。

### 6.5 LP 结果如何决策

| LP 结果 | 说明 | 下一步 |
|---|---|---|
| epsilon=0 都不可行 | 当前控制器、域、终止值或 robust 后继存在结构冲突 | 导出冲突点，停止训练 barrier，改控制器/命题 |
| epsilon=0 可行，epsilon>0 不可行 | 可能有平环、离散化停滞或严格下降要求过强 | 检查状态图强连通分量，考虑多步条件 |
| epsilon>0 可行 | 证书在网格上存在，当前失败主要是神经优化/表示问题 | 直接使用网格证书，或监督拟合 NN 后再验证 |
| 论文条件 MILP 可行、加强 LP 不可行 | 原论文条件可能可证，加强条件不可证 | 按论文条件实现 verifier，并报告加强条件负结果 |

### 6.6 为什么当前系统有可行希望

`[代码事实]` safety filter 的输出是 `max(baseline_action, required_braking)`，而 `required_braking>=0`，因此最终动作不小于 0。动力学为：

\[
d_{t+1}=d_t-v_t\Delta t,
\qquad
v_{t+1}=\operatorname{clip}(v_t-a_t\Delta t,0,3).
\]

所以在 `v>0.5` 的运行域里：

- `v` 不增加；
- `d` 每步至少下降 `0.5×0.05=0.025m`；
- 在零过程扰动的当前 MVP 中，不会形成真正的状态循环。

`[实验事实]` 离散可恢复域分析还得到：discrete recoverable but unsafe = 0。这不是证书存在的证明，但说明可以尝试构造“到安全终止还需多少步”的排序函数。相比继续改变 softplus，这是一条有动力学依据的路线。

## 7. 如果 LP 可行：证书应该怎样实现

### 7.1 最简方案：直接把网格表当证书候选

对二维 AEBS，不必执着于神经网络。可以保存：

- 距离网格；
- 速度网格；
- 每个点的 `B` 值；
- 双线性插值规则。

优点是可解释、可视化容易、不会再受神经网络训练不稳定影响。缺点是后续连续域证明仍需给插值和闭环映射做界。

### 7.2 次选：用 LP 解监督训练一个小网络

如果论文必须保留 neural SBC，可以把 LP 得到的 `B_grid` 当监督标签：

\[
L=L_{fit}(B_\theta,B_{grid})+\lambda L_{certificate}.
\]

这样网络不是从随机初始化猜一个危险地图，而是先逼近已知可行形状。拟合后仍要重新形式化验证；拟合误差可能破坏证书，不能因为 MSE 小就宣布通过。

### 7.3 更符合动力学的结构

可加入物理特征：

\[
m(d,v)=d-d_{safe}-D_{stop}^{discrete}(v),
\]

其中 `m` 是可恢复余量。它比单独输入距离和速度更直接地表达“还来得及刹车吗”。

但不能简单令 `B=h(m)` 并假设一定严格下降。最大制动时停止距离余量可能近似保持不变，因此一阶严格下降仍可能为 0。更合理的是从 robust 动态规划或到达终止集的剩余步数构造 value/ranking function，再让网络拟合。

### 7.4 反例迭代只在“验证器可靠”后使用

CEGIS 的标准逻辑是：训练候选 → 完整验证 → 返回真实反例 → 加入训练 → 重复。已有研究用 SMT 或 branch-and-bound 实现这一闭环。[^6][^7]

当前 fixed-grid 的最坏点可以帮助诊断，但它不是完整域反例器。应先有 LP 可行解和连续域验证器，再做最多少量 CEGIS 迭代；不要再做 top-k、activation、权重的开放式组合。

## 8. 如果 LP 不可行：该改什么，而不是继续训练什么

### 8.1 先找结构冲突

LP 不可行时输出或近似定位：

- 哪些安全点一步可能进入 unsafe/inevitable；
- 哪些状态之间构成零下降环；
- 哪个区域同时被要求 `B<=1` 和 `B>=10`；
- 哪个后继插值落在域外或错误终止区域；
- 中速 `1–2m/s` 是否构成主要冲突带。

如果 solver 支持 IIS（不可约不一致约束集），直接导出 IIS；否则逐区域关闭约束做二分定位。

### 8.2 优先修改安全过滤器，不回到鲁棒 PPO

如果某些可恢复点在感知误差下仍会选择过小制动力，应把过滤器改为保证“一步后仍处于可恢复集”的最小动作：

\[
s_{t+1}\in K_{recoverable}\quad\text{for every allowed observation.}
\]

这比“训练 PPO 希望它平均更安全”更直接。安全与性能分离也便于统计过滤器干预成本。相关工作表明，可以直接学习或构造对控制输入的安全约束，而不必依赖一个预先完美的 certificate。[^9]

### 8.3 必要时做控制器与证书联合修改

证书是否存在与控制器紧密相关。如果固定策略把某些安全状态带向危险区，再强的 barrier 网络也不能证明该策略安全。联合学习策略和证书在文献中是一个明确方向。[^8]

但本项目的最小版本不建议立即实现完整联合 RL。先用 LP 反例约束过滤器动作；只有解析过滤器无法兼顾安全和成功时，再考虑联合优化。

### 8.4 多步证书是后备路线，不是第一步

若系统轨迹整体安全，但 barrier 某一步允许短暂上升，一步严格下降条件可能过强。k-inductive barrier 允许在若干步窗口内建立下降关系，近期研究正是为解决“一步条件过于严格”而提出这一方向。[^10]

它会改变理论命题和验证器，工作量明显更大。因此只有在以下证据同时成立时再考虑：

- epsilon=0 的一步 LP 不可行；
- robust rollout/reachability 显示轨迹仍安全；
- 不可行原因确实是短期上升而非进入 unsafe。

## 9. 从固定网格走向真正形式化验证

### 9.1 三种层级必须分开写

| 层级 | 可以声称什么 | 不能声称什么 |
|---|---|---|
| rollout | 测试的若干轨迹未出事 | 所有初始状态、所有误差都安全 |
| grid pass | 所选网格点都满足数值条件 | 网格之间的连续状态也满足 |
| formal continuous certificate | 在给定模型、状态域和不确定性假设下完整验证 | 超出模型和数据假设的真实世界安全 |

### 9.2 连续域验证的推荐实现

对每个网格 cell `C`，需要证明：

\[
\inf_{s\in C}
\left[B(s)-\sup_{\xi\in\mathcal C(s)}B(F(s,\xi))-\epsilon\right]
\ge0.
\]

实现路线：

1. 用 interval bound propagation/CROWN 得到 `B(s)`、PPO 动作和 `B(next)` 的上下界；
2. 对无法判定的 cell 二分；
3. 同时细分状态盒和感知误差区间；
4. 对每个 cell 输出 `certified / violated / unknown`，不能把 unknown 当通过；
5. 先在二维 AEBS 上完成，再考虑更复杂 benchmark。

神经 barrier 的正式验证通常依赖 bound propagation、线性松弛和 branch-and-bound；这类方法已用于随机系统 barrier 和 verification-in-the-loop 训练。[^5][^6]

### 9.3 原论文中的期望与当前 worst-case max 不是同一个对象

原论文条件是对扰动分布的期望：

\[
B(s)\ge \mathbb E[B(s')]+\epsilon.
\]

当前 MVP 计算的是有限候选点中的最大值：

\[
B(s)\ge \max_j B(s'_j)+\epsilon.
\]

如果候选集合严格包住所有可能后继，worst-case 条件比任意分布的期望条件更强；但当前只有几个候选点，既没有完整连续 support，也没有构造概率分布或分布歧义集。因此它目前应称为“有限候选的鲁棒下降检查”，还不是完整的 distributionally robust SBC。

推荐顺序：

1. 先在确定性过程模型 + 连续感知区间上做 worst-case 证书；
2. 有真实状态转移残差后，再拟合 `Q(w|s)` 或歧义集合；
3. 最后实现期望上界或 Wasserstein/矩约束优化。

## 10. 过程不确定性下一阶段怎样补

### 10.1 当前零残差能说明什么

它只能说明：用于生成数据和预测下一状态的是同一个确定性公式。它不能检验路面附着、制动延迟、传感器时序、执行器误差或未建模动力学。

### 10.2 最少需要采什么数据

每条记录至少包含：

```text
(d_t, v_t, commanded_action, d_{t+1}, v_{t+1}, dt, scenario_id)
```

残差定义为：

\[
w_t=s_{t+1}^{observed}-f(s_t,a_t).
\]

必须保证这里的 `a_t` 是实际执行动作，感知误差造成的动作变化不再被重复记作 `w_t`。

### 10.3 MVP 建模方式

先不做复杂生成模型：

- 仍用 `4×4` 状态 cell；
- 每个 cell 报告样本数、均值、协方差和支持区间；
- 样本少的 cell 合并，不能用 1–2 个点声称状态条件分布；
- 留出独立转移测试集，报告 support miss rate；
- 如果仿真能注入制动系数/延迟，训练和测试要使用独立场景参数。

等有真实非零残差后，再讨论概率期望、分布漂移和数据驱动安全验证。数据驱动 barrier 文献可以提供如何从有限轨迹构造概率保证的参考，但这不能替代本项目自己的数据覆盖审查。[^11]

## 11. 推荐的详细实施计划

### Phase A：先让实验回答正确的问题

预计工作量：0.5–1 天。影响：最高。

- [ ] 在一个共享函数中统一 success、unsafe、stopped-safe、timeout、out-of-domain 判定。
- [ ] success 第一次发生时立即 `terminated=True`，奖励只加一次。
- [ ] timeout 使用 `truncated=True`。
- [ ] `AebsEnv.step()` 和 `robust_controller.evaluate()` 调用同一事件函数。
- [ ] 增加边界单元测试：`d=6`、`v=0.5`、`d=5`、`v=0`。
- [ ] 固定原 200 个初始条件，只重新评估 baseline 和现有 safety filter 一次。
- [ ] 输出五类 episode outcome、最小距离、成功时间、干预率和额外制动力。

Phase A 完成标准：所有回合只属于一个互斥终局类别，类别比例之和为 100%，成功回合不再跑满 400 步。

### Phase B：修正感知契约的可部署性

预计工作量：1–2 天，不含新增采集。

- [ ] 导出 2.7757m 半径对应的校准异常样本。
- [ ] 确认标签和图像是否有效，保留所有真实难例。
- [ ] 增加 global 与 2-bin prediction-conditioned contract。
- [ ] 使用同一个未触碰 test set 比较 coverage、mean/median/max width。
- [ ] 记录每个 bin 的 calibration/test 数量，不用 4-bin 主结果掩盖小样本。

Phase B 完成标准：选择规则在看 test 结果前确定；目标覆盖率不下降；区间明显窄于旧 4-bin 全局最大使用方式。

### Phase C：只改一次安全过滤器

预计工作量：1–2 天。

- [ ] 过滤器接收当前样本的 `distance_lower_bound`，不再固定减 2.7757m。
- [ ] 保留同一停车距离公式和同一 baseline PPO。
- [ ] 加入干预率日志。
- [ ] 用 Phase A 的相同初始条件评估 old-global-filter 与 adaptive-filter。

Phase C 决策门：如果 unsafe 仍为 0 且 success/timeout 显著改善，固定该控制器；如果 unsafe 反弹，回到 lower bound 校准，不能手工缩小半径。

### Phase D：证书 LP 可行性实验

预计工作量：2–4 天。

- [ ] 新建 `Aebs/mvp/grid_certificate_lp.py`。
- [ ] 复用现有 grid、masks、robust successors 和 terminal value。
- [ ] 先实现零过程扰动和 3 个 semantic 样本的兼容诊断。
- [ ] 先解加强条件纯 LP；只有它不可行时才解论文条件 MILP。
- [ ] 保存 `b_grid.npy`、solver status、epsilon_max、冲突状态和热力图数据。
- [ ] 用 21 点 semantic dense scan 检查端点假设。

Phase D 完成标准：得到明确的 `feasible/infeasible`，而不是再得到一个平均 loss。

### Phase E：根据 LP 结果二选一

如果 LP 可行：

- [ ] 先用表格 + 双线性插值作为 grid certificate；
- [ ] 如必须保留 neural SBC，再监督拟合 LP 值；
- [ ] 报告拟合前后每条约束的最大误差。

如果 LP 不可行：

- [ ] 导出最小冲突区域；
- [ ] 检查是否为中速带、感知区间内部或一步进入 inevitable；
- [ ] 只修改安全过滤器的最低动作约束；
- [ ] 仍不可行时才评估多步证书。

### Phase F：连续域形式化验证

预计工作量：1–3 周，取决于现有验证工具兼容性。

- [ ] 将状态域划分为二维盒；
- [ ] 对 barrier、controller/filter 和 dynamics 做区间界传播；
- [ ] 对 unknown cell 做 branch-and-bound；
- [ ] 加入离散化/Lipschitz remainder；
- [ ] 把状态名从 `verified` 拆成 `grid_pass` 和 `formally_verified`。

Phase F 完成标准：每个 cell 都为 certified，或如实报告 remaining unknown；不能用网格点全通过代替。

### Phase G：真实过程不确定性

这是完整论文路线，不属于当前最简 MVP。

- [ ] 收集独立状态转移数据；
- [ ] 做 coverage 和 support miss 检查；
- [ ] 构造状态条件分布或歧义集；
- [ ] 将 max-support 或 expectation bound 接入证书；
- [ ] 重新进行连续域验证。

## 12. 建议的文件级修改地图

| 文件 | 修改内容 | 是否需要训练 |
|---|---|---:|
| `Aebs/system/env.py` | 统一终局事件、success 立即终止、timeout 语义 | 否 |
| `Aebs/semantic/robust_controller.py` | 复用事件函数，报告五类 outcome | 否 |
| `Aebs/semantic/safety_filter.py` | 支持每个观测的 lower bound，记录干预 | 否 |
| `Aebs/semantic/conformal.py` | 增加 prediction-conditioned 2-bin contract | 只需重新校准 |
| `Aebs/semantic/train.py` | 导出异常样本诊断和 coverage-width 表 | 视是否复用 checkpoint |
| `Aebs/mvp/robust_sbc.py` | 增加 theorem gate；状态名改为 grid 级；dense semantic 检查 | 否/验证 |
| `Aebs/mvp/grid_certificate_lp.py` | 新增直接可行性 LP | 否 |
| `Aebs/mvp/report.py` | 增加 outcome、LP、grid/formal 分级报告 | 否 |
| `Aebs/uncertainty/collect.py` | 后续读取真实 transition residual | 需真实数据 |

## 13. 最小实验命令设计

这一节是命令接口建议，不代表现在运行。

### 13.1 修正后的控制评估

```bash
/opt/miniconda3/bin/conda run --no-capture-output -n vt \
  python -m Aebs.semantic.evaluate_controllers \
  --baseline Aebs/controller/best_model/best_model.zip \
  --filter results/mvp/02_safety_filter/safety_filter.json \
  --semantic-checkpoint results/mvp/01_semantic/semantic_encoder.pt \
  --episodes 200 \
  --seed 7 \
  --output results/mvp/02_safety_filter_corrected_eval
```

### 13.2 网格证书可行性

```bash
/opt/miniconda3/bin/conda run --no-capture-output -n vt \
  python -m Aebs.mvp.grid_certificate_lp \
  --controller results/mvp/02_safety_filter/safety_filter.json \
  --semantic-checkpoint results/mvp/01_semantic/semantic_encoder.pt \
  --uncertainty results/mvp/03_process_uncertainty/state_dependent_uncertainty.npz \
  --grid-size 80 \
  --epsilon-mode maximize \
  --condition strengthened \
  --output-dir results/mvp/04_grid_certificate_lp
```

### 13.3 如何查看

```bash
/opt/miniconda3/bin/conda run --no-capture-output -n vt \
  python -m Aebs.mvp.report --results results/mvp
```

报告第一屏应直接给出：

```text
Evaluation semantics: corrected / legacy
Controller outcomes: success / unsafe / stopped_safe / timeout
Certificate level: rollout / grid / formal
LP feasibility: feasible / infeasible
epsilon_max: ...
continuous semantic interval checked: yes / no
```

## 14. 明确不再做的事情

- 不做多随机种子，直到单次运行的定义和逻辑正确。
- 不再尝试 softplus、square、权重 100、top-k 比例等 barrier 组合。
- 不通过继续删除低速、目标、不可恢复之外的状态来降低 violations。
- 不手工缩小 2.7757m 半径来换安全过滤器表现。
- 不把 deterministic simulator 的 0 residual 写成“真实过程没有不确定性”。
- 不把 `80×80` 网格通过称为形式化证明。
- 不在端点单调性未证明时把 3 点枚举称为连续感知区间最坏情况。
- 不在 success/timeout 分开前用 return 得出控制器优劣结论。

## 15. 三种可以完成的成果层级

### 路线 A：可信的实证改进论文

目标：正确的感知区间 + 自适应安全过滤器 + 完整 outcome 评估。

完成可能性：高。现有代码已接近，只需先修评估并降低无效保守性。

可声称：在固定 AEBS 模型和测试误差模式下，过滤器显著降低危险率，同时报告成功与性能代价。

不可声称：获得完整连续域概率安全证明。

### 路线 B：二维 AEBS 网格证书

目标：通过 LP 获得可行的网格 barrier，并在密集网格/感知扫描上验证。

完成可能性：中等偏高。依据是二维、确定性、单向动力学和现有离散可恢复性结果，但最终取决于 LP。

可声称：在指定离散抽象和不确定候选集上得到证书。

不可声称：未经 cell bounds 的连续域证明。

### 路线 C：完整 Semantic-DR 连续形式化证书

目标：可部署语义区间 + 真实状态条件过程扰动 + 连续区间/branch-and-bound 验证 + 概率条件。

完成可能性：存在，但不确定，工作量最大。它需要真实转移数据和完整验证工具链，不能通过一两次 barrier 训练完成。

这三条路线不是互斥的。最合理的是 A → B → C，每一级都有可单独提交和汇报的结果。

## 16. 最终建议：下一次只做两个动作

为了保持“最简实现”，下一轮不要训练任何模型，只做：

1. **修正并重新评估一次控制器终局指标。** 这是判断 safety filter 是否真正完成任务的最低前提。
2. **实现并运行一次网格 LP 可行性。** 这是判断 barrier 还能不能做成的最直接证据。

这两个结果会把后续选择变成清楚的四格表：

| 控制器完成任务？ | LP 有证书？ | 结论 |
|---|---|---|
| 是 | 是 | 固定控制器，进入表格/神经拟合与连续验证 |
| 是 | 否 | 控制器实证可用，但当前一步证书命题不合适 |
| 否 | 是 | 证书定义只覆盖 safety，没有覆盖任务完成；先修性能 |
| 否 | 否 | 停止 barrier 调参，重新设计过滤器/控制目标 |

这比再生成一个 `04_robust_sbc_xxx` 目录更接近“完成实验”。

## 17. 资料来源与证据边界

本文对当前结果和代码行为的判断来自本地仓库、已保存 metrics 以及原论文；外部文献只用于说明可采用的方法，不代表这些方法已经在本项目中实现。

[^1]: 原论文 *Provably Probabilistic Safe Controller Synthesis for Vision-Based Neural Network Control Systems*，Theorem 2.2、Theorem 3.1 及 Section 3.2，见本项目 `720_file_Paper.pdf`。
[^2]: Anastasios N. Angelopoulos and Stephen Bates, [A Gentle Introduction to Conformal Prediction and Distribution-Free Uncertainty Quantification](https://arxiv.org/abs/2107.07511), 2021.
[^3]: Yaniv Romano, Evan Patterson, and Emmanuel Candès, [Conformalized Quantile Regression](https://proceedings.neurips.cc/paper/2019/hash/5103c3584b063c431bd1268e9b5e76fb-Abstract.html), NeurIPS 2019.
[^4]: Henrik Boström and Ulf Johansson, [Mondrian Conformal Regressors](https://proceedings.mlr.press/v128/bostrom20a.html), COPA 2020.
[^5]: Frederik Baymler Mathiesen, Simeon Calvert, and Luca Laurenti, [Safety Certification for Stochastic Systems via Neural Barrier Functions](https://arxiv.org/abs/2206.01463), 2022.
[^6]: Xinyu Wang et al., [Simultaneous Synthesis and Verification of Neural Control Barrier Functions through Branch-and-Bound Verification-in-the-loop Training](https://arxiv.org/abs/2311.10438), 2023.
[^7]: Alessandro Abate et al., [Quantitative Verification with Neural Networks](https://arxiv.org/abs/2301.06136), 2023–2026.
[^8]: Haitong Ma et al., [Joint Synthesis of Safety Certificate and Safe Control Policy Using Constrained Reinforcement Learning](https://proceedings.mlr.press/v168/ma22a.html), L4DC 2022.
[^9]: Will Lavanakul et al., [Safety Filters for Black-box Dynamical Systems by Learning Discriminating Hyperplanes](https://proceedings.mlr.press/v242/lavanakul24a.html), L4DC 2024.
[^10]: John Wooding et al., [k-Inductive Neural Barrier Certificates for Unknown Nonlinear Dynamics](https://arxiv.org/abs/2605.20108), 2026. 这是较新的后备方向，本文没有把它当作当前最简路线。
[^11]: K. Salamati and M. Zamani, [Data-Driven Safety Verification of Stochastic Systems via Barrier Certificates](https://proceedings.mlr.press/v168/salamati22a.html), L4DC 2022.

### Sources

- [原论文（本地 PDF）](./720_file_Paper.pdf)：当前 SBC 定理、离散验证补偿和整体 SafePVC 流程的直接来源。
- [Conformal prediction 入门与有限样本保证](https://arxiv.org/abs/2107.07511)：用于区分边际覆盖、条件覆盖和适用前提。
- [随机系统 neural barrier 的 bound propagation 与 branch-and-bound](https://arxiv.org/abs/2206.01463)：用于连续区域证书路线。
- [Verification-in-the-loop neural barrier](https://arxiv.org/abs/2311.10438)：用于说明为什么训练样本通过不等于连续域验证通过。
- [CEGIS neural certificate](https://arxiv.org/abs/2301.06136)：用于反例驱动训练与形式化验证闭环。
- [安全证书与控制策略联合合成](https://proceedings.mlr.press/v168/ma22a.html)：用于固定策略不可证时的后续路线。
