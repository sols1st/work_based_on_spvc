# SafePVC 论文改进实验阶段汇报

> 汇报日期：2026-09-12  
> 实验对象：CARLA Emergency Braking（AEBS）  
> 原论文：*Provably Probabilistic Safe Controller Synthesis for Vision-Based Neural Network Control Systems*  
> 当前状态：最简 MVP 已完整实现并运行；感知契约与安全过滤器取得正向结果，robust SBC 未通过验证，实验在此收口。

## 1. 一句话结论

本阶段完成了“语义感知契约 + 鲁棒安全控制 + 状态相关扰动接口 + robust SBC 训练与验证”的最简闭环实现。语义距离估计达到 `0.1004m MAE`，状态条件感知契约达到 `97.5%` 测试覆盖率；解析 safety filter 在四种感知误差模式下都把 unsafe rate 降到 `0%`。但是，训练得到的神经 robust SBC 在固定网格上仍有大量 decrease violations，因此**当前只能证明实验闭环已跑通，不能声称已经得到形式安全证书**。

## 2. 为什么要改原论文

原 SafePVC 的主要流程是：

```text
state/environment -> cGAN image -> distilled MLP -> PPO controller
                  -> fixed disturbance distribution -> neural SBC -> verifier
```

原方法希望通过 stochastic barrier certificate（SBC）给出无限时域的概率安全保证，但存在三个值得改进的问题：

1. cGAN 和蒸馏 MLP 引入了两层视觉近似误差，这些误差没有被清晰地纳入证书。
2. 原文把环境影响统一建模成与状态无关的固定扰动分布，难以反映不同状态下误差大小不同的事实。
3. 训练、扰动估计与最终验证之间可能使用不同的误差口径，容易产生 train-verification mismatch。

原始改进设想很完整，还包括 Wasserstein ambiguity set、自适应状态划分、统计置信修正、联合反例和 trust-region controller 更新。本阶段为了先跑通，只实现其中最关键、可由现有 AEBS 数据支持的 MVP 子集。

## 3. 本阶段最简实现范围

最终实际实现的链路为：

```text
真实图像
  -> semantic encoder 估计障碍物距离
  -> conformal calibration 构造状态条件距离区间
  -> controller / analytic safety filter
  -> semantic 区间端点 worst-case 闭环
  -> 4x4 状态相关 process uncertainty 查询接口
  -> robust barrier 训练
  -> 80x80 固定网格 verifier
  -> verified / violated + worst counterexample
```

为控制工作量，本阶段固定：

- 一个 benchmark：AEBS；
- 一个随机种子：seed 7；
- 一个语义变量：障碍物距离；
- 一个固定 `80×80` 验证网格；
- 不做多 seed、超参数扫描和额外 benchmark；
- 不实现现有数据无法支撑的完整 distributionally robust ambiguity set。

## 4. 完成了哪些代码改进

### 4.1 用语义编码替代生成图像链路

原论文采用“状态生成图像，再把生成器蒸馏成 MLP”的路线。本阶段直接训练：

$$E_\phi(o)\rightarrow \hat d,$$

其中输入是真实图像，输出是控制直接需要的障碍物距离。这样删除了图像生成和生成器蒸馏造成的额外近似层。

### 4.2 建立状态条件 semantic contract

不把语义模型输出当作完全准确的点估计，而是在独立 calibration set 上计算误差，按距离状态分箱构造：

$$d^{\rm true}\in[\hat d-r_i,\hat d+r_i].$$

训练和验证时显式枚举区间中心与两个端点，使感知误差直接进入闭环检查。

### 4.3 尝试鲁棒语义控制器，并增加 safety filter

首先尝试用 exact、区间内部随机点和区间端点混合训练 PPO，并进行轻量行为蒸馏。该控制器结果变差，因而没有作为最终 controller。

随后实现解析 safety filter。它根据当前距离、速度、语义误差最坏端点和制动动力学，计算最低必要制动力，并使用：

$$u_{\rm safe}=\max(u_{\rm baseline},u_{\rm required}).$$

最终 barrier 与 verifier 使用的是 safety-filter controller，而不是失败的 robust PPO。

### 4.4 分离 semantic uncertainty 与 process uncertainty

早期实现曾把“语义误差导致的动作变化”再次当作 process disturbance；而 SBC 已经枚举 semantic endpoints，这会重复计算同一种不确定性。

修正后明确区分：

- semantic uncertainty：感知距离区间，在 controller 输入端枚举；
- process uncertainty：动力学模型预测与真实下一状态之间的 residual，在状态转移后叠加。

同时实现 `4×4` 状态分箱和统一 disturbance box 查询接口。

### 4.5 实现 robust SBC trainer 和 verifier

训练目标包含：

- 全域非负：$B(s)\ge0$；
- 初始区域：$B(s)\le1$；
- unsafe 区域：$B(s)\ge10$；
- robust decrease：

$$B(s)-\max_{\xi,w}B(f(s,\pi(\xi))+w)\ge\epsilon.$$

verifier 对每个网格状态枚举：

- semantic multiplier：`-1, 0, +1`；
- process disturbance：mean 和 support corners；
- 输出 violation 数量、最小 margin、最坏状态和最坏不确定性组合。

### 4.6 修正证书验证域

实验过程中修正了三个会导致错误判断的问题：

1. terminal 自环状态不能要求每步严格下降；
2. 已经不可避免发生碰撞的状态不应作为“可由当前 controller 保证恢复”的 decrease domain；
3. terminal successor 应使用固定 terminal payoff，而不是神经网络在域外任意外推的值。

离散制动距离分析得到：

- operational grid 中 discrete recoverable / unrecoverable：`5419 / 269`；
- initial region 最小离散 recoverability margin：`7.47m`；
- discrete recoverable but rollout-unsafe：`0`；
- safety filter 动作非负，速度单调不增，因此 `v<=0.5m/s` 被定义为全局安全终止带。

修正后最终 decrease verifier 检查 `4483` 个 high-speed、recoverable、nonterminal operational states。

## 5. 主要实验结果

### 5.1 Semantic encoder 与感知契约

| 指标 | 结果 |
|---|---:|
| Test MAE | 0.1004 m |
| Test RMSE | 0.1934 m |
| 最大绝对误差 | 1.2678 m |
| State-conditional contract coverage | 97.50% |
| 目标 coverage | 95% |
| 四个距离分箱半径 | `[0.0834, 2.7757, 0.1649, 0.4160] m` |
| Semantic action MAE | 0.0443 |

成功点：语义估计明显优于原有 real-image state estimate，且 calibration 后测试覆盖率超过目标值。

局限：第二个距离分箱的半径达到 `2.7757m`，说明该区间存在较大误差或少量异常值，会使后续 worst-case 验证明显保守。

### 5.2 Robust PPO 尝试

下表为 baseline → robust PPO：

| 模式 | Unsafe rate | Mean return |
|---|---:|---:|
| exact | 0% → 0% | 405.817 → 282.814 |
| uniform | 1% → 21.5% | 405.485 → 280.708 |
| random boundary | 17% → 28% | 400.651 → 278.523 |
| worst endpoint | 100% → 100% | 241.775 → 165.364 |

结论：该改良没有成功。混合观测训练和轻量蒸馏既没有改善 worst-case safety，又显著损失性能，因此后续不使用该 policy。

### 5.3 Analytic safety filter

下表为 baseline → safety-filter controller：

| 模式 | Unsafe rate | Mean return |
|---|---:|---:|
| exact | 0% → 0% | 405.747 → 372.498 |
| uniform | 4% → 0% | 404.442 → 116.761 |
| random boundary | 12.5% → 0% | 402.440 → 15.293 |
| worst endpoint | 100% → 0% | 241.608 → 330.017 |

这是本阶段最明确的控制成功结果：四种模式下 unsafe rate 均为 `0%`，特别是 worst endpoint 从 `100%` 降到 `0%`。

代价也很明显：uniform 和 random-boundary 模式下 return 大幅下降，说明过滤器比较保守。它实现了“安全优先”，但还没有实现最佳的 safety-performance trade-off。

### 5.4 Process uncertainty

| 指标 | 结果 |
|---|---:|
| 状态 cells | 16 |
| 每个 cell 样本数 | 256 |
| Process residual support | 全部为 0 |

这不是“真实系统完全没有扰动”，而是因为当前 AEBS simulator dynamics 是确定性的，已有数据也没有真实 transition residual。该结果只证明 uncertainty 的变量定义和接口已经修正，不能作为真实过程噪声的统计结论。

### 5.5 Robust SBC 关键实验演进

| 版本 | 检查状态数 | Violations | Min margin | 主要结论 |
|---|---:|---:|---:|---|
| 初始 robust SBC | 6400 | 4170 | -0.529670 | 基础链路跑通，但明显失败 |
| unsigned staged | 6400 | 2886 | -0.624824 | violation 数下降，最坏值变差 |
| grid-train diagnostic | 6400 | 1096 | -1.024010 | 平均违例下降，但牺牲最坏点 |
| hard-case max-loss | 6400 | 4284 | -0.025465 | 最坏幅度改善，但区域约束被破坏 |
| top-k warm-start | 6320 | 975 | -1.065387 | violation 数较少，仍存在严重局部反例 |
| filtered zero-process | 6320 | 901 | -0.105069 | 使用正确 controller/扰动定义，暴露证书域问题 |
| complete domain | 5688 | 3656 | -0.252854 | 区域条件基本满足，decrease 失败 |
| viability domain | 5419 | 3642 | -0.220436 | 删除有理论依据的 inevitable states 后仍失败 |
| terminal value | 5419 | 3655 | -0.053706 | 修正 terminal 外推，最坏幅度改善 |
| low-speed curriculum | 4483 | 2576 | -0.001793 | 区域条件全部满足；最坏幅度很小，但大量微小违例 |
| softplus-square | 4483 | 2556 | -0.005527 | 主违例仅少 20 点，真实上升点明显增加，最终失败 |

说明：这些版本不是为了形成大规模超参数搜索，而是在修复 controller、uncertainty、certificate domain 和 terminal semantics 时进行的逐步诊断。最终已经停止继续增加变体。

## 6. 最终版本做到了什么

最终 `softplus-square` 结果为：

| 指标 | 结果 |
|---|---:|
| 主验证 epsilon | 0.001 |
| 主 violations | 2556 / 4483 |
| 主 min margin | -0.005527 |
| 主 mean margin | 0.407652 |
| epsilon=0 violations | 2089 / 4483 |
| epsilon=0 min margin | -0.004527 |
| Initial violations | 1 / 6400 |
| Expanded unsafe violations | 0 / 6669 |
| Nonnegative violations | 0 |
| Worst state | `d=7.785m, v=1.215m/s` |
| Worst semantic endpoint | `+1` |
| Worst process disturbance | mean，即零 residual |

在最坏点：

$$B(s)=0.089132,\qquad B(s')=0.093660.$$

因此 epsilon=0 时已经有：

$$B(s)-B(s')=-0.004527<0,$$

更不可能满足 epsilon=0.001 的严格条件。这是 verifier 给出的直接反例。

## 7. 最后为什么失败

这里将原因分为“结果直接支持的结论”“合理推断”和“尚未完成的理论工作”。

### 7.1 结果直接支持的原因

#### 原因一：失败不只是 epsilon 取值过大

如果只因为 `epsilon=0.001` 太严格，那么把 epsilon 改为 0 后应基本通过。但最终仍有：

```text
2089 / 4483 states with B(next) > B(s)
```

所以主要问题不是严格余量，而是候选 barrier 在大量状态上真实上升。

#### 原因二：训练达成了区域约束，但没有达成 decrease 条件

最终模型满足：

- nonnegative violations = 0；
- expanded unsafe violations = 0；
- initial 只差 1 个点。

这说明网络可以表达初始区、危险区和非负约束。失败集中在动态 decrease 条件，而不是所有 loss 都没有学会。

#### 原因三：不同 loss 改良存在明显 trade-off

- grid training 和 top-k 能降低 violation 数量，却可能产生更差的局部最坏 margin；
- max-loss 能改善最坏 margin，却会破坏 initial/unsafe region；
- softplus-square 保持非负，却使 epsilon=0 violations 从上一版 `1379` 增加到 `2089`。

因此当前加权 soft loss 会在“平均 decrease、最坏 decrease、initial、unsafe”之间找到折中，而不是同时满足所有硬约束。

#### 原因四：softplus-square 假设被实验否定

该修改原本希望改善小 barrier 区域的梯度。但与 low-speed curriculum 相比：

| 指标 | low-speed curriculum | softplus-square |
|---|---:|---:|
| epsilon=0 violations | 1379 | 2089 |
| epsilon=0 min margin | -0.000793 | -0.004527 |
| epsilon=0.001 violations | 2576 | 2556 |

它只把主 violations 减少 20，却让真实上升点增加 710，最坏 margin 也变差。因此不能继续沿这个方向调参。

### 7.2 根据结果作出的合理推断

#### 推断一：当前固定 controller 与 chosen SBC 条件之间可能存在可行性问题

Safety filter 在有限 rollout 上达到 0% unsafe，并不自动意味着存在一个小型神经网络能够满足当前全域、严格、逐步下降的 SBC 充分条件。SBC 条件是充分条件而非必要条件；系统经验上安全，候选证书仍可能不存在或很难被当前网络表示。

目前不能区分以下两种情况：

1. 合格 barrier 存在，但当前 `2-16-8-1` 网络和非凸训练没有找到；
2. 对当前 policy、验证域、边界目标和严格 decrease 形式，所要求的 barrier 本身不可行。

要严格区分它们，需要约束求解器、解析构造或更完整的可行性分析，不属于本次最简 MVP。

#### 推断二：感知契约中的大半径增加了保守性

四个 semantic radii 中有一个达到 `2.7757m`。验证器必须对整个区间端点负责，即使大误差主要来自少数样本。这会扩大 controller next-state 的 worst-case 范围，使统一 decrease 条件更难满足。

不过最终最坏反例使用 `semantic multiplier=+1`，而 process disturbance 为 0，所以该反例确实来自感知 worst endpoint 下的闭环，不是旧 process-noise double counting 导致的假失败。

#### 推断三：固定小网络和加权损失不是硬约束求解器

当前训练最小化的是采样平均 penalty。低 loss 不代表每个网格点都满足约束；只要多数状态 margin 很大，少量或大量幅度较小的违反仍可能与低平均 loss 共存。这解释了最终 `decrease_loss=0.000582` 很小，但 verifier 仍报告 `57.15%` 训练 violation 和 `2556` 个网格违反。

### 7.3 尚未完成、因此不能声称的内容

- 当前 verifier 是固定网格检查，不是完整连续域形式证明；还没有加入严格 cell-wise Lipschitz remainder。
- 当前 process uncertainty 为确定性 simulator residual，没有真实环境 transition 数据。
- 尚未实现 Wasserstein ambiguity set 与 finite-sample statistical margin。
- 尚未实现 adaptive partition、联合反例 CEGIS 和 trust-region controller update。
- 单次 conformal coverage 不能直接推出无限时域 perception coverage；本阶段只能把区间当作工程 worst-case contract 使用。

这些内容会影响最终理论完备性，但不是当前 `violated` 的借口：即使在现有较简化的 fixed-grid、zero-process-noise 条件下，候选 barrier 也没有通过。

## 8. 哪些地方算成功

本阶段可以明确汇报以下成功点：

1. 完成从 image generation surrogate 到 task-relevant semantic encoder 的实现转换。
2. 完成独立 train/calibration/test 划分和状态条件 conformal contract。
3. Semantic contract 达到 `97.5%` 测试覆盖率，高于 `95%` 目标。
4. 明确发现 robust PPO fine-tuning 无效，没有选择性隐藏负结果。
5. Analytic safety filter 在四种测试模式下均达到 `0%` unsafe。
6. 修正 semantic uncertainty 与 process uncertainty 的重复计算问题。
7. 完成统一的 state-dependent uncertainty API，即使当前真实 process residual 数据缺失。
8. 完成 robust SBC 训练、固定网格验证、最坏联合反例和结构化 metrics 输出。
9. 修正 terminal、unsafe、goal、inevitable state 和 viability domain 的逻辑边界。
10. 最终明确得到“区域约束可满足，但 robust decrease 未满足”的可解释负结论。

## 9. 做到了哪一步

| 模块 | 状态 | 是否可作为论文结果 |
|---|---|---|
| Semantic encoder | 完成 | 可以，正向结果 |
| State-conditional semantic contract | 完成 | 可以，正向结果，但需说明样本量小 |
| Robust PPO | 完成但失败 | 可以作为负结果或消融 |
| Analytic safety filter | 完成且 rollout 成功 | 可以，主要正向结果 |
| State-dependent process uncertainty API | 完成 | 可以作为实现贡献 |
| 真实 process uncertainty 学习 | 未完成，缺数据 | 不能声称完成 |
| Robust SBC trainer | 完成 | 可以作为方法实现 |
| Fixed-grid robust verifier | 完成 | 可以报告 violated 与反例 |
| Valid neural SBC | 未获得 | 不能声称 verified |
| 连续状态域形式证书 | 未完成 | 不能声称完成 |
| 完整 Semantic-DR-SafePVC | 未完成 | 只能称 MVP，不可称完整理论框架 |

## 10. 后续还有哪些可能性

下面是研究层面的可能方向，不属于当前最简实验，也不建议在明天汇报前继续运行。

### 可能性一：用停止距离构造解析 barrier，而不是继续盲调神经网络

Safety filter 本身基于离散停止距离，且 domain analysis 已证明 discrete recoverable states 与 rollout safety 一致。可以尝试直接构造：

$$h(d,v)=d-d_{\rm unsafe}-D_{\rm stop}(v),$$

再将它转换为满足边界条件的解析或半解析 barrier。这条路线最有物理解释，也更容易判断可行或不可行，是后续最值得优先考虑的方向。

### 可能性二：把 barrier 与 controller 联合设计

当前 safety filter 的目标是避免碰撞，不是主动让某个 barrier 每步严格下降。后续可以在保持 safety filter 兜底的前提下，让 controller 显式最小化 worst-case next barrier，并使用 trust region 限制性能退化。

### 可能性三：把加权 soft loss 改成真正的约束优化

可以使用增广拉格朗日、逐约束 multiplier、CEGIS 或可满足性求解，使违反点作为硬约束处理，减少“平均 loss 很小但大量点仍违反”的现象。

### 可能性四：改善 semantic contract 数据质量

重点检查半径 `2.7757m` 的分箱：确认是否存在标注、分箱样本不足或异常值问题。在不降低目标 coverage、不删除困难样本的前提下，增加 calibration 数据或使用更合理的 Mondrian 分组。

### 可能性五：补充真实 process residual 数据

需要保存真实/高保真 simulator transition：

$$w_t=s_{t+1}^{\rm observed}-f(s_t,u_t).$$

有了这类数据后，才能真正估计 state-dependent distribution，并进一步构造 Wasserstein ambiguity set 和 finite-sample confidence radius。

### 可能性六：完成连续域验证

若以后获得 fixed-grid 零违反，再使用 interval bound propagation、分支定界或 cell-wise Lipschitz bounds 覆盖网格之间的连续状态，最终才有资格称为形式证书。

## 11. 建议对导师如实表达的最终结论

可以这样概括：

> 我先按最简范围把论文建议的核心改进落到了 AEBS 上。感知部分从 cGAN 图像代理改为语义距离编码，并通过 conformal calibration 得到了 97.5% 覆盖率的状态条件感知契约。控制部分发现简单 robust PPO 反而恶化安全性，因此改用带有物理制动约束的解析 safety filter，在四种误差测试模式下都实现了 0% unsafe。随后我把 semantic uncertainty 和 process uncertainty 分开，完成了 robust SBC 训练与 fixed-grid worst-case verifier。最终 initial、unsafe 和 nonnegative 条件基本能够满足，但逐步 robust decrease 条件仍有 2556/4483 个网格点违反，epsilon=0 时也有 2089 个真实上升点，因此不能声称已经得到有效安全证书。当前最简实现和验证链路已经完成，实验到此收口。后续如果继续研究，优先考虑根据离散停止距离构造解析 barrier，而不是继续做神经网络超参数调整。

## 12. 导师可能追问的问题

### Q1：既然 safety filter 的 unsafe rate 是 0%，为什么 SBC 还失败？

有限次 rollout 中 0% unsafe 是经验结果；SBC 要求在整个验证域、所有感知端点下满足一个更强的逐步函数下降条件。它是充分条件，不是安全性的必要条件，因此经验安全和“当前候选 SBC 未证成”并不矛盾。

### Q2：是不是把 epsilon 再调小就能通过？

不是。epsilon=0 时仍有 2089/4483 个状态满足 `B(next)>B(s)`，所以不仅是严格余量问题。

### Q3：是不是多训练几个 seed 就能找到证书？

有这种可能，但没有保证，而且不符合当前最简实验范围。现有结果更值得先做可行性分析或解析 barrier 构造，而不是继续随机搜索。

### Q4：process uncertainty 为什么是 0？

当前 simulator dynamics 是确定性的，现有数据只有图像和距离标签，没有真实 transition residual。0 表示本次可观测模型 residual 为 0，不表示真实车辆没有过程噪声。

### Q5：现在论文能写什么贡献？

可以写 semantic abstraction、状态条件感知契约、uncertainty 解耦、安全过滤器和反例可解释验证流程。不能写“已经获得 robust probabilistic safety certificate”，除非后续确实找到并完成连续域验证。

## 13. 当前实验收口决定

- 不继续修改 barrier 网络。
- 不继续调 loss、epsilon、learning rate 或证书域。
- 不做多随机种子和额外 benchmark。
- 不为了得到好看的数字删除验证状态或缩小感知半径。
- 保留 `04_robust_sbc_low_speed_curriculum` 作为当前相对更好的 SBC checkpoint。
- 将 `04_robust_sbc_softplus_square` 作为被实验否定的最终单变量尝试。
- 下一步只整理论文表述、结果表格和局限性，不再追加本阶段实验。

## 14. 相关文件

```text
720_file_Paper.pdf                         原论文
论文改进思路.md                            完整改进设想
改进实验记录.md                            详细实验日志与计划
artical-F122/Aebs/mvp/config.json          固定 MVP 配置
artical-F122/Aebs/mvp/run.py               统一实验入口
artical-F122/Aebs/mvp/robust_sbc.py        barrier 训练与 verifier
artical-F122/Aebs/mvp/analyze_domain.py    可恢复域分析
artical-F122/Aebs/mvp/verify_checkpoint.py checkpoint 灵敏度验证
artical-F122/Aebs/semantic/safety_filter.py safety filter
artical-F122/Aebs/uncertainty/              状态相关扰动接口
results/mvp/                                服务器实验结果
```
