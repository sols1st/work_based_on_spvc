# 原论文方法分析与改进思路

> 把 SafePVC 从零散修补重构成一个更严谨的框架：
> **语义抽象 + 状态相关不确定性 + 分布鲁棒 SBC + 自适应反例驱动联合优化**。

---

## 1. 总体目标

原方法验证的是近似闭环

$$s_{t+1}=f\big(s_t,\pi(g(s_t,z_0))\big)+\Delta s_t,\qquad \Delta s_t\sim\mu,$$

即把所有视觉影响都塞进一个与状态无关的固定扰动 $\Delta s\sim\mu$。改进后重构为

$$\boxed{s_{t+1}=f\big(s_t,\pi(\xi_t)\big)+w_t},$$

其中感知、扰动、分布三类不确定性被显式分离：

$$\xi_t\in\mathcal C_\xi(s_t,z_t),\qquad w_t\sim Q(\cdot|s_t),\qquad Q(\cdot|s_t)\in\mathcal P(s_t).$$

三个核心变化：

| 原 SafePVC | 改进后 |
|---|---|
| 图像生成 $g$ | 语义集合 $\mathcal C_\xi$ |
| 固定扰动 $\mu(\Delta s)$ | 状态相关 $Q(\Delta s\mid s)$ |
| 单一经验分布 | 分布不确定集合 $\mathcal P(s)$ |

最终寻找 $(\pi,B)$，满足 $B(s)\le1\,(s\in S_0)$、$B(s)\ge\frac1{1-p}\,(s\in X_u)$，以及核心安全条件：

$$\boxed{B(s)-\sup_{\xi\in\mathcal C_\xi(s)}\sup_{Q\in\mathcal P(s)}\mathbb E_{w\sim Q}\big[B(f(s,\pi(\xi))+w)\big]\ge\epsilon.}$$

---

## 2. 方法主线

```
o → ξ̂ → C_ξ          (Step 1 语义抽象)
        ↓
  ξ∈C_ξ → π           (Step 2 鲁棒策略)
        ↓
  w ~ P(·|s)          (Step 3 状态相关不确定性)
        ↓
  B(s) ≥ sup_{ξ,Q} E_Q[B(s')] + ε   (Step 4 分布鲁棒 SBC)
        ↓
  verify → certify / cex / refine   (Step 5 自适应验证)
        ↓
  (s*,ξ*,Q*) → trust-region 更新    (Step 6 反例驱动优化)
        ↓
       (π*, B*)
```

本质：把证明对象从「learned image surrogate」升级为「certified semantic abstraction + distributionally robust stochastic closed loop」，从而补上原论文三个理论缺口——**视觉近似误差未进入证书、扰动被错简化为 state-independent、有限样本估计无置信度保证**。

---

## 3. Step 1 — 语义抽象

### 3.1 取消 cGAN 图像生成

原流程 $(s,z)\to G_{\rm cGAN}\to g_{\rm MLP}\to o$ 存在 $g_{\rm real}\to G_{\rm cGAN}\to g_{\rm MLP}$ 两重误差源。直接取消「状态→图像」重建，不再要求重建 $8\times16$ / $32\times32$ 等高维视觉输入，只学控制真正需要的 semantic variable。

### 3.2 定义任务相关 semantic latent

$$\xi=[\xi_1,\dots,\xi_k]\in\Xi\subset\mathbb R^k,\quad k\text{ 尽量小}.$$

- X-Plane：$\xi=(d,\theta,\kappa,c_v)$，即横向偏差、heading 偏差、局部曲率、视觉置信度；与原 benchmark 对齐可取 $\xi=(d,\theta)$。
- CARLA emergency braking：$\xi=(d_{\rm obs},v_{\rm rel},\mathrm{TTC},c_o)$，其中 $\mathrm{TTC}=\dfrac{d_{\rm obs}}{\max(v_{\rm rel},\varepsilon)}$。

选择原则：$\xi$ 保留控制与安全相关信息，而非视觉外观。

### 3.3 语义数据集

原 $D=\{(s_i,o_i)\}$ 改为 $D_{\rm sem}=\{(o_i,s_i,z_i,\xi_i)\}_{i=1}^N$，其中 $\xi_i$ 直接从模拟器 ground-truth state 获得（无需人工标注）。划分为 $D_{\rm train},D_{\rm cal},D_{\rm test}$，**必须保留独立 calibration set**。

### 3.4 训练 encoder

$E_\phi:O\to\Xi$，输出 $\hat\xi_t=E_\phi(o_t)$，训练目标

$$L_{\rm sem}=\sum_{j=1}^k w_j\,\ell_j\big(E_{\phi,j}(o),\xi_j\big),\qquad \ell_j=|\hat\xi_j-\xi_j|^2\text{（连续量）}.$$

学习方向从 $(s,z)\to\text{image}$ 变为 $\text{image}\to\text{control-relevant semantics}$。

### 3.5 控制充分性检查

设视觉 policy $\pi_{\rm img}(o)$，训练 semantic policy $\pi_\xi(\xi)$，要求

$$L_{\rm suff}=\mathbb E_o\|\pi_{\rm img}(o)-\pi_\xi(E_\phi(o))\|_2^2$$

较小（或 $J(\pi_\xi)\approx J(\pi_{\rm img})$），否则只是把 cGAN 换成另一个 encoder。

### 3.6 从点估计升级为 certified abstraction

在 calibration set 上算残差 $r_i=\|\xi_i-E_\phi(o_i)\|$（或逐维 $r_{i,j}$），经 conformal calibration 得到分位数 $q_{j,1-\alpha_j}$，构造

$$\mathcal C_\xi(o)=\prod_{j=1}^k[\hat\xi_j-q_j,\ \hat\xi_j+q_j],$$

即从「$\hat d=10\text{m}$」升级为「$d\in[9.6,10.4]\text{m}$」。

### 3.7 状态相关的 semantic set

全局固定 $q_j$ 太保守。令 $\delta_\xi=\delta_\xi(s,z)$：在大雾/低光照/远距离等环境下 uncertainty 更大。学习 uncertainty scale $\sigma_\omega(s,z)$，用 normalized residual

$$r_i=\frac{\|\xi_i-\hat\xi_i\|}{\sigma_\omega(s_i,z_i)}$$

做 conformal calibration，得到

$$\mathcal C_\xi(s,z)=\{\xi:\|\xi-\hat\xi\|\le q_{1-\alpha}\,\sigma_\omega(s,z)\},$$

使 easy environment → small set，hard environment → large set。

---

## 4. Step 2 — 鲁棒策略学习

保留 PPO 预训练 $\pi_0$，但输入改为 $\xi$：$\pi_0=\operatorname{PPO}(\pi,\xi)$，$u_t=\pi_\theta(\xi_t)$。搜索空间从视觉高维输入降为 $\Xi\subset\mathbb R^k$，训练与后续验证都更简单。

**必须加入 uncertainty augmentation**：否则只在精确 $\xi$ 上训练、却在 $\xi\in\mathcal C_\xi$ 上验证，会造成 train-verification mismatch。训练时采样 $\tilde\xi=\xi+e_\xi,\ e_\xi\in\mathcal E_\xi(s,z)$，$\pi_\theta(a\mid\tilde\xi)$；或直接用 worst-case robust loss

$$\max_{e_\xi\in\mathcal E_\xi}L_{\rm RL}\big(\pi_\theta(\xi+e_\xi)\big).$$

---

## 5. Step 3 — 状态相关不确定性

### 5.1 重构闭环

原 $F(s,z)=f(s,\pi(g(s,z)))$ 改为 $F(s,\xi,w)=f(s,\pi(\xi))+w$。明确区分两类不确定性：**perception uncertainty**（$\xi-\hat\xi$）与 **process/environment uncertainty**（$w$），不再塞进单一的 $\Delta s$。

### 5.2 state-dependent disturbance

原假设 $\Delta s\sim\mu$ 与 $s$ 无关，改为 $w_t\sim\mu(\cdot|s_t,\xi_t)$（复杂度可控时可先取 $w_t\sim\mu(\cdot|s_t)$）。对每个 state region $C_i$ 收集 $\{w_j:s_j\in C_i\}$ 得局部经验分布 $\hat\mu_i$。

### 5.3 不把 $\hat\mu$ 当真实分布

定义 ambiguity set（$W$ 可取 Wasserstein 距离）

$$\mathcal P(s)=\{Q:W(Q,\hat\mu_s)\le\rho_s\},$$

半径 $\rho_s$ 由 sample size 与 confidence level 确定，使 $\mu_{\rm true}(\cdot|s)\in\mathcal P(s)$ 以高概率成立。最终系统模型为

$$s_{t+1}=f(s_t,\pi(\xi_t))+w_t,\quad \xi_t\in\mathcal C_\xi(s_t,z_t),\ w_t\sim Q_t(\cdot|s_t),\ Q_t\in\mathcal P(s_t).$$

---

## 6. Step 4 — 分布鲁棒 SBC

### 6.1 下降条件

原 Theorem 2.2 的 $B(s)\ge\mathbb E[B(s')]+\epsilon$ 改为（见 §1 核心安全条件）：

$$B(s)-\sup_{\xi\in\mathcal C_\xi(s)}\sup_{Q\in\mathcal P(s)}\mathbb E_{w\sim Q}\big[B(f(s,\pi(\xi))+w)\big]\ge\epsilon.$$

初始区/unsafe 区约束 $B(s)\ge0$、$B(s)\le1\ (s\in S_0)$、$B(s)\ge\frac1{1-p}\ (s\in X_u)$ 保留；只有 condition (iv) 从 $\mathbb E_{\Delta s\sim\mu}$ 变为 $\sup_\xi\sup_Q\mathbb E_Q$。

### 6.2 网络 loss

原 $L_L=L_{\rm dec}+\lambda_RL_{\rm region}+\lambda_LL_{\rm lip}$ 改为

$$L_B=L_{\rm Rdec}+\lambda_I L_{\rm init}+\lambda_U L_{\rm unsafe}+\lambda_L L_{\rm localLip},$$

其中 $L_{\rm Rdec}=\mathbb E_s\big[\operatorname{ReLU}(\mathcal R_B(s)-B(s)+\epsilon)\big]$，$\mathcal R_B(s)=\sup_{\xi\in\mathcal C_\xi(s)}\sup_{Q\in\mathcal P(s)}\mathbb E_Q[B(f(s,\pi(\xi))+w)]$，与 theorem condition 完全对齐。

### 6.3 去掉全局 Lipschitz，改局部

原 Theorem 3.1 的 $K=\tau L_B(1+L_f\sqrt{1+(L_\pi L_g)^2})$ 中 $L_g$ 与感知 MLP 相关；语义抽象后删除该项。对每个 cell $C_i$ 用局部 $L_B^{(i)},L_f^{(i)},L_\pi^{(i)}$ 得 $K_i^{\rm state}$；感知不确定性单独处理 $K_i^{\rm sem}=L_B^{(i)}L_{f,u}^{(i)}L_\pi^{(i)}\delta_{\xi,i}$。合为

$$K_i^{\rm total}=K_i^{\rm state}+K_i^{\rm sem}+K_i^{\rm stat}.$$

### 6.4 显式加入统计误差

Monte Carlo 估计 $\hat E_N[B]$ 需加 statistical margin，bounded case 下

$$\eta_i=B_{\max}\sqrt{\tfrac{\log(2/\delta)}{2N_i}}.$$

最终 verification 不查 $B(\tilde s_i)-\hat E[B]\ge\epsilon$，而查

$$B(\tilde s_i)-\hat{\mathcal R}_B(\tilde s_i)-K_i^{\rm state}-K_i^{\rm sem}-\eta_i\ge\epsilon,$$

使 state discretization error、semantic perception error、finite-sample error 全部显式进入证书。

---

## 7. Step 5 — 自适应验证

用 adaptive partition 替代固定 grid。对每个 cell $C_i$ 计算 certified margin

$$M_i=\inf_{s\in C_i}B(s)-\sup_{\xi,Q}\mathbb E_Q[B(s')].$$

- $M_i\ge\epsilon$ → 整 cell certified；
- $M_i<0$ 且找到明确 violating state → 产生 counterexample；
- $0\le M_i<\epsilon$ 或 bound 太松 → split $C_i\to C_i^1\cup C_i^2$。

算法变为 verify → certify / counterexample / refine partition，比 uniform grid 更 scalable。

---

## 8. Step 6 — 反例驱动的联合优化

### 8.1 联合反例

原 counterexample 只是 $s_{\rm cex}$，改进为

$$c^*=(s^*,\xi^*,Q^*,w^*),\qquad \xi^*=\arg\max_{\xi\in\mathcal C_\xi(s^*)}\mathcal R_B,\quad Q^*=\arg\max_{Q\in\mathcal P(s^*)}\mathbb E_Q[B(s')].$$

它明确告知：哪个 state、哪个感知误差、哪类扰动分布最危险。

### 8.2 更新 SBC

固定 $\pi_k$，对 $(s^*,\xi^*,w^*)$ 加大权重：$L_B^{\rm cex}=\operatorname{ReLU}[B(s^{\prime*})-B(s^*)+\epsilon]$，得 $B_k\to B_{k+1}$。

### 8.3 更新 controller

固定 $B_{k+1}$，更新 $\pi_k\to\pi_{k+1}$，目标

$$\min_\pi\sup_{\xi\in\mathcal C_\xi(s^*)}\sup_Q\mathbb E_Q\big[B(f(s^*,\pi(\xi))+w)\big],$$

即使 worst-case next barrier value 尽可能低。

### 8.4 trust region 与性能约束

不用原 $L_{\rm mse}=\mathbb E\|\pi-\pi_0\|^2$，改加 trust region $D_{\rm KL}(\pi_{k+1}\|\pi_k)\le\rho$（或 $\|\pi_{k+1}-\pi_k\|_\infty\le\rho$），减少 $\pi\to\mu_\pi\to B$ 反复震荡；同时对性能直接约束 $J(\pi)\ge J(\pi_0)-\delta_J$，使 safety 与 performance 都成为明确目标。

### 8.5 重新估计与三层闭环

controller 更新后重跑模拟器 $w^{k+1}$，重建 $\hat\mu^{k+1}(\cdot|s)$ 与 $\mathcal P^{k+1}(s)$；若 counterexample 反复指向某种环境 $z^*$（heavy fog / shadow / low illumination），则补充 $D_{\rm new}$ 重训 $E_\phi$ 并重新 calibration。形成三层闭环：

$$\boxed{E_\phi\leftrightarrow\pi\leftrightarrow B.}$$

---

## 9. 算法伪代码

```
输入:  D_sem, f, S0, Xu, p, α, δ
输出:  π, B

E_φ   ← TrainSemanticEncoder(D_train)
C_ξ   ← ConformalCalibration(E_φ, D_cal, α)
π_0   ← RobustPPO(f, C_ξ)
P     ← StateDependentDisturbanceEstimation(π_0, C_ξ)

loop:
  B ← TrainRobustSBC(π, C_ξ, P)
  r ← Verify(B, π, C_ξ, P)
  if r = SAT:       return (π, B)
  if r = UNKNOWN:   AdaptiveSplit()
  if r = UNSAT:     c* = (s*, ξ*, Q*)
                    B ← UpdateB(c*)
                    π ← TrustRegionUpdate(c*, B)
                    P ← Reestimate(π)
```

---

## 10. 理论保证（分层）

原论文只求 $P_{s_0}(\mathrm{Safe})\ge p$，改进后分两层契约：

1. **感知抽象保证**：$P_{\rm data}[\xi^{\rm true}\in\mathcal C_\xi]\ge1-\alpha$；
2. **分布歧义保证**：$P_{\rm data}[\mu_{\rm true}(\cdot|s)\in\mathcal P(s)]\ge1-\delta$。

两层契约成立时，robust SBC 证明 $P_{\rm dyn}[\forall t,s_t\notin X_u]\ge p$，最终表述为

$$\boxed{P_{\rm data}\Big[\inf_{Q\in\mathcal P,\,\xi\in\mathcal C_\xi}P_Q(\mathrm{Safe})\ge p\Big]\ge1-\delta_{\rm total}.}$$

> **注意**：无限时域下不能从 marginal conformal $P(\xi_t\in C_t)\ge1-\alpha$ 推到序列级 $P(\forall t,\xi_t\in C_t)\ge1-\alpha$。推荐把 $\mathcal C_\xi$ 定义为工程上可接受的 robust perception contract，由 SBC 做确定性 worst-case verification（理论更干净），而非专门设计 sequence-level conformal。

---

## 11. 实验设计

至少四组 ablation：

1. cGAN-SafePVC vs Semantic-SafePVC；
2. fixed $\mu$ vs state-dependent $\mu(\cdot|s)$；
3. empirical $\hat\mu$ vs distributionally robust $\mathcal P(s)$；
4. global grid/Lipschitz vs adaptive local verification。

指标：$p_{\rm certified}$、$T_{\rm verify}$、$N_{\rm cells}$、$N_{\rm cex}$、$J_{\rm task}$、empirical violation rate。

感知模块直接对比「pixel reconstruction quality」与「semantic safety relevance」，报告 $\mathrm{MAE}(d),\mathrm{MAE}(\theta),\mathrm{Coverage}(\mathcal C_\xi),\mathrm{Width}(\mathcal C_\xi)$，以及 fog / lighting / rain / blur 下的 coverage，而非只展示 Raw/Downsampled/Generated 生成图。

---

## 12. 核心对照表

| 原 SafePVC | 改进方法 |
|---|---|
| cGAN 生成图像 | semantic encoder |
| MLP 近似 cGAN | certified semantic set |
| image-level approximation | task-relevant abstraction |
| $\Delta s\sim\mu$ | $w\sim\mu(\cdot\mid s)$ |
| empirical distribution | ambiguity set $\mathcal P(s)$ |
| Monte Carlo expectation | confidence-corrected expectation |
| global Lipschitz | local/cell-wise bound |
| uniform discretization | adaptive partition |
| state counterexample | joint semantic-state counterexample |
| MSE-to-$\pi_0$ | trust-region + performance constraint |
| $\pi\leftrightarrow B$ | perception $\leftrightarrow\pi\leftrightarrow B$ |
