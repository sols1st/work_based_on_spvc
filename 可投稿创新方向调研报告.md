# SafePVC 后续可投稿创新方向调研报告

## 一、结论先行

经过对原论文、当前代码和实验结果，以及视觉闭环控制、共形感知边界、安全过滤器、策略蒸馏、神经屏障证书、有限状态抽象等相关工作的交叉调研，最重要的结论是：

1. **不建议把“神经 SBC 换成 LP”作为论文创新。** 用线性规划寻找离散屏障函数、用有限抽象验证神经控制器都已有大量先例。LP 可以作为验证工具或可行性诊断，但仅更换求解器不足以形成新论文。
2. **不建议把“状态相关共形误差区间”单独作为创新。** 2025 年已经出现直接研究 state-dependent conformal perception bounds 与神经符号验证接口的工作；共形预测加 CBF、可达性分析也已较拥挤。[^1][^2][^3]
3. **不建议继续反复调当前 neural SBC。** 多轮实验已经说明失败不是训练轮数或某个损失权重的问题，而是证书条件、控制任务和函数表达之间存在结构性矛盾。继续改网络形状、权重、epsilon，论文价值和成功概率都很低。
4. **当前最值得发展的结果，是已经训练出一个部署时不调用 safety filter 的 standalone PPO，并在四种测试模式下达到 100% success、0% unsafe、0% timeout。** 这说明“把安全过滤器的行为迁移进策略”在这个系统中具有真实可行性。
5. **最推荐的论文方向是：把这种经验迁移升级为可证明的“安全策略编译”方法。** 简单说，就是安全过滤器只当离线教师；最终部署一个普通 PPO；再用状态相关的感知误差界和局部动作误差界，证明学生 PPO 在指定状态域内继承教师的安全性与到达目标能力。

建议论文主线暂定为：

> **Perception-Aware Safety Filter Compilation：把视觉闭环安全过滤器编译成无需运行时过滤的、可验证的神经策略。**

这个方向并非完全无人涉及。已有工作研究过运行时安全指导的 policy repair、训练期安全过滤、CBF-RL 以及模仿安全控制器。[^4][^5] 因此真正的创新不能写成“首次用安全过滤器教 PPO”，而应落在以下组合上：

- 最终策略部署时完全没有 filter；
- 不只报告平均模仿误差，而是计算可验证的、状态相关的最坏动作偏差；
- 同时计入视觉语义估计误差；
- 给出教师到学生的安全或 reach-avoid 性质转移定理；
- 用反例只补充“证明余量不足”的区域，而不是盲目全局调参。

如果这五点真正完成，并增加至少一个额外任务或数据条件，具备形成一篇完整论文的可能。现在还不能声称一定不与任何论文重复；学术上也无法通过普通检索证明绝对不存在相同工作。但目前检索到的最接近工作尚未同时覆盖上述完整闭环，这里存在可以继续深入的研究空间。

## 二、当前项目实际上已经做到了什么

原始论文研究视觉输入下的安全控制器综合：通过生成模型近似状态到图像的映射，把视觉控制器压缩或转换为便于分析的模型，再训练随机屏障证书，给出概率安全下界。原方法的核心是“感知近似、控制器和概率证书联合迭代”。[^6]

在本项目中，已经完成了以下关键工作：

- 建立真实图像到语义距离的 encoder；测试 MAE 为 0.1004 m，RMSE 为 0.1934 m。
- 构造四个距离区间的状态条件感知契约，测试覆盖率为 97.5%。
- 修正评估环境中 success、unsafe、stopped、timeout 等终止语义。
- 构造 adaptive anti-stall safety filter，解决原 filter 在部分扰动模式下“安全但一直停住”的问题。
- anti-stall filter 在 exact、uniform、random boundary、worst endpoint 四种模式下均实现 100% success、0% unsafe、0% timeout。
- 用该 filter 离线生成教师动作，训练出 `standalone_ppo_distilled_v2`。正式测试时不调用 filter，四种模式仍均为 100% success、0% unsafe、0% timeout。
- 建立 process uncertainty 的独立接口，并确认当前确定性模拟器的过程残差为零；旧 uncertainty 实际混入了感知导致的动作差，不能再作为独立过程噪声重复计算。
- 完成多版 robust SBC 训练和网格检查，但所有证书均未通过。standalone PPO 对应 SBC 仍有 2769/4483 个 decrease violations，最小 margin 为 -0.165517。
- 开始用固定网格 LP 检查离散证书可行性；第一版 infeasible 中包含区域边界与插值节点冲突，尚不能用于否定控制器本身。

这些结果构成了一个清楚的研究转折：**控制策略的经验闭环表现已经明显成功，但原始 neural SBC 形式无法证明它。** 这不是“项目全部失败”，而是暴露了一个可以成为论文问题的问题：如何把一个安全教师的行为迁移到无过滤学生，并给出比重新拟合全局神经屏障更直接、更紧的证明。

## 三、为什么几个直观方向容易与已有工作重复

### 3.1 只做状态相关共形感知边界：重合风险高

“不同状态使用不同感知误差半径”是合理改进，但已经有工作直接提出 state-dependent conformal perception bounds，并把它作为感知模块和形式验证器之间的接口。[^1] 此外，共形预测已经被用于 measurement-robust CBF、风险感知 CBF、可达性分析以及未知环境下的安全控制。[^2][^3][^7]

因此，本项目中的四段距离契约可以作为论文的组成模块，但不能单独当作主要创新。要形成区别，至少需要进一步回答：

- 感知契约如何与学生策略的动作误差共同组成闭环误差预算；
- 为什么这样的组合界比全局最大感知半径更不保守；
- 感知覆盖率如何转化为整条任务轨迹的失败概率，而不是只给单样本覆盖率；
- 校准数据、测试数据和闭环访问状态分布发生变化时，保证是否仍成立。

### 3.2 只把 neural SBC 换成 LP：重合风险很高

屏障证书本来就经常通过凸优化、线性规划、SOS 或有限抽象求解。已有研究使用 LP 生成屏障候选并配合 SMT 验证，也有神经随机屏障方法在验证阶段使用线性松弛和分支定界。[^8][^9] 随机系统的屏障函数和有限状态抽象还可以从动态规划近似的统一角度解释。[^10]

所以 LP 在本项目中的正确定位应当是：

- 诊断当前证书条件是否在离散网格上有解；
- 计算一个可解释的 value/barrier 基准；
- 为神经近似或自适应划分提供教师值；
- 最终验证流程中的一个组成部分。

如果论文只说“原文用神经 SBC，我们改用 LP”，审稿人很容易认为方法是已有工具的直接替换，而且固定网格 LP 本身还不能证明连续状态空间。

### 3.3 只做自适应网格：创新不足

对反例附近细分网格、对收缩性差的区域增加划分，已经广泛用于神经闭环可达性和有限抽象。[^11] 自适应划分可以显著提高效率，但除非提出与视觉感知契约或策略误差强耦合的新划分准则，否则更适合作为实现技术，而不是论文唯一贡献。

### 3.4 只做安全过滤器蒸馏：存在直接相邻工作

运行时安全控制器指导 policy repair 已在较早工作中提出；训练期间使用安全过滤器以改善策略、最终减少或取消运行时过滤也已有研究。[^4][^5] 因此，目前的 standalone PPO v2 是重要实验结果，但“用 filter 产生标签，然后行为克隆”本身还不够新。

当前结果中 teacher action MAE 为 0.105696，最大动作误差为 2.209528，underbraking 比例为 29.36%。平均测试虽然通过，但最大误差很大。这恰好说明论文不能只依靠 imitation loss；必须把“哪些误差会破坏安全”与“哪些误差无关紧要”区分开。

### 3.5 只加 Wasserstein 或分布鲁棒不确定性：暂时不适合

Wasserstein distributionally robust control、CVaR-CBF 和数据驱动随机系统验证已有丰富研究。[^12] 当前项目没有真实过程转移残差，修正后的 deterministic simulator residual 为零。此时强行加入 Wasserstein ambiguity set，既缺数据支撑，又很可能只是套用已有框架。

最简论文应先把范围明确为：**确定性车辆动力学 + 有限样本视觉感知误差 + 神经策略逼近误差**。只有获得真实转移数据后，才增加过程噪声的分布鲁棒部分。

## 四、候选方向排序

| 排名 | 方向 | 与现有结果衔接 | 创新潜力 | 实现难度 | 主要风险 |
|---|---|---:|---:|---:|---|
| 1 | 感知感知的安全过滤器编译与证书转移 | 很高 | 高 | 中 | 必须得到局部最坏动作误差和严格定理 |
| 2 | 同时保证安全与不停车的 reach-avoid 策略编译 | 高 | 中高 | 中高 | reach-avoid 文献成熟，必须突出视觉与无过滤部署 |
| 3 | 面向整条轨迹的顺序共形风险预算 | 中 | 高 | 高 | 理论难，2026 年已有 time-uniform CP 相邻工作 |
| 4 | 感知和控制敏感的自适应 IMDP 抽象 | 中 | 中 | 高 | 容易被视为已有抽象方法的组合 |
| 5 | neural SBC 与 LP/IMDP 的系统负结果基准 | 高 | 中低 | 低 | 更适合 workshop、benchmark 或论文实验章节 |

## 五、最推荐方向：可验证的安全策略编译

### 5.1 一句话定义

先让安全过滤器作为离线教师产生安全且不会卡住的动作，再训练一个普通 PPO 学生；部署时完全删除 filter；最后通过“教师安全余量大于感知误差和学生动作误差造成的最坏损失”来证明学生继承教师性质。

“编译”这个词比“蒸馏”更准确，因为目标不是让两个网络平均输出相似，而是把教师携带的安全规则转移成一个独立、低延迟、可验证的神经策略。

### 5.2 闭环模型

真实状态为 $s$，视觉语义估计为 $\hat z$，其状态相关误差集合为：

\[
\hat z\in \mathcal C_z(s).
\]

安全教师输出为：

\[
u_T=\pi_T(\hat z),
\]

学生 PPO 输出为：

\[
u_S=\pi_S(\hat z).
\]

在每个状态单元 $C_i$ 内，验证学生相对教师的最坏动作误差：

\[
\delta_i=
\sup_{s\in C_i,\,\hat z\in\mathcal C_z(s)}
|\pi_S(\hat z)-\pi_T(\hat z)|.
\]

这里不能使用全数据平均 MAE 替代 $\delta_i$。平均 MAE 小不代表临界刹车区域安全，当前 v1 失败和 v2 最大误差仍较大都说明了这一点。

### 5.3 可证明继承的核心条件

假设教师在单元 $C_i$ 中具有安全下降余量 $m_i$，车辆动力学对动作的敏感系数为 $L_f^u$，用于证明安全的函数对状态的敏感系数为 $L_V$。学生动作偏差最多引起：

\[
L_V L_f^u\delta_i
\]

的证明余量损失。如果满足：

\[
m_i > L_V L_f^u\delta_i + e_i^{cell}+e_i^{perception},
\]

那么教师在该单元中的安全下降仍足以覆盖：

- 学生与教师的动作差；
- 连续状态被网格代表点近似的误差；
- 视觉语义误差引起的动作变化。

这会得到一个非常直观的证书：

- 绿色单元：教师余量足够，学生已继承；
- 黄色单元：系统可能安全，但现有界太松；
- 红色单元：发现真实反例或学生偏差过大，需要补数据或修策略。

它比重新训练一个任意形状的 neural SBC 更贴近当前项目，因为教师的安全规则和学生策略已经存在。论文的核心研究问题变成：**怎样以最小代价把黄色和红色单元变成绿色，而不重新引入运行时 filter。**

### 5.4 与已有工作的实质差异

已有 runtime-safety-guided repair 主要利用安全控制器干预来修改策略；CBF-RL 类方法在训练期利用 CBF 约束，也可以实现无在线过滤部署。[^4][^5] 本方向要站住，必须明确增加下列内容：

1. **闭环视觉误差进入转移证明。** 不只验证 state-based policy，而是从真实状态、视觉语义集合、学生动作到下一状态。
2. **证明的是教师到学生的性质转移。** 不是只说训练更安全，而是给出每个状态单元的充分条件。
3. **局部最坏误差代替平均 imitation loss。** 使用区间传播、线性界传播或单调性分析界定 $\delta_i$。
4. **同时处理 safety 与 liveness。** anti-stall 已经说明，永远刹车可能 0% unsafe，却不能完成任务。
5. **反例修正只修改学生。** 教师和安全定义固定，针对证明失败单元增加样本、权重或局部 loss，直到证成或明确不可证。

调研中没有发现一篇工作同时完整实现“视觉状态相关误差 + 安全教师 + 无 filter 学生 + 局部动作误差验证 + 教师性质转移”。但这只能表述为初步文献检索后的空白，正式投稿前仍要用 Google Scholar、Semantic Scholar、DBLP 和引用网络做一次系统检索，不能直接使用“首次”措辞。

### 5.5 最小可发表算法

算法不需要继续训练 neural SBC，可以由五步组成：

1. 固定已经成功的 anti-stall teacher 和 standalone PPO v2。
2. 把可恢复状态域划分为粗网格；只保留具有动力学意义的 unsafe、goal 和 inevitable 区域。
3. 对每个单元计算教师的最坏一步安全/reach-avoid 余量。
4. 对视觉契约内所有语义输入，计算学生与教师的可验证动作偏差上界。
5. 对未通过单元进行 certificate-margin-aware distillation：只补充该单元样本，并最小化超过允许动作偏差的部分。

建议新的训练目标为：

\[
L=L_{BC}+\lambda_r L_{reachable}+\lambda_c L_{certificate},
\]

其中 $L_{BC}$ 是普通教师动作误差，$L_{reachable}$ 提高真实可达状态权重，$L_{certificate}$ 只惩罚使教师安全余量被耗尽的动作偏差。这样不是所有动作误差都同等重要。

### 5.6 为什么这个方向适合当前项目

- 已经有成功 teacher，不需要重新设计控制器。
- 已经有成功 student，不需要从零训练 PPO。
- 已经有状态条件 semantic contract。
- 已经有离散 viability domain、终止区域和最坏端点枚举代码。
- 失败的 SBC 反而可以作为对照：全局拟合任意 barrier 很难，而直接转移教师已有安全余量可能更紧。
- 用户希望最快得到效果；这个方向第一阶段只需增加局部 action-bound 与 margin 检查，不需要大规模训练。

## 六、第二方向：安全与不停车的 reach-avoid 编译

现有安全过滤器实验揭示了一个清楚的问题：global-max 和第一版 adaptive filter 能把 unsafe 降到 0%，但在 random-boundary 中出现 100% timeout。也就是说，“不撞车”不等于“任务完成”。anti-stall 修正后，四种模式才同时达到 100% success。

这可以提升为 reach-avoid 问题：在避开碰撞集合的同时，有限时间到达目标集合。已有 reach-avoid RL、Lyapunov-barrier certificate 和安全/活性联合验证工作，所以“同时讨论 safety 和 liveness”本身不新。[^13][^14] 可区分之处仍应是：

- 视觉误差条件下的 reach-avoid 教师；
- 把教师编译进无过滤学生；
- 对学生给出有限时域到达概率或最坏步数保证；
- 明确证明 anti-stall 不是启发式补丁，而是维持可达性的控制约束。

这个方向可以与主方向合并为论文的第二个理论结果，而不建议单独再开一条工程路线。

## 七、第三方向：轨迹级共形风险预算

当前 semantic contract 的 97.5% coverage 是样本级统计结果。闭环运行 100 或 400 步时，不能直接把每一步的 97.5% 当成整条轨迹的 97.5%。状态又是由过去预测和动作决定的，误差并非简单独立。

可研究的方向是：为不同状态区域和不同时间分配风险预算，使整个任务在给定置信度下保持安全或完成 reach-avoid。顺序共形、time-uniform conformal 与在线自适应共形已经快速发展。[^15][^16] 因此创新需要落在视觉闭环控制的具体组合与更紧的风险记账，而不能只是调用顺序共形算法。

这一方向理论价值高，但不适合当前“最快做出效果”的目标，原因是需要重新设计数据划分、轨迹校准和概率定理。建议作为主方向完成后的增强版，而不是第一阶段。

## 八、LP 和 SBC 在新论文中的位置

新思路不是简单“去掉 SBC”，而是把 SBC 从唯一方法降为对照或辅助工具：

- 原 neural SBC：作为原论文风格的基线，展示在当前控制器和定义下难以证成。
- 固定网格 LP：作为离散可行性诊断，判断是否存在满足指定不等式的离散 value/barrier。
- 教师安全余量：作为主要证明对象，直接利用 safety filter 或解析停止距离的结构。
- 连续误差修正：把网格半径、动作误差和感知误差加入余量，避免把网格通过误写成连续证明。
- 如果最终采用有限抽象/IMDP：应把它描述为计算转移概率或 reach-avoid value 的后端，而不是论文唯一创新。随机屏障和有限抽象本来就有统一的动态规划解释。[^10]

若 LP v2 仍 infeasible，应逐步输出冲突约束，而不是再次调 PPO：

1. 先检查 region 约束是否在同一节点矛盾；
2. 再检查 successor 插值是否跨越 goal/unsafe 边界；
3. 令 epsilon=0 检查 non-increase 可行性；
4. 若仍不可行，检查是否存在状态循环要求严格下降；
5. 最后才判断证书形式或控制器不适合。

## 九、最小实施计划

### 阶段 A：一周内得到方向性结果

- [ ] 固定 teacher、standalone PPO v2、semantic encoder 和现有四段 contract，禁止再改网络超参数。
- [ ] 修完并运行 LP v2，仅作为离散诊断。
- [ ] 在 `80×80` 或现有网格上计算 teacher 与 student 的动作差热力图。
- [ ] 分别报告全域、可恢复域、真实 rollout 访问域中的最大动作误差。
- [ ] 计算每个单元允许的最大动作偏差：安全余量除以动力学/证书敏感度。
- [ ] 输出 certified / bound-too-loose / counterexample 三类单元比例。

这一阶段的最关键图不是新的 loss 曲线，而是二维状态图：横轴距离、纵轴速度；同时画教师余量、学生误差上界和最终可证明区域。

### 阶段 B：最小方法闭环

- [ ] 对未通过单元生成教师动作，不做全域重复采样。
- [ ] 增加 certificate-margin-aware loss。
- [ ] 训练一个 v3 student，并保持部署时无 filter。
- [ ] 比较 v2 与 v3 的四种闭环结果、局部最坏误差和 certified volume。
- [ ] 若某些单元不可证，区分“真实反例”和“界太松”，不能只报告 violation count。

### 阶段 C：论文所需补充

- [ ] 至少增加一个额外 benchmark，优先选择低维、带视觉或感知映射、可以构造解析安全教师的任务。
- [ ] 增加不同图像条件或数据来源，独立划分训练、calibration 和 test。
- [ ] 最终论文实验使用多个种子或置信区间。MVP 可以固定 seed 7，但投稿不能仅靠一个 seed。
- [ ] 与 baseline PPO、runtime filter、普通蒸馏、训练期 safety filter、主方法做公平比较。
- [ ] 测量部署延迟：原 PPO、PPO+filter、standalone student。
- [ ] 报告 safe、success、timeout、return、steps、最坏动作误差、certified state volume、验证时间。

## 十、必须做的消融实验

| 编号 | 方法 | 要回答的问题 |
|---|---|---|
| B0 | 原始 PPO | 没有教师时是否安全 |
| B1 | PPO + runtime filter | 教师本身的安全与性能上限 |
| B2 | 普通行为蒸馏 v2 | 只减平均动作误差是否足够 |
| B3 | 只加 reachable-state 权重 | 改善是否仅来自访问分布 |
| B4 | 只加局部最坏误差 loss | 局部界是否真正缩小 |
| B5 | 完整 margin-aware compilation | certified volume 和闭环性能是否共同提高 |
| B6 | 去掉 state-dependent perception contract | 状态相关契约是否比全局最大半径更紧 |
| B7 | 部署时 runtime filter | 比较安全、成功率和计算成本 |

论文的主结果不能只写“四种模式都是 100%”。需要展示：普通蒸馏虽然测试通过，但可证明区域较小；margin-aware 方法在不降低 success 的情况下显著扩大可证明区域，并缩小最坏动作误差。

## 十一、理论和实验中的硬性红线

1. **不能把网格检查写成连续域形式证明。** 必须加入 cell 内变化界，或明确只称离散验证。
2. **不能把 epsilon=0 直接写成原论文无限时域 SBC 定理成立。** 原定理要求严格正下降量；若采用 epsilon=0，应换成适合不增性、有限时域或 reach-avoid DP 的正确结论。
3. **不能把样本 coverage 直接当成轨迹安全概率。** 必须说明风险组合方式和适用假设。
4. **不能用真实状态距离分箱，却在部署时假装可以直接查询该分箱。** 应说明分箱用于离线验证，或使用可观测语义的保守映射。
5. **不能把 semantic action delta 再当 process noise。** 两者重复计算会制造虚假的保守性。
6. **不能根据同一测试集反复修改方法后仍把它当独立测试集。** 当前 400 张数据需要重新规划 calibration/test 或补数据。
7. **不能使用“首次”而没有系统引用检索。** 应使用“we introduce”并通过相关工作逐项说明差异。

## 十二、可能的论文贡献表述

如果实现成功，可以将贡献写成下面四点：

1. 提出一种 perception-aware safety filter compilation 方法，将带视觉不确定性的安全教师迁移为部署时无需运行时 filter 的神经策略。
2. 提出状态相关的 certificate-aware action error contract，把感知误差、学生策略逼近误差和连续状态离散误差统一为可检查的余量条件。
3. 给出教师 reach-avoid 性质向学生策略转移的充分条件，并设计反例驱动的局部再蒸馏算法。
4. 在 AEBS 和至少一个额外视觉控制任务上证明，该方法相较普通蒸馏扩大 certified region，同时维持安全、成功率和较低部署延迟。

建议避免以下表述：

- “提出了新的 LP 屏障证书”；
- “首次使用状态相关共形预测”；
- “首次把 safety filter 蒸馏进 PPO”；
- “神经 SBC 失败，所以 LP 一定更好”；
- “200 条 rollout 无碰撞，因此系统被证明安全”。

## 十三、论文题目候选

主推：

> **Compiling Perception-Aware Safety Filters into Certifiable Neural Policies**

备选：

> **From Runtime Shielding to Certified Standalone Policies for Vision-Based Control**

> **Certificate-Margin-Aware Policy Distillation for Filter-Free Vision-Based Safety Control**

若最终只能完成离散验证，应在题目和摘要中使用 `grid-certified` 或 `finite-abstraction-based`，不要笼统声称 continuous-state formal safety。

## 十四、投稿可能性判断

### 可以形成论文的最低条件

- standalone PPO 在无 filter 闭环中保持当前正向效果；
- 给出一个正确、可审查的教师到学生性质转移定理；
- 至少一部分非平凡连续域或带误差修正的离散域得到正式证成；
- 主方法相较普通蒸馏明显扩大 certified region；
- 至少两个任务，或一个任务加真实/高保真视觉条件与充分消融；
- 清楚处理样本级感知覆盖与轨迹级安全结论的边界。

### 只能形成阶段性报告或 workshop 的情况

- 只有 AEBS 单一二维模拟器；
- 只有 seed 7 和四种手工误差模式；
- LP 仅在固定网格通过，没有连续修正；
- 方法只是调整蒸馏权重，没有新的证明；
- 继续以 neural SBC violation 下降作为主要贡献。

### 完全不建议继续的情况

- 为得到 `verified` 人为删除大量状态；
- 缩小感知半径但不重新校准覆盖率；
- 把安全 filter 留在正式闭环，却仍声称得到 standalone policy；
- 将 empirical 0% unsafe 写成概率 100% 安全。

## 十五、最终建议

项目可以继续，而且有形成论文的可能，但应停止沿着“继续调 neural SBC，直到 violation 变成 0”这条路线推进。当前最强结果已经从 SBC 转移到 filter-free student：它既有明确工程价值，也暴露了一个可研究的证明缺口。

下一步最小且最有信息量的工作只有一个：

> **计算 teacher 与 standalone PPO v2 在整个可恢复状态域、视觉语义契约内的局部最坏动作偏差，并将其与 teacher 的局部安全余量比较。**

如果大部分状态单元已经满足余量条件，说明方向非常有希望，只需局部补训练和连续误差修正；如果大部分单元失败，但真实 rollout 区域通过，则可以研究 reachable-set-conditioned certificate；如果真实可达区域也大量失败，则当前 100% 测试结果只是样本覆盖有限，应该先改学生，而不是写证明。

这一步会直接决定论文能否成立，比再训练一版 SBC 或扫描更多超参数更有效。

## 参考文献与资料来源

[^1]: Waite et al., “State-Dependent Conformal Perception Bounds for Neuro-Symbolic Verification of Autonomous Systems,” NeuS 2025. https://proceedings.mlr.press/v288/waite25a.html
[^2]: “Safe Perception-Based Control under Stochastic Sensor Uncertainty using Conformal Prediction,” 2023. https://arxiv.org/abs/2304.00194
[^3]: “Conformal Prediction in the Loop: Risk-Aware Control Barrier Functions for Safe Control under Uncertainty,” IEEE L-CSS, 2025. https://ieeexplore.ieee.org/document/11007767
[^4]: “Runtime-Safety-Guided Policy Repair,” 2020. https://arxiv.org/abs/2008.07667
[^5]: “CBF-RL: Safety Filtering Reinforcement Learning in Training with Control Barrier Functions,” 2025. https://arxiv.org/abs/2510.14959
[^6]: “Provably Probabilistic Safe Controller Synthesis for Vision-Based Neural Network Control Systems,” 用户提供的 `720_file_Paper.pdf`, DAC 2026 draft.
[^7]: Dean et al., “Guaranteeing Safety of Learned Perception Modules via Measurement-Robust Control Barrier Functions,” CoRL 2021. https://proceedings.mlr.press/v155/dean21a.html
[^8]: “Reasoning about Safety of Learning-Enabled Components in Autonomous Cyber-Physical Systems,” 2018. https://arxiv.org/abs/1804.03973
[^9]: “Safety Certification for Stochastic Systems via Neural Barrier Functions,” 2022. https://arxiv.org/abs/2206.01463
[^10]: “A Unifying Perspective for Safety of Stochastic Systems: From Barrier Functions to Finite Abstractions,” 2023. https://arxiv.org/abs/2310.01802
[^11]: “Contraction-Guided Adaptive Partitioning for Reachability Analysis of Neural Network Controlled Systems,” 2023. https://arxiv.org/abs/2304.03671
[^12]: “Wasserstein Distributionally Robust Control Barrier Function using Conditional Value-at-Risk,” 2023. https://arxiv.org/abs/2309.08700
[^13]: Hsu et al., “Safety and Liveness Guarantees through Reach-Avoid Reinforcement Learning,” RSS 2021. https://kaichiehhsu.github.io/files/RSS21_RARL.pdf
[^14]: Mandal et al., “Formally Verifying Deep Reinforcement Learning Controllers with Lyapunov Barrier Certificates,” 2024. https://arxiv.org/abs/2405.14058
[^15]: Xu and Xie, “Sequential Predictive Conformal Inference for Time Series,” ICML 2023. https://proceedings.mlr.press/v202/xu23r.html
[^16]: “Time-uniform conformal and PAC prediction,” 2026. https://arxiv.org/abs/2602.06297

### 补充来源

1. Ma et al., “Learning Vision-Based Neural Network Controllers with Semi-Probabilistic Safety Guarantees,” AAAI 2026. https://ojs.aaai.org/index.php/AAAI/article/view/40882
2. “Counterexample-Guided Synthesis of Perception Models and Control,” ACC 2021. https://ieeexplore.ieee.org/document/9482896
3. “Conformal Reachability for Safe Control in Unknown Environments,” 2026. https://arxiv.org/abs/2602.03799
4. “Safety Beyond Training Data: Robust OOD MPC via Conformalized SLS,” L4DC 2026. https://proceedings.mlr.press/v331/srinivasan26a.html
5. “Verification of Neural Reachable Tubes via Scenario Optimization and Conformal Prediction,” L4DC 2024. https://proceedings.mlr.press/v242/lin24a.html
6. “Safety Guarantees for Neural Network Dynamic Systems via Stochastic Barrier Functions,” 2022. https://arxiv.org/abs/2206.07811
7. “Data-Driven Safety Verification of Stochastic Systems via Barrier Certificates: Wait-and-Judge,” L4DC 2022. https://proceedings.mlr.press/v168/salamati22a.html
8. “Scalable Verification of Neural Control Barrier Functions Using Linear Bound Propagation,” L4DC 2026. https://proceedings.mlr.press/v331/vertovec26a.html
