# SafePVC 最简改进实验计划

> 最后更新：2026-09-17
> 当前目标：result 14 已同时保住低速和高速临界样本，四类rollout与高速回归均全通过；全域仅剩0.003440%面积未判定。下一步不再训练，只将连续语义区间从16段细分到64段以收紧sound IBP界。

## 0. 当前计划与进度总览

当前阶段结论：**result 14 已消除result 13的遗忘：四类rollout全部100% success、0% unsafe、0% timeout；`6–8m × 2–3m/s`高速加密检查恢复为0 violations；全域IBP unresolved从1831降到248，未决面积仅0.003440%。** 剩余最差格为 `d=[6.066406,6.068359]m、v=[0.738281,0.740234]m/s`，所需动作3.0，16段IBP下界2.938747，仅差0.061253。密集语义采样的动作余量已经为0，而保守下界仍略低，因此先判定为IBP区间依赖性造成的未决，不继续训练PPO。下一步只将每个完整semantic contract区间由16段细分为64段，分段并集仍与原契约完全相同。

这里的“去掉 filter”是指：filter 只在训练数据生成阶段充当老师；正式 rollout、部署和后续 SBC 使用的都是 `standalone_ppo.zip`，不加载 filter JSON。

### 0.0 当前最简推进（优先看这里）

| 顺序 | 状态 | 工作 | 输出/完成标准 |
|---|---|---|---|
| F1 | [x] 代码完成 | 统一 success、unsafe、目标外停车、越界、timeout 五类互斥结果 | `Aebs/system/outcomes.py` |
| F2 | [x] 代码完成 | success 立即终止并只奖励一次；环境 400 步后使用 truncated | `Aebs/system/env.py` |
| F3 | [x] 代码完成 | 评估器报告 success/unsafe/stopped/timeout、最小距离、干预率 | `Aebs/semantic/robust_controller.py` |
| F4 | [x] 代码完成 | 新增 observed-distance-bin adaptive safety filter，保留旧 JSON 兼容 | `Aebs/semantic/safety_filter.py` |
| F5 | [x] 代码完成 | 一条命令公平比较 baseline/global/adaptive；不训练任何模型 | `Aebs/semantic/compare_safety_filters.py` |
| F6 | [x] 已运行 | 固定 seed 7、200 episodes 比较 baseline/global/adaptive | Adaptive 的 uniform 和 worst-endpoint 明显改善，四种模式 unsafe 均为 0% |
| F7 | [x] 部分通过 | 判断第一版 adaptive 是否可直接固定 | random-boundary 仍为 success=0%、timeout=100%，暂不定稿 |
| F8 | [x] 代码完成 | 增加 anti-stall：过滤后最终动作单步不把速度制动到 target speed 以下 | `adaptive_anti_stall_filter.json`；快速测试通过 |
| F9 | [x] 已运行并通过 | 用同一初始条件评估 anti-stall filter | 四种模式均为 success=100%、unsafe=0%、timeout=0% |
| P1 | [x] 代码完成 | 用成功的 anti-stall filter 在 `128×128` 状态网格上生成教师动作 | `Aebs/semantic/distill_filter_to_ppo.py` |
| P2 | [x] 代码完成 | 从原 PPO 初始化，训练 actor 模仿教师；重点修正低速多刹车 | v2 默认 300 epochs；不做参数扫描 |
| P3 | [x] 代码完成 | 保存并直接评估 standalone PPO，评估过程中不调用 filter | `results/mvp/02_standalone_ppo_distilled_v2/standalone_ppo.zip` |
| P4 | [x] 代码完成 | 报告 teacher 动作拟合误差及四种模式的 success/unsafe/timeout | `Aebs/mvp/report.py` |
| P5 | [x] 第一版已运行但失败 | v1 蒸馏与 4×200 episodes 评估 | 动作 MAE=0.3569；四种模式全部 timeout，原因是低速区多刹车 |
| P6 | [x] v2 已运行并通过 | 运行低速临界带修正后的 v2 | 四种模式均 success=100%、unsafe=0%、timeout=0% |
| S1 | [x] 代码和配置完成 | SBC controller 改为 standalone PPO v2 | 不再加载 safety-filter controller |
| S2 | [x] 代码和配置完成 | 使用 `epsilon=0`、`softplus` 和 viability/terminal 修正版 | 输出 `04_robust_sbc_standalone_ppo` |
| S3 | [x] 代码完成 | SBC 结束后自动输出 status、violations、margin、worst state | 无需先运行 report 才能复制摘要 |
| S4 | [x] 已运行但失败 | standalone PPO neural SBC | violations=2769/4483，min margin=-0.165517 |
| L1 | [x] 代码完成 | 固定网格稀疏 LP 证书，默认使用 standalone PPO v2 | 不训练神经网络、不扫描参数 |
| L2 | [x] 诊断完成 | 报告有最坏单步 unsafe successor 的状态数量 | 区分 barrier 拟合失败与控制器全域不安全 |
| L3 | [x] 第一版已运行但建模冲突 | LP 返回 infeasible，但 robust one-step unsafe successor=0 | unsafe 插值跨过 v=0.5 边界，同时要求终止节点 B=0 和 B=10 |
| L4 | [x] 代码修正完成 | initial/unsafe 约束与固定主网格节点对齐 | 不改 PPO、网格、域或求解参数 |
| L5 | [ ] 等待用户重跑 | 用同一命令生成 v2，不覆盖第一版 | 输出 `04_grid_certificate_lp_standalone_ppo_v2` |
| T1 | [x] 代码完成 | 新增 teacher→student 固定网格安全转移诊断 | `Aebs/mvp/check_certificate_transfer.py` |
| T2 | [x] 代码完成 | 对每个可恢复 operational state 密集采样 semantic contract，分别检查 teacher/student | 默认 `80×80` 网格、每状态 33 个语义点 |
| T3 | [x] 代码完成 | 自动输出通过率、违反数、最小下一状态可恢复余量、最大欠制动和最坏状态 | `results/mvp/05_certificate_transfer_check/metrics.json` |
| T4 | [x] 已运行并通过 | 在服务器 `vt` 环境执行一次 go/no-go 检查 | teacher/student 均 100% sampled-pass，0 violations |
| T5 | [x] 已运行并发现反例 | 只对近距离高速区加密状态/语义采样，并重点检查动作安全阈值余量 | teacher 0 violations；student 19/18646 violations，min margin=-0.022734m |
| T6 | [x] 已运行但未通过 | 从 v2 初始化，对动作余量<0.1的97个状态做局部修复，同时锚定全域原 student 动作 | rollout 全通过；violations 19→16，min action surplus -0.051422→-0.009133 |
| T7 | [x] 已运行并通过 | 从 T6 checkpoint 短训15轮；临界 raw target=3.05，使部署动作经PPO裁剪后精确为3.0 | rollout全通过；局部 violations=0；retention MAE=0.005445 |
| T8 | [x] 已运行、部分通过 | 对 Tanh PPO 做自适应 IBP，比较连续语义输入内动作下界与cell安全动作上界 | certified 45.46%，unrecoverable 53.82%，unresolved 0.72% |
| T9 | [x] 已运行并通过 | 将每个感知区间严格切成16个连续子区间，分别IBP并取最小下界 | local verified；evaluated=1924、certified=1001、unresolved=0 |
| T10 | [x] 已运行、接近通过 | 将相同 split-IBP 扩展到完整 `5–16m × 0.5–3m/s` 域 | certified=85.47%、unrecoverable=14.52%、unresolved=0.01% |
| T11 | [x] 已运行、部分有效 | 用精确离散低速边界动作替代统一的 `3.0m/s²` | 消除 `v≈0.5` 最差格；仍有627个未决格，面积0.01% |
| T12 | [x] 已运行、不采用 | 只修复 `v=0.62–0.75m/s` 附近的离散 viability boundary | 低速样本达标，但高速反例0→17，全域unresolved 627→1831 |
| T13 | [x] 已运行、主要目标通过 | 从 result 08 同时回放低速与高速临界样本 | rollout和高速回归全通过；全域仅剩248个未决cell、0.003440%面积 |
| T14 | [x] 代码已同步、待用户运行 | 不改PPO，只将semantic split从16增至64以收紧sound IBP | `Aebs/mvp/verify_policy_ibp_full_fine.py`；目标unresolved=0 |
| T15 | [ ] T14 通过后 | 整理确定性条件安全定理，并单独处理 conformal 单步覆盖到轨迹风险的概率口径 | 不把95%单步coverage直接写成95%整轨迹安全 |

本轮明确不做：在线 safety filter 部署、PPO 环境交互微调、不同 seed、参数扫描、额外 benchmark，也不再重训 neural SBC。当前只运行 LP v2 和 teacher→student 固定网格诊断。二者都是离散诊断，不是连续状态空间安全证明。

### 0.1 总体进度

| Step | 状态 | 目标 | 当前产物 | 结论 |
|---|---|---|---|---|
| Step 0 | [x] 已完成 | 统一 MVP 配置和一键入口 | `Aebs/mvp/config.json`、`Aebs/mvp/run.py`、`results/mvp/summary.json` | 服务器上可用一条命令运行，已验证。 |
| Step 1 | [x] 已完成 | 固定 seed 7 语义模型和状态条件感知契约 | `results/mvp/01_semantic/semantic_encoder.pt`、`metrics.json` | 语义模型可用，state-conditional contract 测试 coverage 为 97.5%。 |
| Step 2 | [x] standalone PPO 已完成 | 将成功 filter 的行为迁移到一个无运行时 filter 的 PPO | `02_standalone_ppo_distilled_v2/standalone_ppo.zip` | 四种模式均 100% success、0% unsafe、0% timeout。 |
| Step 3 | [x] deterministic MVP 已修正 | 建立独立于 semantic error 的 process disturbance 模型 | `results/mvp/03_process_uncertainty/` | 16 个 cell 均为零 residual；旧 `03_uncertainty` 只保留为错误定义的历史负例，真实过程数据仍缺失。 |
| Step 4 | [x] 已完成 | 训练最简 robust SBC/barrier | `04_robust_sbc_softplus_square` | 训练和保存链路完整；最终仍 violated，停止继续调参。 |
| Step 5 | [x] verifier 条件与 viability domain 已接通 | 固定网格 verifier 输出证成/失败/反例 | `Aebs/mvp/analyze_domain.py`、`Aebs/mvp/verify_checkpoint.py` | 离散不可恢复集和低速安全终止带已有动力学依据；当前失败来自 decrease 条件，不再是域口径遗漏。 |
| Step 6 | [x] MVP 已跑通并收口 | 生成完整结果汇总 | `summary.json`、各阶段 `metrics.json`、本记录 | 工程目标完成；验证结果为负，不包装成正向证书。 |

### 0.2 目前已经解决了什么

- 已确认原始 SafePVC 代码的关键问题：扰动未统一接入训练/验证、硬编码 Lipschitz 项、训练和验证扰动分布不一致等。
- 已跑通原始 AEBS baseline，作为旧方法参考。
- 已训练并固定真实图像到语义距离的 encoder，显著优于旧图像状态估计模型。
- 已把感知误差整理成状态条件 semantic contract：`d` 的误差半径随真实距离区间变化。
- 已实现并运行最简 semantic controller fine-tuning，但该路径目前是负例。
- 已实现 `4×4` uncertainty 查询接口，并识别出旧采样代码把 semantic error 错当成 process disturbance；修正模式已经写好。
- 已实现独立的 MVP barrier trainer 和 fixed-grid robust verifier，能保存 checkpoint、metrics 和最危险反例。
- 已完成最终 softplus-square 单变量实验并明确否定该修改；不再追加实验。
- 已修正控制评估语义；第一版 adaptive filter 在 uniform 和 worst-endpoint 上取得正向效果。
- 已把所有当前结果同步到服务器和本地文档。

### 0.3 目前还没解决什么

- robust controller 没有改善安全性，反而让 uniform/random-boundary unsafe rate 变差。
- 当前 barrier 训练没有证成 robust decrease；所有已运行 verifier 都是 `violated`。
- `decrease_weight=100` 的单因素修正没有奏效，说明不是简单加大 decrease loss 权重即可解决。
- verifier 中 state discretization/statistical margin 仍是 MVP 近似，还没有替换成更严谨的最终理论界。
- 还没有得到可以写成论文主结果的 `verified` certificate。
- `04_robust_sbc_gridtrain_diag` 把 violation count 降到 1096/6400，但 min margin 变差到 -1.02401，最坏点仍在 `d≈13.35m, v=3.0m/s`，最坏组合为 `semantic=-1.0` + `support_low`。
- `04_robust_sbc_hardcase_maxloss` 把 min margin 压到 `-0.02547`，但 violation count 反弹到 4284/6400；最终 `init_loss=2.6904`、`unsafe_loss=5.5440`，说明它牺牲了区域约束，不能视为接近证成。
- 低速安全终止带、离散不可恢复集和 terminal successor payoff 已修正；这些是已解决的历史问题，不应再通过继续删点降低 violations。
- baseline 与 fine-tuned controller 在 `worst_endpoint` 下 unsafe rate 都是 100%；在控制器真实不安全时继续拟合 barrier 不可能得到可信证书。
- `03_uncertainty` 旧结果不是独立 process noise：它来自 semantic error 导致的动作差，而 semantic endpoints 已在 SBC 中显式枚举，形成 double counting。
- `Downsampled.h5` 只有 400 张图像和距离标签，没有真实状态转移残差；论文最终需要补 process disturbance 数据。
- 第一版 adaptive filter 在 random-boundary 下仍为 0% success、100% timeout；anti-stall 已解决该问题，但目前安全行为仍在 filter 中，尚未证明 standalone PPO 能完整复制。
- standalone PPO v1 已失败：在 `d=8m, v=0.45m/s` 等关键状态，teacher 动作为 0，而 student 仍输出约 1.0 的制动；车辆因此在约 7.9m 处停住，400 步后 timeout。v2 增加 `0.3–0.7m/s` 临界带样本、低速恢复动作和低速过度制动惩罚。
- 新脚本第一阶段是“监督蒸馏到 PPO 网络”，不是 PPO 在线强化学习更新；如果 v2 仍有 unsafe/timeout，才需要第二阶段在已修正终止逻辑的环境中做短程 PPO 微调。

### 0.4 最简计划完成情况

| 优先级 | 待做                                                                                   | 不做什么                                              | 完成标准                                                                             |
| --- | ------------------------------------------------------------------------------------ | ------------------------------------------------- | -------------------------------------------------------------------------------- |
| P0  | [x] 初步定位反例集中在 `d≈13.4m, v=3.0m/s` 附近，说明高速远距区域的 robust decrease 最难满足                  | 不换 benchmark，不加 seed                              | 已记录两个负例的 worst state。                                                            |
| P1  | [x] 已完成并运行：取消默认 `softplus` 正值输出，并支持 region warmup / decrease 权重分阶段                   | 不做大网格搜索                                           | violation count 从 4170 降到 2886，但仍未证成。                                            |
| P2  | [x] 已完成：重新运行 Step 4/5，输出 `04_robust_sbc_unsigned_staged`，不覆盖当前负例                     | 不覆盖已有 `04_robust_sbc*`                            | `metrics.json` 已记录新旧对比所需指标。                                                      |
| P3  | [x] 代码已完成：补 verifier 反例诊断字段，检查 worst case 是哪个 semantic endpoint 和 disturbance corner | 不为了好看缩小 conformal 半径                              | 下一次运行后可直接从 metrics/report 读取诊断字段。                                                |
| P4  | [x] 已完成并运行：把 verifier `80×80` grid 混入训练集                                             | 不改 seed，不改 controller，不改 contract                 | violation count 降到 1096，但 min margin 变差，说明平均 loss 不够关注最坏点。                       |
| P5  | [x] 已完成并运行：hard-case 局部采样和 batch max-decrease loss                                   | 不改 benchmark，不改 seed，不改 controller，不改 contract    | min margin 改善，但 violation count 和 region constraints 明显恶化，方案失败。                  |
| P6  | [x] 已完成并运行：top-k + warm start + 完整区域报告                                               | 不追逐单个移动的 worst state                              | violation 降至 975/6320，但 min margin 未改善，region 仍有 16/80 个违反。                      |
| P7  | [x] 已完成：解析安全过滤器 controller rollout                                                   | 暂不运行 uncertainty 和 barrier                        | 四种模式均为 0% unsafe，worst endpoint 从 100% 降为 0%。                                    |
| P8  | [x] 已完成：生成独立的 deterministic process residual                                         | 不把 semantic action delta 重复塞进扰动盒                  | `03_process_uncertainty` 为 16 个零 residual cell，定义修正完成。                           |
| P9  | [x] 接线代码已完成、待 P8 后运行                                                                 | 不复用 controller 不一致的旧产物                            | config 已统一指定 safety-filter controller、semantic endpoints 和新 process uncertainty。 |
| P10 | [x] 已完成：在修正闭环上从头训练 barrier                                                           | 不热启动旧 controller/旧 uncertainty 的 barrier          | violation=901/6320、min margin=-0.10507，但暴露证书域和非负条件遗漏。                            |
| P11 | [x] 已完成：完整证书域修正版                                                                     | 不更换 controller/contract/process model             | nonnegative/init 达标、unsafe 仅剩 1 点，但 decrease 仍有 3656/5688。                       |
| P12 | [x] 已完成但失败：goal-guided 小学习率续训                                                        | 不更换 controller/contract/process model             | decrease 和三个 region 条件均恶化，不采用该 checkpoint。                                       |
| P13 | [x] 已完成：连续停止距离 domain analyzer                                                       | 不训练 barrier                                       | 仅 8 个连续可恢复 operational state 在离散 rollout 中 unsafe；旧口径含 112 个初始 goal。             |
| P14 | [x] 已完成：精确离散停止距离诊断                                                                   | 不训练 barrier                                       | discrete recoverable but unsafe=0；仅保守排除 14 点；initial margin=7.47m。               |
| P15 | [x] 代码已完成、待运行：离散 viability certificate domain                                        | 不通过随意删点制造证书                                       | inevitable set 并入高 barrier 区，decrease 只检查 5419 个 recoverable operational states。 |
| P16 | [x] 已完成：viability-domain barrier                                                     | 不使用失败的 goal-guided checkpoint                     | decrease=3642/5419；initial/expanded unsafe 接近达标，发现 terminal successor 仍使用任意网络值。  |
| P17 | [x] 已完成：safe terminal value 修正版                                                      | 不改变 controller、semantic contract、viability domain | min margin 改善到 -0.05371，unsafe=0，但 decrease 仍为 3655/5419。                        |
| P18 | [x] 已完成：checkpoint epsilon sensitivity                                               | 不训练模型                                             | epsilon=0 仍有1950点；epsilon=0.001 即增至3505，真实上升与严格余量问题并存。                           |
| P19 | [x] 已完成：全局低速安全停止带 + epsilon sensitivity                                              | 不训练模型                                             | epsilon=0 为1652/4483、min=-0.00679；epsilon=0.001 为2588/4483。                      |
| P20 | [x] 已完成：低速域 curriculum 配置与运行                                                         | 不直接复用未经尺度校准的epsilon=0.01                          | threshold=0.5；前100 epochs epsilon=0，后200 epochs epsilon=0.001。                   |
| P21 | [x] 已完成：low-speed curriculum barrier                                                 | 不改变 controller/viability/process model            | min margin显著改善，区域条件全满足；epsilon=0真实上升降至1379，但violations仍平台化。                      |
| P22 | [x] 已完成但失败：softplus-square 非负 barrier                                                | 不改变controller/domain/epsilon日程                    | 主 violations 仅 2576→2556，epsilon=0 violations 1379→2089，判定无效。                    |
| P23 | [x] 实验收口                                                                             | 不再增加变体、调参、seed 或 benchmark                        | 保留最简端到端实现，按真实结果撰写论文。                                                             |
| P24 | [x] 快速评估代码完成                                                                         | 不训练 PPO/barrier                                   | 修正互斥终局、一次性 success reward、timeout 和干预指标。                                         |
| P25 | [x] 已运行快速对比                                                                         | 不扫描 buffer/target speed                           | 已在同一初始条件比较 baseline、global-max、adaptive-bin 和 anti-stall。                         |
| P26 | [x] 第一轮结果已判定                                                                         | 不根据单一 return 选模型                                  | Adaptive 部分通过：uniform/worst 改善，但 random-boundary 仍超时，暂不定稿。                       |
| P27 | [x] anti-stall 代码完成                                                                  | 不改 PPO、contract、target speed 或 buffer             | 过滤后最终制动力不会在单步内把速度压到 0.45m/s 以下。                                                  |
| P28 | [x] anti-stall 快速复评                                                                  | 仍只用 seed 7、200 episodes                           | 四种模式均为 success=100%、unsafe=0%、timeout=0%。                                           |
| P29 | [x] standalone PPO 蒸馏代码                                                             | 不把 filter 放进部署闭环                                | teacher 只生成训练标签；student 保存为普通 PPO zip。                                           |
| P30 | [x] standalone PPO v1 已运行但失败                                                       | 不包装失败结果                                           | 四种模式均 timeout=100%；已定位为低速动作拟合偏差。                                               |
| P31 | [x] v2 代码完成                                                                           | 不改 benchmark/seed，不加入运行时 filter                 | 临界速度带加密、低速恢复标签、低速过制动惩罚、逐项评估日志。                                          |
| P32 | [x] standalone PPO v2 已运行并通过                                                      | 暂不做 RL 微调、SBC 或参数扫描                            | 四种模式均 success=100%、unsafe=0%、timeout=0%；固定该 checkpoint。                                |

### 0.5 当前运行与查看结果命令

当前首先运行安全转移 go/no-go 检查。它不训练模型，结束后会自动打印需要复制的摘要：

```bash
cd /root/work_based_on_spvc/artical-F122

/opt/miniconda3/bin/conda run --no-capture-output -n vt \
  python -m Aebs.mvp.check_certificate_transfer
```

结果保存在：

```text
results/mvp/05_certificate_transfer_check/metrics.json
results/mvp/05_certificate_transfer_check/state_diagnostics.npz
```

判读顺序：

1. teacher 若不是 `100%` sampled-pass，不能继续声称学生继承安全，应先修正或缩小到有物理依据的教师保证域；
2. teacher 通过且 student 达到至少 `90%`，进入局部界形式化和针对失败 cell 的最小再蒸馏；
3. student 为 `50%–90%`，只做局部学生修正，不训练 SBC；
4. student 低于 `50%`，停止证书转移路线，当前 rollout 成功不足以支持全域结论；
5. 无论通过率多高，本脚本仍只是固定网格和语义密集采样诊断，不能写成连续域形式保证。

代码同步到服务器后，在服务器执行下面唯一一条正式实验命令。它会读取已经成功的 anti-stall teacher，训练普通 PPO actor，并直接用普通 PPO 完成四种模式评估：

```bash
cd /root/work_based_on_spvc/artical-F122

/opt/miniconda3/bin/conda run --no-capture-output -n vt \
  python -m Aebs.semantic.distill_filter_to_ppo
```

运行完成后查看统一报告：

```bash
/opt/miniconda3/bin/conda run --no-capture-output -n vt \
  python -m Aebs.mvp.report --results results/mvp
```

重点看 `[Standalone PPO: no runtime safety filter]`：

- 首选通过：exact、uniform、random-boundary、worst-endpoint 全部 `unsafe=0%`、`timeout=0%`，success 接近或等于 100%；
- 最低可继续标准：exact/uniform 不退化，random-boundary 和 worst-endpoint 的 unsafe 相对 baseline 显著下降；
- `teacher action MAE` 只用于诊断，不作为通过条件；v2 虽为 0.1057，但四种闭环测试全部通过，说明关键轨迹上的动作已经正确；
- 若动作 MAE 已很小但仍不安全，说明二维均匀网格没有覆盖 rollout 中关键状态，下一步只增加 rollout teacher samples；
- 若动作 MAE 很大，说明当前网络/训练轮数未学会老师，下一步才增加 epochs；
- 若 student 已达到四种模式 0% unsafe/timeout，就停止修改 PPO，固定 checkpoint，再让 SBC 针对这个 PPO 训练。

单独查看原始 JSON：

```bash
python -m json.tool \
  results/mvp/02_standalone_ppo_distilled_v2/metrics.json | less
```

### 0.6 上一轮 barrier 收口

本轮已记录 `04_robust_sbc_softplus_square` 结果。代码保留为可复现的最简实现，但不再创建下一版实验。

新增/修改文件：

```text
Aebs/semantic/safety_filter.py
Aebs/uncertainty/collect.py
Aebs/mvp/robust_sbc.py
Aebs/mvp/run.py
Aebs/mvp/config.json
Aebs/mvp/report.py
Aebs/mvp/verify_checkpoint.py
```

最终保留能力：

- 新增显式 barrier 输出映射：`linear`、`softplus`、`softplus_square`，并把所用映射写入 metrics。
- 最终配置只改变 `softplus_square` 输出，其余实验条件保持不变，因而失败结论可直接与 low-speed curriculum 对比。
- 最终输出目录为 `results/mvp/04_robust_sbc_softplus_square/`。
- decrease verifier 分母为 4483；expanded unsafe verifier 包含原 unsafe grid 和 269 个 inevitable grid states。
- 最终结论：initial/unsafe/nonnegative 基本满足，但 robust decrease 未满足，因此 certificate 为 `violated`。

### 0.7 teacher → standalone PPO 安全转移检查结果

服务器已运行 `python -m Aebs.mvp.check_certificate_transfer`，结果保存于：

```text
results/mvp/05_certificate_transfer_check/
├── metrics.json
└── state_diagnostics.npz
```

| 指标 | 结果 |
|---|---:|
| 全部状态网格 | 6400 |
| 可恢复 operational states | 4483 |
| 每状态 semantic samples | 33 |
| state-observation pairs | 147939 |
| teacher sampled-pass | 4483/4483（100%） |
| student sampled-pass | 4483/4483（100%） |
| teacher/student violations | 0/0 |
| teacher 最小下一状态可恢复余量 | 0.003987 m |
| student 最小下一状态可恢复余量 | 0.003987 m |
| 最大 student 欠制动 | 2.291187 |
| 最大绝对动作误差 | 2.291187 |
| 最坏 student 状态 | d=6.949 m, v=2.354 m/s |

详细判断：

- 这是目前最强的正向诊断：在固定网格和语义密集采样层面，student 与 teacher 都保持了离散可恢复集合的一步不变性。
- 最大动作误差不等于最大安全风险。`2.291187` 的最大欠制动位于低速且仍有约 `1.68m` 最小安全余量的 cell，因此没有必要继续只优化全局 MAE。
- 真正薄弱区域是第一距离分箱的高速部分。最差状态的动作仍比离散安全阈值高 `0.132900`，但由于离散停止步数跳变，转换成距离余量后只剩约 `4mm`。
- student 余量的 `p01=0.09559m`、`p05=0.44691m`，说明极小余量主要集中在少量边界状态，而不是整个状态域普遍脆弱。
- 本结果仍不是形式保证：80×80 状态网格之间没有 remainder，33 个语义点之间也没有可验证网络输出界；当前 adaptive teacher 使用 observed-distance bin，而原 contract 按 true-state bin 校准。
- 下一步不训练 PPO/SBC，只加密 `d≈6–8m`、`v≈2–3m/s` 区域并计算连续 cell 误差。如果加密后出现负 margin，应局部修 student 或收缩到严格有余量的可证域；如果仍有稳定正余量，再接入线性界传播或区间传播。

局部加密代码已经完成并同步到服务器，运行：

```bash
cd /root/work_based_on_spvc/artical-F122

/opt/miniconda3/bin/conda run --no-capture-output -n vt \
  python -m Aebs.mvp.refine_certificate_transfer
```

默认输出：

```text
results/mvp/06_certificate_transfer_local_refinement/metrics.json
results/mvp/06_certificate_transfer_local_refinement/state_diagnostics.npz
```

脚本会自动输出 teacher/student violations、最小下一状态余量、最小动作安全阈值余量和最坏语义位置。这里优先看 `min action surplus`：距离余量会因为网格越来越靠近 viability boundary 而自然趋近 0；只要 student 在整个感知区间内仍严格高于保持下一状态可恢复所需的动作阈值，才值得进入神经网络区间界验证。

局部加密已经运行，结果如下：

| 指标 | teacher | student |
|---|---:|---:|
| checked recoverable states | 18646 | 18646 |
| violations | 0 | 19 |
| pass rate | 100% | 99.8981% |
| minimum next margin | 约 0 | -0.022734 m |
| minimum action surplus | 约 0 | -0.051422 |

最坏反例为 `d=7.140m, v=2.600m/s, semantic=+1 endpoint`：required action 约为 `3.0`，teacher=`3.0`，student=`2.993318`。全部明显反例都集中在要求接近最大制动的窄边界带，因此不改变控制任务、contract 或网络结构，只修 student 局部动作。

局部修复代码已完成并同步。运行一条命令即可训练 v3、自动做四种 rollout，并自动复跑同一局部加密检查：

```bash
cd /root/work_based_on_spvc/artical-F122

/opt/miniconda3/bin/conda run --no-capture-output -n vt \
  python -m Aebs.mvp.repair_standalone_ppo
```

输出目录：

```text
results/mvp/07_standalone_ppo_local_repair/
├── standalone_ppo_repaired.zip
├── metrics.json
└── local_refinement/
    ├── metrics.json
    └── state_diagnostics.npz
```

通过标准保持最简：四种 rollout 继续为 0 unsafe、0 timeout，局部加密 student violations 从 19 降至 0。若二者不能同时满足，不扫描权重或学习率，停止该修复版本并分析失败原因。

第一版局部修复实际结果：四种 rollout 均保持 100% success、0% unsafe、0% timeout；retention MAE=`0.018698`，但局部 violations 仅从 19 降到 16，最小下一状态余量仍为 `-0.022579m`，最小动作余量改善为 `-0.009133`。它改善了动作逼近但没有让临界动作跨过 3.0 裁剪阈值，因此判定未通过。

针对该单一原因，新增 saturation repair：临界样本训练期 raw target 为 `3.05`，部署时 PPO action space 仍把动作裁剪到合法上限 `3.0`。从第一版 repair checkpoint 开始，只训练 15 轮、学习率 `5e-6`。运行：

```bash
cd /root/work_based_on_spvc/artical-F122

/opt/miniconda3/bin/conda run --no-capture-output -n vt \
  python -m Aebs.mvp.repair_action_saturation
```

输出目录为 `results/mvp/08_standalone_ppo_saturation_repair/`，并会再次自动输出四种 rollout 和局部加密摘要。通过标准不变：四种 rollout 继续 0 unsafe/timeout，局部 violations 必须降为 0；否则停止策略修复路线，不再追加第三组训练参数。

Saturation repair 已运行并通过：selected states=89、local samples=5785、retention MAE=`0.005445`；四种 rollout 全部 100% success、0% unsafe、0% timeout；局部加密 status=`dense_sampled_pass_ready_for_nn_bounds`、violations=0。

下一步自适应 IBP 验证代码已经同步并通过实现自检。它对每个 true-state cell 构造完整感知输入盒，通过 Tanh/Linear 区间传播得到 raw/clipped action 下界，再与保持下一状态可恢复所需的保守动作上界比较。运行：

```bash
cd /root/work_based_on_spvc/artical-F122

/opt/miniconda3/bin/conda run --no-capture-output -n vt \
  python -m Aebs.mvp.verify_policy_ibp
```

默认输出 `results/mvp/09_policy_ibp_local/metrics.json`。结果分为：

- `local_action_condition_verified`：局部区域中所有与可恢复域相交的 cell 均已由 IBP 证成；
- `partial_local_verification`：部分 cell 的 IBP 下界过松或达到最大细分深度，属于 unresolved，不等于发现 unsafe counterexample。

该步骤证明的只是局部连续动作条件。即使全部通过，仍需补全全域覆盖、successor closure 和 conformal trajectory-risk 记账后，才能形成完整概率安全证书。

普通 IBP 实际结果：evaluated=25940、certified cells=2444、unresolved cells=15001；按面积计 certified=45.46%、物理不可恢复=53.82%、unresolved=0.72%。未决格数量大是因为它们已被细分到约 `0.001m × 0.001m/s`，应以面积判断。最坏格要求动作3.0，普通IBP只能下界到2.577751。

尝试调用项目自带 auto_LiRPA CROWN，但两格接口测试超过90秒仍无返回，已终止，当前环境不采用该后端。更简单的 sound 修正是把每个完整感知区间连续切成16段，对每段独立IBP并取最小下界；区间并集与原契约完全相同，不遗漏输入。最坏未决格自检结果：`action lower=3.0`、`required upper=3.0`，已证成。

运行完整 split-IBP：

```bash
cd /root/work_based_on_spvc/artical-F122

/opt/miniconda3/bin/conda run --no-capture-output -n vt \
  python -m Aebs.mvp.verify_policy_ibp_split
```

输出目录为 `results/mvp/10_policy_ibp_semantic_split/`。该步骤不训练任何模型，判断标准为 unresolved area 是否进一步降到0或接近0。

局部 split-IBP 已运行并完全通过：status=`local_action_condition_verified`、evaluated cells=1924、certified cells=1001、unresolved=0；面积中 certified=46.37%、物理不可恢复=53.63%。这说明局部区域内，对于全部连续真实状态和契约内全部连续语义输入，student 动作均不低于保持下一状态可恢复所需的保守阈值。

下一步扩展到完整 operational 域，仍使用16段 sound split-IBP：

```bash
cd /root/work_based_on_spvc/artical-F122

/opt/miniconda3/bin/conda run --no-capture-output -n vt \
  python -m Aebs.mvp.verify_policy_ibp_full
```

范围为 `d∈[5,16]m、v∈[0.5,3]m/s`，输出到 `results/mvp/11_policy_ibp_full_domain/`。其中不可恢复状态只做分类，不计入需要证明的集合；所有与可恢复域相交的 cell 都必须 certified 才能得到完整条件不变性结论。

首次全域实际结果：`partial_local_verification`，evaluated=6548、certified=2957、unresolved=703；面积比例为 certified=85.47%、unrecoverable=14.52%、unresolved=0.01%。这不是发现了占比0.01%的不安全区域，而是区间界无法在当前规则下判定。

最差未决格为 `d=[6.023438,6.025391]m、v=[0.5,0.501953]m/s`。旧规则对所有与 viability boundary 相交的 cell 都要求 `3.0m/s²` 最大制动，因而将 PPO 下界 `0.764492` 错判为不足。但在最后一个低速制动带 `0.5<v<0.65`内，让下一步速度降到 `0.5m/s` 所需的精确上界为：

$$
a_{\mathrm{req}}=\operatorname{clip}\!\left(\frac{v_{\mathrm{high}}-0.5}{0.05},0,3\right).
$$

对最差格该值约为 `0.039063m/s²`，明显低于已证明的 PPO 动作下界。当 `v_high>=0.65m/s` 时上式自动恢复为 `3.0m/s²`，所以这不是降低高速区安全要求，也没有改动 PPO、契约或可恢复集。

修正后代码已同步到服务器并通过导入/语法检查。下一步只运行：

```bash
cd /root/work_based_on_spvc/artical-F122

/opt/miniconda3/bin/conda run --no-capture-output -n vt \
  python -m Aebs.mvp.verify_policy_ibp_full_corrected
```

新结果保存在 `results/mvp/12_policy_ibp_full_corrected/`，不覆盖首次全域结果。判读只分两种：

- `status=local_action_condition_verified` 且 `unresolved=0`：全域确定性条件不变性验证通过，停止修改控制器和 verifier；
- 仍有 unresolved：不立即重训 PPO，只检查剩余 cell 是真实动作不足，还是区间界精度不足。

即使 unresolved=0，结论也必须准确写成：在当前离散动力学模型正确、过程扰动为零、且每步感知误差都位于 semantic contract 内的条件下，standalone PPO 保持可恢复集一步不变。它不会自动把97.5%的测试coverage变成97.5%整轨迹安全保证；轨迹概率口径仍需单独处理。

修正后全域验证已运行，但未完全通过：evaluated=6312、certified=2938、unresolved=627；certified=85.48%、unrecoverable=14.51%、unresolved=0.01%。新最差格为 `d=[6.056641,6.058594]m、v=[0.648438,0.650391]m/s`，需要动作 `3.0`，PPO 区间下界为 `2.459181`。这一格跨过 `v=0.65m/s` 的离散停车步数切换点；不能再通过降低验证阈值消除，否则会真正放宽安全要求。

下一步的单一实验是低速边界局部修复。它从已成功的 result 08 初始化，只在 `v=0.62–0.75m/s` 且贴近离散可恢复边界的状态上增加制动，并用全域原策略动作做 retention 约束。正式部署仍然只有 PPO，不加运行时 safety filter。运行：

```bash
cd /root/work_based_on_spvc/artical-F122

/opt/miniconda3/bin/conda run --no-capture-output -n vt \
  python -m Aebs.mvp.repair_low_speed_boundary
```

一条命令会自动完成：

1. 生成对齐离散 viability curve 的低速边界诊断样本；
2. 从 result 08 开始局部短训15轮；
3. 复跑 exact、uniform、random-boundary、worst-endpoint 四类200回合 rollout；
4. 复跑 `6–8m × 2–3m/s` 旧高速局部加密检查；
5. 用修复后 PPO 复跑完整 `5–16m × 0.5–3m/s` split-IBP。

结果保存在 `results/mvp/13_standalone_ppo_low_speed_repair/`。通过必须同时满足：四类 rollout 均为0% unsafe、0% timeout；旧高速局部 violations=0；最后输出 `status=local_action_condition_verified、unresolved=0`。任一项不满足都不宣称完整证书成立。

result 13 已运行且判定不通过。低速边界训练从373个违反降到0，四类rollout全部100% success、0% unsafe、0% timeout，说明新边界可以学会。但 retention MAE 上升到 `0.063693`，旧高速局部出现17个反例，最差为 `d=7.14m、v=2.6m/s`；全域 unresolved=1831、unresolved area=0.025399%。这是顺序局部训练忘掉旧临界样本，不是低速方向失败。

下一步不从 result 13 继续训练，而是重新从 result 08 开始，同时回放478个低速临界状态和78个高速临界状态。其他训练设置不变，并固定使用CPU，消除SB3的GPU低利用率警告。运行：

```bash
cd /root/work_based_on_spvc/artical-F122

/opt/miniconda3/bin/conda run --no-capture-output -n vt \
  python -m Aebs.mvp.repair_dual_boundary
```

输出保存在 `results/mvp/14_standalone_ppo_dual_boundary_repair/`。脚本仍会自动完成四类rollout、高速局部回归检查和全域IBP，通过标准不变。

result 14 已运行。联合回放训练样本violations=0；四类rollout全部100% success、0% unsafe、0% timeout；高速局部密集采样恢复为100% pass、0 violations。这些结果说明低速与高速两组安全临界可以在同一个无filter PPO中同时保留。

全域16段IBP仍是 `partial_local_verification`，但只剩248个未决cell，unresolved area=`0.003440%`。记录的最差100个cell全部与viability boundary相交，范围仅在 `d≈6.045–6.100m、v≈0.648–0.805m/s`；最差动作界缺口从result 12的0.540819降到0.061253。下一步不修改策略，只提高连续semantic input的分段精度：

```bash
cd /root/work_based_on_spvc/artical-F122

/opt/miniconda3/bin/conda run --no-capture-output -n vt \
  python -m Aebs.mvp.verify_policy_ibp_full_fine
```

该命令使用result 14 checkpoint，将每个语义误差区间从16个连续子区间分为64个，然后对64个sound IBP下界取最小值。它覆盖的输入并集与原semantic contract完全相同，不删除感知误差，也不改安全动作阈值。结果保存到 `results/mvp/15_policy_ibp_full_semantic64/`。

## 1. 本阶段只解决什么

原 SafePVC 使用图像生成代理和状态无关固定扰动：

$$
s_{t+1}=f\left(s_t,\pi(g(s_t,z_0))\right)+\Delta s_t,
\qquad \Delta s_t\sim\mu.
$$

最简改进实验要跑通下面的闭环：

$$
s_{t+1}=f\left(s_t,\pi(\xi_t)\right)+w_t,
$$

其中：

$$
\xi_t\in\mathcal C_\xi(s_t),
\qquad
w_t\sim Q(\cdot\mid s_t),
\qquad
Q(\cdot\mid s_t)\in\mathcal P(s_t).
$$

最终训练和检查的 robust SBC 条件为：

$$
B(s)-\sup_{\xi\in\mathcal C_\xi(s)}
\sup_{Q\in\mathcal P(s)}
\mathbb E_Q\left[B\left(f(s,\pi(\xi))+w\right)\right]
\ge\epsilon.
$$

这一轮的目标不是立即得到最强的论文结果，而是确认以下四个模块能真正串联：

1. 真实图像到语义距离；
2. 语义误差集合进入控制器；
3. 状态相关扰动进入训练和验证；
4. robust SBC 能输出成功、失败或违反点，而不是只停留在公式中。

## 2. 最简实验范围

### 2.1 只使用一个 benchmark

- 使用：CARLA AEBS。
- 暂不使用：X-Plane11，因为当前仓库未提供对应源码和数据。

### 2.2 只使用一个语义变量

第一版定义：

$$
\xi=d.
$$

- $d$：车辆到目标点的距离，由真实图像预测；
- $v$：速度继续由系统状态直接输入控制器。

不增加 TTC、置信度、多目标类别等额外语义变量。现有图像生成器不编码速度，因此不从图像估计 $v$。

### 2.3 只使用一个固定实验配置

- 固定 `seed=7`，仅用于结果可复现；不做不同随机种子比较。
- 只保留一套 train/calibration/test 划分。
- conformal 只保留一套可用于后续验证的状态条件误差契约。
- 每个模块只跑一个主配置；只有程序失败或结果明显不合理时才做必要修正。

### 2.4 暂时不做的内容

- 不做 5 个随机种子统计；
- 不做大规模网格搜索；
- 不同时比较多套网络结构；
- 不实现完整 Wasserstein 线性规划；
- 不先做自适应状态划分；
- 不补建 X-Plane11；
- 不立即重写整篇论文。

这些内容在最简端到端实验跑通之后再考虑。

## 3. 实验环境

- 服务器：`root@10.11.154.191:20020`
- 工作目录：`/root/work_based_on_spvc/artical-F122`
- Conda：`/opt/miniconda3/bin/conda`
- 环境：`vt`
- Python：3.8.20
- PyTorch：2.3.1
- GPU：NVIDIA GeForce RTX 4090

数据文件：`Aebs/data/Downsampled.h5`。

| 数据 | 规模 |
|---|---:|
| 灰度图像 | 400 张，`32×32` |
| 距离标签 | 400 个，范围 `[5,16] m` |

统一输出目录：

```text
results/mvp/
├── 00_baseline/
├── 01_semantic/
├── 02_controller/
├── 03_uncertainty/
└── 04_robust_sbc/
```

每一步至少保存：

- `config.json`；
- `metrics.json`；
- `train.log`；
- 必要的模型或统计文件。

## 4. 已完成工作

### 4.1 原始实现审计

已确认原实现存在以下关键问题：

1. `Aebs/VT/loop.py` 硬编码 `k_except_l=1.2`，没有实现论文 Theorem 3.1 中完整的 Lipschitz 项。
2. `Aebs/system/estimate.py` 计算的扰动没有接入 `env.py` 或 `verify.py`。
3. 训练使用三角扰动，验证使用均匀扰动。
4. learner 使用 `reach_prob=0.95`，verifier 使用 `reach_prob=0.9`。
5. `refine_enabled=False`，原反例精化没有真正启用。
6. 有限样本 expectation 没有统计置信裕量。
7. 所谓 cGAN 的现有训练脚本实际主要是监督 MSE 图像生成。

### 4.2 原始基线已跑通

原入口：

```bash
cd /root/work_based_on_spvc/artical-F122
/opt/miniconda3/bin/conda run --no-capture-output -n vt \
  python -u -m Aebs.VT.loop
```

一次原始运行结果：

| 指标 | 结果 |
|---|---:|
| 迭代 | 101 轮 |
| 耗时 | 107.01 s |
| 日志中最佳安全概率下界 | 80.563% |
| 最后一轮 hard violations | 0 / 10000 |

该值仅作为旧代码基线。由于它仍使用硬编码误差项和状态无关扰动，不能与后续 robust certificate 直接等价比较。

### 4.3 语义编码器已跑通

已新增：

- `Aebs/semantic/model.py`；
- `Aebs/semantic/conformal.py`；
- `Aebs/semantic/train.py`；
- `tests/test_semantic.py`。

固定 `seed=7` 的主要结果：

| 指标 | 旧感知模型 | 新语义模型 |
|---|---:|---:|
| 真实图像距离 MAE | 2.862 m | 0.100 m |
| 真实图像距离 RMSE | 3.454 m | 0.193 m |
| 最大绝对误差 | 6.725 m | 1.268 m |

状态条件感知误差契约使用 4 个距离区间：

| 真实距离 | 允许的距离估计误差半径 |
|---|---:|
| `[5.00, 7.75) m` | 0.083 m |
| `[7.75, 10.50) m` | 2.776 m |
| `[10.50, 13.25) m` | 0.165 m |
| `[13.25, 16.00] m` | 0.416 m |

当前契约测试 coverage 为 97.5%。第二个距离区间半径较大，但最简实验先保留该保守值，不增加新的 conformal 变体。

### 4.4 第一版鲁棒 PPO 已运行，但不作为最终方案

第一版只在语义误差边界上微调 PPO，结果如下：

| 感知误差 | 原 PPO unsafe rate | 边界微调 PPO unsafe rate |
|---|---:|---:|
| exact | 0.0% | 0.0% |
| uniform | 1.8% | 0.0% |
| random boundary | 12.2% | 0.4% |
| worst endpoint | 100.0% | 100.0% |

但 exact mean return 从 405.762 降到 308.772，下降约 23.9%。因此该版本只证明训练和评估代码能运行，不能作为最终 controller。

### 4.5 MVP Step 0--3 已接通并完成一次服务器运行

本轮新增和修改：

- 新增 `Aebs/mvp/config.json`：固定最简实验配置。
- 新增 `Aebs/mvp/run.py`：统一串联 semantic、controller、uncertainty 三个阶段，并写 `results/mvp/summary.json`。
- 修改 `Aebs/semantic/robust_controller.py`：controller observation 训练从“只采 endpoint”改为 50% exact、25% interval uniform、25% endpoint，并在 PPO 后增加轻量 imitation distillation。
- 新增 `Aebs/uncertainty/model.py` 和 `Aebs/uncertainty/collect.py`：生成 `4×4` 状态相关扰动盒。

服务器运行命令：

```bash
cd /root/work_based_on_spvc/artical-F122
/opt/miniconda3/bin/conda run --no-capture-output -n vt \
  python -u -m Aebs.mvp.run --config Aebs/mvp/config.json
```

输出文件：

```text
results/mvp/config.json
results/mvp/summary.json
results/mvp/01_semantic/metrics.json
results/mvp/01_semantic/semantic_encoder.pt
results/mvp/02_controller/metrics.json
results/mvp/02_controller/robust_controller.zip
results/mvp/02_controller/train.log
results/mvp/03_uncertainty/metrics.json
results/mvp/03_uncertainty/state_dependent_uncertainty.npz
results/mvp/04_robust_sbc/metrics.json
results/mvp/04_robust_sbc/barrier.pt
results/mvp/04_robust_sbc_weight100/metrics.json
results/mvp/04_robust_sbc_weight100/barrier.pt
```

本次运行总耗时约 1097.0 s，其中 controller 训练和评估约 932.0 s，uncertainty 采样约 20.1 s。

已解决：

| 步骤 | 状态 | 说明 |
|---|---|---|
| Step 0 | 已完成 | 统一 MVP 入口和配置已可运行。 |
| Step 1 | 已完成 | seed 7 semantic checkpoint 已复用到 `results/mvp/01_semantic/`。 |
| Step 2 | 已接通但结果不合格 | mixture controller 可以完成训练、保存模型和评估，但安全性/收益没有达到要求。 |
| Step 3 | 已完成 | `4×4` state-dependent uncertainty 已生成，16 个 cell 全部有样本。 |

本次 controller 结果：

| 模式 | baseline unsafe | robust unsafe | baseline return | robust return |
|---|---:|---:|---:|---:|
| exact | 0.0% | 0.0% | 405.817 | 282.814 |
| uniform | 1.0% | 21.5% | 405.485 | 280.708 |
| random boundary | 17.0% | 28.0% | 400.651 | 278.523 |
| worst endpoint | 100.0% | 100.0% | 241.775 | 165.364 |

结论：

- mixture + distillation 的代码路径已经跑通；
- nominal exact safety 没有恶化，仍为 0.0% unsafe；
- 但 uniform 和 random boundary unsafe rate 明显恶化；
- exact mean return 从 405.817 降到 282.814，下降约 30.3%，超过 10% 目标；
- worst endpoint 仍然 100.0% unsafe；
- 因此 Step 2 当前只能作为可运行中间产物，不能作为最终 controller。

本次 uncertainty 结果：

| 指标 | 结果 |
|---|---:|
| cell 数 | 16 |
| 每个 cell 样本数 | 4096 |
| 最大均值置信半径，距离维 | `2.12e-14` |
| 最大均值置信半径，速度维 | `0.00533` |
| 全局支撑下界，距离维 | `-2.12e-14` |
| 全局支撑上界，距离维 | `2.12e-14` |
| 全局支撑下界，速度维 | `-0.14940` |
| 全局支撑上界，速度维 | `0.11577` |

解释：

- 距离维扰动几乎为 0 是合理的，因为当前动力学里下一距离只由当前距离和速度决定，不直接由 action 决定。
- 速度维扰动反映 semantic error 通过 controller action 对下一速度造成的影响。
- `state_dependent_uncertainty.npz` 已保存，可供后续 robust SBC 查询。

当时仍未解决（以下是阶段性记录，最终状态见文档开头和 4.24）：

| 问题 | 当前状态 | 下一步 |
|---|---|---|
| controller robust fine-tuning 失败 | mixture 训练后 uniform/random boundary 更差 | 暂不继续大规模调参；下一步先用 baseline controller 接入 SBC，保留 robust controller 失败结果作为负例。 |
| robust SBC 初版未证成 | trainer/verifier 已接通，但 verification 为 `violated` | 分析反例区域和 loss 权重，先修 barrier 训练目标。 |
| full certificate 未产生 | Step 6 已能汇总，但结果不是 certificate | 需要把 verifier 从 `violated` 推到 `unknown` 或 `verified`，才能写成正向结果。 |

### 4.6 MVP Step 4--5 已接通，第一版结果为 violated

本轮继续新增：

- 新增 `Aebs/mvp/robust_sbc.py`：独立的最简 barrier trainer 和固定网格 robust verifier。
- 更新 `Aebs/mvp/run.py`：在 Step 3 后自动运行 robust SBC。
- 更新 `Aebs/mvp/config.json`：加入 `robust_sbc` 配置。

由于 Step 2 的 robust controller 结果不合格，本次 Step 4/5 先使用 baseline controller，并把 semantic contract 和 `4×4` state-dependent uncertainty 接入 barrier 条件。

训练配置：

| 参数 | 值 |
|---|---:|
| controller | baseline PPO |
| barrier 网络 | `MLP([2,16,8,1])` |
| train states | 4096 |
| epochs | 150 |
| batch size | 512 |
| learning rate | 0.001 |
| epsilon | 0.01 |
| verifier grid | `80×80` |

训练过程：

| epoch | loss | train robust decrease violation |
|---:|---:|---:|
| 1 | 9.1217 | 34.47% |
| 25 | 7.1529 | 56.45% |
| 50 | 4.2489 | 62.72% |
| 75 | 1.8952 | 61.69% |
| 100 | 0.2897 | 59.81% |
| 125 | 0.2391 | 56.05% |
| 150 | 0.1987 | 49.41% |

固定网格 verifier 结果：

| 指标 | 结果 |
|---|---:|
| status | `violated` |
| checked states | 6400 |
| violation count | 4170 |
| min margin | -0.52967 |
| mean margin | 0.04243 |
| worst distance | 13.354 m |
| worst speed | 3.0 m/s |

结论：

- Step 4/5 的代码链路已经跑通：barrier checkpoint 和 verifier metrics 均已保存。
- verifier 能给出结构化反例，不再只是公式或旧 `k_except_l=1.2` 判断。
- 当前 barrier 训练没有证成 robust decrease，不能声称安全证书成立。
- loss 降低但 violation rate 仍高，说明模型在满足 region constraints 后没有足够优化 decrease 条件。

下一步只做一个最小修正方向：

- 保持 benchmark、seed、semantic contract、uncertainty 和 baseline controller 不变；
- 调整 barrier loss 权重，把 decrease loss 从 `10×` 提高，并降低/分阶段处理 region loss；
- 重新跑 Step 4/5，目标先把 verifier 从 `violated` 推到 `unknown` 或显著减少 violation count。

### 4.7 单次最小修正：decrease loss 权重提高到 100，仍未解决

为了不扩展实验范围，本轮只改一个因素：

- 保持 benchmark、seed、semantic contract、uncertainty、baseline controller 不变；
- 保持 barrier 网络、训练点数、epoch 数和 verifier grid 不变；
- 只把 `decrease_weight` 从隐含的 `10.0` 提高到 `100.0`；
- 输出目录改为 `results/mvp/04_robust_sbc_weight100/`，不覆盖上一版结果。

运行结果：

| 指标 | weight=10 | weight=100 |
|---|---:|---:|
| final train violation | 49.41% | 48.49% |
| verifier status | `violated` | `violated` |
| verifier violation count | 4170 / 6400 | 4427 / 6400 |
| min margin | -0.52967 | -0.63910 |
| worst distance | 13.354 m | 13.494 m |
| worst speed | 3.0 m/s | 3.0 m/s |

结论：

- 单纯提高 robust decrease loss 权重没有解决问题；
- training violation 略降，但 verifier violation count 和最小 margin 反而变差；
- 主要反例仍集中在高速、大距离区域，典型状态约为 `d≈13.4 m, v=3.0 m/s`；
- 下一步不应继续盲目加权重，而应检查 barrier 训练目标本身：region loss、decrease loss 和当前正值 barrier 形式可能存在尺度冲突。

下一步仍保持最简：

- 不增加 seed、不换 benchmark、不做大搜索；
- 先记录当前两个 barrier 负例；
- 接下来只改 barrier 参数化或 region 约束尺度，例如改成不强制 `softplus` 正值输出，或先预训练 region 再训练 decrease。

### 4.8 取消 softplus + 分阶段训练，violation count 明显下降但仍 violated

用户运行了 `results/mvp/04_robust_sbc_unsigned_staged/` 配置。

本次只改变：

- `square_output=false`，取消 barrier 末端 `softplus`；
- `region_warmup_epochs=25`；
- warmup 阶段 `active_decrease_weight=0.0`；
- warmup 后 `decrease_weight=100.0`；
- 保持 benchmark、seed、semantic contract、uncertainty、controller、训练点数、epoch 数和 verifier grid 不变。

训练过程：

| epoch | loss | decrease loss | init loss | unsafe loss | active decrease weight | train violation |
|---:|---:|---:|---:|---:|---:|---:|
| 1 | 9.4018 | 0.00917 | 0.0000 | 9.4018 | 0.0 | 30.79% |
| 25 | 7.4180 | 0.03932 | 0.0125 | 7.4055 | 0.0 | 65.82% |
| 50 | 8.0225 | 0.01799 | 0.0834 | 6.1402 | 100.0 | 51.12% |
| 75 | 5.8774 | 0.02075 | 0.1129 | 3.6893 | 100.0 | 50.66% |
| 100 | 3.1006 | 0.01848 | 0.1394 | 1.1136 | 100.0 | 41.92% |
| 125 | 1.4317 | 0.01297 | 0.1221 | 0.0130 | 100.0 | 28.93% |
| 150 | 0.9244 | 0.00856 | 0.0583 | 0.0102 | 100.0 | 10.38% |

verifier 对比：

| 指标 | weight=10 | weight=100 | unsigned staged |
|---|---:|---:|---:|
| status | `violated` | `violated` | `violated` |
| violation count | 4170 / 6400 | 4427 / 6400 | 2886 / 6400 |
| min margin | -0.52967 | -0.63910 | -0.62482 |
| mean margin | 0.04243 | 0.04038 | 0.05658 |
| worst distance | 13.354 m | 13.494 m | 13.354 m |
| worst speed | 3.0 m/s | 3.0 m/s | 3.0 m/s |

结论：

- 这是目前最好的 Step 4/5 版本：violation count 从 4170 降到 2886，训练 violation 从约 49% 降到 10.38%。
- 但 verifier 仍是 `violated`，不能作为安全证书。
- min margin 没有改善，说明“违反点总数减少”和“最坏反例强度”不是同一个问题。
- 最坏反例仍在高速、大距离区域，约 `d=13.35m, v=3.0m/s`。

本轮后续已继续写代码，但未运行实验：

- verifier 下一版将记录 worst case 的 semantic endpoint 和 disturbance corner；
- 额外记录 `B(s)`、`max B(next)` 和 successor；
- 目标是判断最坏反例来自感知区间左端点、右端点，还是某个扰动盒角点。

### 4.9 verifier grid 训练与 hard-case/max-loss 的结果

`04_robust_sbc_gridtrain_diag` 将完整 `80×80` verifier grid 混入训练，并补齐最坏候选诊断：

| 指标 | unsigned staged | gridtrain diag |
|---|---:|---:|
| violation count | 2886 / 6400 | **1096 / 6400** |
| min margin | -0.624824 | -1.024010 |
| mean margin | 0.056584 | 0.086414 |
| final train violation | 10.38% | **3.47%** |
| init loss | 0.058287 | **0.011349** |
| unsafe loss | 0.010181 | **0.001212** |

该版本显著改善整体违例数和区域 loss，但少量严重反例仍被平均 loss 淹没。最坏点为 `d=13.354m, v=3.0m/s`，候选组合为 `semantic=-1.0`、`support_low`。

随后运行 `04_robust_sbc_hardcase_maxloss`，在上述反例附近增加 2048 个样本，并加入 batch 最大 decrease loss：

| 指标 | gridtrain diag | hardcase maxloss |
|---|---:|---:|
| violation count | **1096 / 6400** | 4284 / 6400 |
| min margin | -1.024010 | **-0.025465** |
| mean margin | 0.086414 | -0.006333 |
| final train violation | **3.47%** | 55.75% |
| init loss | **0.011349** | 2.690421 |
| unsafe loss | **0.001212** | 5.544029 |

hardcase/maxloss 把单个最坏 margin 压到接近 0，却造成大量小幅违例，并破坏 init/unsafe 区域分离。最坏点也迁移到 `d=7.785m, v=2.886m/s`，组合变为 `semantic=+1.0`、`support_high`。因此固定围绕一个反例采样加 batch max loss 不稳定，本版本不能继续作为主路线。

### 4.10 下一次最小实验：top-k + warm start + 完整条件报告

已写好但尚未运行的 `04_robust_sbc_topk_warmstart`：

- 从 `04_robust_sbc_gridtrain_diag/barrier.pt` 热启动，保留目前最好的整体解；
- 不再加入固定 hard-case 样本；
- 停用单点 `max_decrease_loss`，改为每个 batch 最差 10% 状态的 top-k 平均 loss；
- 学习率降低到 `1e-4`，避免热启动后大步破坏已有解；
- init/unsafe 权重由 1 提高到 10；
- 修正训练 violation 指标，使其与包含 `epsilon` 的 verifier 条件一致；
- `v=0` 在现有 AEBS rollout 中属于安全终止状态，因此不再要求其满足严格 decrease，并在结果中报告剔除数量；
- verifier 同时报告 initial region、unsafe region 和按速度分箱的 decrease 结果。

这一步的成功标准不是只看 min margin，而是三项同时成立：decrease violation 不反弹、min margin 改善、region violations 接近并最终达到 0。

### 4.11 top-k/warm-start 结果与路线修正

用户已运行 `04_robust_sbc_topk_warmstart`：

| 指标 | 结果 |
|---|---:|
| verifier status | `violated` |
| decrease violations | 975 / 6320 |
| excluded terminal states | 80 |
| min margin | -1.065387 |
| mean margin | 0.098659 |
| initial region violations | 16 / 6400 |
| unsafe region violations | 80 / 6400 |
| final train violation | 15.23% |

相较 gridtrain 的 `1096/6400`，排除 80 个明确终止状态后 violation count 继续下降；initial/unsafe 区域也只剩少量违反。但最坏 margin 没有改善，worst case 回到 `d=13.354m, v=3.0m/s`、`semantic=-1.0`、`support_low`。因此 top-k 能改善整体覆盖，仍无法消除真实最坏方向。

此时停止继续调整 barrier，原因不是训练轮数不足，而是发现两个前置契约错误：

1. baseline 和已训练 robust controller 的 `worst_endpoint unsafe_rate` 都为 100%。感知契约允许的输入已经能产生实际 unsafe trajectory，正确的 barrier 不应为这个闭环生成安全证书。
2. 旧 `collect_uncertainty` 用 semantic error 引起的 controller action 差生成“扰动”，而 robust SBC 又独立枚举 semantic endpoints；这是对感知误差的重复计入。旧 uncertainty 还来自 robust controller，而 SBC 使用 baseline controller，存在 controller mismatch。

已完成但未运行的修正：

- 新增 `Aebs/semantic/safety_filter.py`：用全局最保守感知半径计算 conservative distance，再用停止距离公式给 baseline 动作设置最低制动力；
- safety filter 仍保留 baseline 行为，只在最低安全制动力更大时介入；
- `robust_sbc.py` 和 `uncertainty/collect.py` 已能加载 safety-filter JSON controller spec；
- `collect.py` 新增 `deterministic_process_residual` 模式。当前模拟器是确定性的，且数据集中没有 transition residual，因此该模式输出零过程扰动，而不再把 semantic error 重复当成 process noise；
- 真实状态相关 process uncertainty 仍需新采集的 transition 数据，零扰动只用于先跑通诚实的 deterministic MVP。

下一次只评估 safety filter，不运行 barrier。通过条件为四种模式均无 unsafe episode，尤其是 `worst_endpoint unsafe_rate=0`。若未通过，再调整解析过滤规则；若通过，再统一用该 controller 重建后续产物。

### 4.12 safety filter 通过最小安全测试

用户已运行 `results/mvp/02_safety_filter/`：

| 感知模式 | baseline unsafe | filtered unsafe | baseline return | filtered return |
|---|---:|---:|---:|---:|
| exact | 0.00% | **0.00%** | 405.747 | 372.498 |
| uniform | 4.00% | **0.00%** | 404.442 | 116.761 |
| random boundary | 12.50% | **0.00%** | 402.440 | 15.293 |
| worst endpoint | 100.00% | **0.00%** | 241.608 | 330.017 |

结论：

- safety filter 达到当前最重要的进入条件：200 回合测试中四种模式均无 unsafe episode；
- worst endpoint 从完全失败变为 0% unsafe，后续重新训练 SBC 现在才具有实际意义；
- exact return 只中度下降，但 uniform/random-boundary return 大幅下降，说明使用全局最大感知半径 `2.7757m` 很保守；
- 当前先保留此保守版本跑通证书，后续性能改进可将全局半径替换成 observation-consistent 的分段半径或在线集合传播。

下一步只生成新的 `03_process_uncertainty`。因为当前 simulator dynamics 是确定性的，且 `Downsampled.h5` 不包含 transition residual，新产物应为 16 个 cell 的零 residual box。这一步的作用是修正变量定义和消除 double counting，不声称已经完成真实状态相关过程噪声实验。

### 4.13 process uncertainty 定义修正完成

用户已运行 `results/mvp/03_process_uncertainty/`：

| 指标 | 结果 |
|---|---:|
| source | `deterministic_simulator_residual` |
| cells | 16 |
| count per cell | 256..256 |
| support low min | `[0.0, 0.0]` |
| support high max | `[0.0, 0.0]` |
| mean radius max | `[0.0, 0.0]` |

该结果与当前确定性动力学一致。旧 `03_uncertainty` 的非零速度扰动来自 semantic error 引起的 action 差，不属于独立 process disturbance，后续不再使用；报告工具会将它明确标为 historical。

至此，下一版 SBC 的输入定义为：

- controller：已通过四种 rollout 安全测试的 analytic safety filter；
- perception uncertainty：由 semantic contract 给出的三个候选 `-radius/0/+radius`；
- process disturbance：当前 deterministic MVP 的零 residual；
- barrier：全新随机初始化，不继承旧错误闭环下的 checkpoint。

下一步运行 `04_robust_sbc_filtered_zero_process`，重新检查 decrease、initial region 和 unsafe region 三类条件。

### 4.14 safety-filter 闭环首次 barrier 结果与完整证书条件修正

用户已运行 `04_robust_sbc_filtered_zero_process`：

| 指标 | 结果 |
|---|---:|
| status | `violated` |
| decrease violations | 901 / 6320 |
| min margin | -0.105069 |
| mean margin | 0.186424 |
| initial violations | 69 / 6400 |
| unsafe violations | 199 / 6400 |
| unsafe minimum | 3.450354，目标为 10 |
| final train violation | 14.16% |

相较旧 controller 的 top-k 结果，violation 从 `975` 降到 `901`，min margin 从 `-1.06539` 改善到 `-0.10507`。这说明 safety filter 不仅通过 rollout，也让 barrier decrease 更容易满足。

但该结果暴露了三项 verifier 定义遗漏：

1. worst state 为 `d=5.835m, v=0.228m/s`，已经满足 AEBS 成功条件 `d≤6m, v<0.5m/s`，应属于停止的 goal set，而非继续强制严格下降的状态；
2. decrease condition 不应在 unsafe set 内继续检查，unsafe set 应由 `B(s)≥10` 的区域条件单独处理；
3. barrier 在 worst state 为 `-3.772705`，违反完整 SBC 所需的全局非负条件，但旧 verifier 没有报告这一项。

下一版 `04_robust_sbc_complete_domain` 已完成以下修正，尚未运行：

- decrease domain 改为全状态域减去 goal/terminal set 和 unsafe set；
- goal set 定义为 `d≤6m, v<0.5m/s`，停止状态仍包括 `v=0`；
- unsafe set 定义保持 `5m≤d≤6m, v≥0.5m/s`；
- verifier 单独报告 excluded goal/unsafe 数量；
- verifier 新增全域 `B(s)≥0` 检查；
- barrier 恢复 `softplus` 非负输出，从结构上保证非负性；
- initial/unsafe 训练点改为各自完整 `80×80` 网格，并用最差 10% 的 region loss，避免均值掩盖少量严重违反；
- init/unsafe loss 权重提高到 10。

这次修正属于证书定义对齐，不是为了删掉普通反例：进入 unsafe 的前驱状态仍在 decrease domain 中，其 successor 的高 barrier 会继续形成违反；只排除已经终止成功或已经位于 unsafe set 内的当前状态。

### 4.15 完整证书域结果：区域条件基本满足，低速 decrease 成为主问题

用户已运行 `04_robust_sbc_complete_domain`：

| 条件 | 结果 |
|---|---:|
| decrease violations | 3656 / 5688 |
| min margin | -0.252854 |
| mean margin | 0.079811 |
| initial violations | **0 / 6400** |
| unsafe violations | 1 / 6400 |
| nonnegative violations | **0** |
| goal constraints | 尚未显式训练/检查 |

按速度分箱：

| speed | violations / count | min margin | mean margin |
|---|---:|---:|---:|
| `(0,0.5]` | 921 / 936 | -0.252854 | -0.014866 |
| `(0.5,1.0]` | 790 / 936 | -0.023709 | 0.034873 |
| `(1.0,2.0]` | 1385 / 1872 | -0.051418 | 0.118731 |
| `(2.0,3.0]` | 560 / 1944 | -0.014318 | 0.109556 |

当前 worst state 为 `d=6.532m, v=0.418m/s`，无 semantic/process 扰动时仍保持约 `0.418m/s` 向 goal 前进。网络为了满足临近的 unsafe 高值区域，使 barrier 随距离接近 6m 反而上升；训练中又没有 goal 低值约束，因此低速通向成功区域的状态几乎全部违反 decrease。

下一版 `04_robust_sbc_goal_guided` 已写好但未运行：

- certificate goal 从 `v≤0.5` 收紧为 `v≤0.45`，与 unsafe 的 `v≥0.5` 留出 0.05m/s 间隔；
- 新增 goal grid：`5m≤d≤6m, 0≤v≤0.45m/s`，要求 `B(goal)≤1`；
- verifier 新增 `goal_violation_count` 和 `goal_max`；
- 从 `04_robust_sbc_complete_domain/barrier.pt` 热启动；
- 学习率降到 `1e-4`，训练 250 epochs；
- 保留平均 decrease loss，并加入权重 5 的最差 10% decrease loss；
- initial/unsafe/goal 各自使用权重 10 的 top-10% region loss。

这一轮不改变 safety filter、semantic contract 或 process residual，只补齐 goal 边界条件并继续优化同一个正确闭环。

### 4.16 goal-guided 失败：先诊断物理可恢复域

用户已运行 `04_robust_sbc_goal_guided`：

| 指标 | complete domain | goal guided |
|---|---:|---:|
| decrease violations | 3656 / 5688 | 4953 / 5704 |
| min margin | -0.252854 | -0.463302 |
| initial violations | 0 | 19 |
| unsafe violations | 1 | 246 |
| goal violations | 未检查 | 206 |
| nonnegative violations | 0 | 0 |
| final train violation | 64.14% | 86.84% |

显式 goal 低值与 unsafe 高值给连续 barrier 带来更强的形状约束，但没有解决 decrease，反而破坏了各区域条件。最坏点迁移到 `d=10.430m, v=1.709m/s`，说明问题不再只是旧 worst point 或 goal 边界。

当前更根本的问题是：矩形状态域包含大量车辆离障碍很近、速度很高的状态。若从某状态出发，即使使用最大制动 `3m/s²` 也不能在 `d=6m` 前降到 `0.5m/s`，该状态属于物理不可恢复集。只要验证域包含这种会确定性进入 unsafe 的状态，就不可能存在同时满足 initial-low、unsafe-high 和全域 decrease 的 barrier。

已新增但未运行 `Aebs/mvp/analyze_domain.py`：

- 在 `80×80` 状态网格计算停止距离
  `max(v²-0.5²,0)/(2·3)`；
- 用该停止距离得到 physical recoverability margin；
- 从每个网格状态运行 safety filter；
- 每一步采用两个 semantic endpoints 中制动力更小的 worst endpoint；
- 分类为 goal、safe stop、unsafe 或 400 步未结束；
- 报告“物理可恢复但 rollout unsafe”和“物理不可恢复但 rollout 未 unsafe”的差异。

下一步只运行该诊断。后续若定义 certificate domain，必须满足：包含完整 initial region、有物理或可达性依据、对边界转换继续验证；不能仅为了减少 violation 任意删除网格点。

### 4.17 连续停止距离诊断结果：只剩 8 个离散边界误差

用户已运行第一版 `03_domain_analysis`：

| 指标 | 结果 |
|---|---:|
| continuous recoverable / unrecoverable | 5513 / 887 |
| initial-region minimum margin | 7.541667m |
| rollout goal | 5067 |
| rollout stopped safe | 72 |
| rollout unsafe | 783 |
| rollout unresolved | 478 |
| continuous recoverable but unsafe | 8 |
| unrecoverable but not unsafe | 112 |

对口径进一步拆解：`unrecoverable but not unsafe=112` 与初始 goal 网格点数量一致，这些状态已经满足 `d≤6m, v<0.5m/s`，不应参加 recoverability 分类。`recoverable but unsafe=8` 则位于连续停止距离边界附近；由于模拟器先用当前速度更新距离，再以 `0.15m/s` 每步降低速度，连续公式会略微低估实际离散制动距离。

分析器已更新但尚未重跑：

- recoverability 比较只针对排除 initial goal、initial unsafe 和 stopped state 后的 operational domain；
- 新增与 `dt=0.05`、`a_max=3` 完全一致的离散停止距离；
- 同时报告 continuous 与 discrete recoverable/unrecoverable；
- 分别报告两种公式的 recoverable-but-unsafe 和 conservative exclusions；
- 报告 rollout unsafe 状态中的最大 discrete margin，用于确认边界是否还需额外数值缓冲。

若离散公式得到 `discrete recoverable but unsafe=0`，下一步就用 `discrete margin≥0` 定义可恢复验证域，并把 `discrete margin<0` 作为 inevitable unsafe set 施加高 barrier 约束。

### 4.18 离散停止距离诊断通过，viability domain 已接入

用户重跑 `03_domain_analysis` 后得到：

| 指标 | 结果 |
|---|---:|
| continuous recoverable / unrecoverable | 5441 / 247 |
| discrete recoverable / unrecoverable | 5419 / 269 |
| initial continuous margin | 7.541667m |
| initial discrete margin | 7.470000m |
| continuous recoverable but unsafe | 8 |
| discrete recoverable but unsafe | **0** |
| continuous/discrete conservative exclusions | 0 / 14 |
| max discrete margin among rollout-unsafe | -0.033070m |

结论：与 simulator `dt=0.05` 一致的离散停止距离没有漏掉任何 operational rollout-unsafe 网格状态；14 个额外排除点属于保守边界，所有实际 unsafe 状态至少位于离散边界内侧 0.033m。initial region 距离边界仍有至少 7.47m，因此新证书域完整包含初始集。

已完成但尚未运行的 `04_robust_sbc_viability_domain`：

- `certificate_masks` 新增精确离散 stopping distance；
- 原 unsafe set 为 `5≤d≤6, v≥0.5`；
- operational domain 中 discrete margin `<0` 的 269 点标为 inevitable set；
- expanded unsafe = original unsafe ∪ inevitable；
- decrease 只在 discrete recoverable operational domain 检查，预计 5419 点；
- expanded unsafe 中的所有点一起训练并验证 `B(s)≥10`；
- 完整 initial region 继续要求 `B(s)≤1`；
- softplus 继续保证全域非负；
- 撤销失败的强制 goal-low 条件，report 会显示 `goal enforced=false`；
- 从区域条件最好的 `04_robust_sbc_complete_domain/barrier.pt` 热启动，不使用失败的 goal-guided checkpoint。

这不是为了结果好看而删除违反点，而是把 safety property 的适用域从物理上不可控的整个矩形，修正为包含全部 initial set、且经离散动力学验证无漏判 unsafe 的 recoverable domain。

### 4.19 viability-domain 结果：safe terminal successor 仍被错误评估

用户已运行 `04_robust_sbc_viability_domain`：

| 条件 | 结果 |
|---|---:|
| decrease violations | 3642 / 5419 |
| min margin | -0.220436 |
| initial violations | 1 / 6400 |
| expanded unsafe violations | 2 / 6669 |
| nonnegative violations | 0 |
| excluded inevitable | 269 |

expanded unsafe 和 initial 已接近满足，但低速 goal 邻域仍产生大量 decrease violations。worst state `d=6.114m, v=0.304m/s` 的 next state 会继续接近安全 goal；当前 `B(s)=1.068`、`B(next)=1.279`。虽然 goal constraint 不参与证书，网络在 goal 内的任意输出最高达到 14.85，而 robust successor 仍直接查询该值。

对于 reach-avoid 的 stopped process，进入安全 goal 或速度为 0 后过程已经结束，terminal payoff 应由证书定义固定，而不是继续使用只在 operational domain 学习的神经网络外推值。因此已完成但尚未运行 `04_robust_sbc_terminal_value`：

- 对每个 semantic/process successor 单独调用 certificate mask；
- successor 若进入 goal 或 stopped set，使用固定 `B_terminal=0`；
- successor 若进入 original unsafe 或 inevitable set，继续查询网络高 barrier，不做屏蔽；
- robust max 仍在全部 semantic endpoints 上取最大值；
- 从 `04_robust_sbc_viability_domain/barrier.pt` 小学习率续训；
- controller、semantic contract、zero process residual、viability domain 均保持不变。

该修改对齐的是停止过程边界条件，不是把进入危险区的 next state 设为 0。

### 4.20 terminal-value 结果：最坏幅度改善，但大量小违例平台化

用户已运行 `04_robust_sbc_terminal_value`：

| 条件 | viability domain | terminal value |
|---|---:|---:|
| decrease violations | 3642 / 5419 | 3655 / 5419 |
| min margin | -0.220436 | **-0.053706** |
| initial violations | 1 | 1 |
| expanded unsafe violations | 2 | **0** |
| nonnegative violations | 0 | 0 |

终止 payoff 修正确实消除了较严重的 goal 外推问题，但没有减少 violation 数量。250 epochs 内训练 violation 始终约为 67%，说明当前目标已经收敛到折中点而非训练不足。

按速度分箱：

| speed | violations / count | min margin |
|---|---:|---:|
| `(0,0.5]` | 924 / 936 | -0.053706 |
| `(0.5,1.0]` | 801 / 933 | -0.010850 |
| `(1.0,2.0]` | 1377 / 1808 | -0.016787 |
| `(2.0,3.0]` | 553 / 1742 | -0.010672 |

除低速 goal 邻域外，其他速度段的最坏 violation 大多与固定 `epsilon=0.01` 同量级。当前 safety filter 的输出是 `max(baseline, required_braking)`，而 required braking 非负，因此动作不会使速度增加；一旦 `v<0.5`，状态不会再以高于 unsafe 阈值的速度进入障碍区。这给“全局低速安全停止集”提供了控制律依据，但在正式改域前先检查 epsilon 灵敏度。

已新增但未运行 `Aebs/mvp/verify_checkpoint.py`：加载现有 barrier，不做训练，分别以 `epsilon=0/0.001/0.005/0.01` 重跑同一 fixed-grid verifier，并保存每个 epsilon 的完整 worst case、regions 和 speed bins。

### 4.21 epsilon sensitivity：不能只降低 epsilon

用户使用同一个 `04_robust_sbc_terminal_value/barrier.pt` 得到：

| epsilon | violations | min margin | mean margin |
|---:|---:|---:|---:|
| 0 | 1950 / 5419 | -0.043706 | 0.169694 |
| 0.001 | 3505 / 5419 | -0.044706 | 0.168694 |
| 0.005 | 3604 / 5419 | -0.048706 | 0.164694 |
| 0.01 | 3655 / 5419 | -0.053706 | 0.159694 |

结论：

- epsilon=0 仍有 1950 个 `B(next)>B(s)` 的真实上升点，直接把 theorem 改成非严格条件仍不能证成；
- 0 到 0.001 之间新增 1555 个 violations，说明大量状态基本持平，固定每步下降 0.01 与 `dt=0.05` 的动力学尺度不匹配；
- 当前先不选择 epsilon，继续修正安全停止集定义。

safety filter 的最终动作是 baseline action 与非负 required braking 的最大值，因此动作恒 `≥0`，动力学 `v_next=clip(v-action·dt)` 保证速度单调不增。对本论文的 unsafe 定义 `v≥0.5` 而言，一旦当前 `v<0.5`，以后不可能重新进入 unsafe-speed 区域。因此可以对安全事件构造 stopped process，把全局低速带作为 terminal safe set，而不只是障碍附近的 goal。

`verify_checkpoint.py` 已新增 `--terminal-speed-threshold`，下一步仍使用同一 checkpoint、完全不训练，以 threshold=0.5 重复四个 epsilon。预计 decrease domain 从 5419 缩小到约 4483；这一步排除的是由控制律单调性证明安全的状态，而不是按 verifier 结果挑点。

### 4.22 低速安全停止带 sensitivity 与 curriculum 训练配置

用户在 `terminal_speed_threshold=0.5` 下验证同一个 checkpoint：

| epsilon | violations | min margin | mean margin |
|---:|---:|---:|---:|
| 0 | 1652 / 4483 | -0.006787 | 0.208036 |
| 0.001 | 2588 / 4483 | -0.007787 | 0.207036 |
| 0.005 | 2682 / 4483 | -0.011787 | 0.203036 |
| 0.01 | 2730 / 4483 | -0.016787 | 0.198036 |

相较旧域，epsilon=0 的 violations 从 1950 降到 1652，min margin 从 -0.043706 改善到 -0.006787。说明把控制律保证不再加速的低速带视为 stopped-safe 是有效修正；剩余模型仍有真实上升，但幅度很小，适合在正确域上重新训练。

最终正余量选择 `epsilon=0.001`，而不是原来的 0.01。依据是将连续时间下降率 0.02 按 simulator `dt=0.05` 离散化：`0.02×0.05=0.001`。该值仍严格大于 0，不是改成无余量条件。

随后运行的 `04_robust_sbc_low_speed_curriculum` 配置为：

- 从 `04_robust_sbc_terminal_value/barrier.pt` 热启动；
- certificate terminal speed threshold 设为 0.5，训练与验证都只覆盖 4483 个 high-speed recoverable operational states；
- 第 1–100 epochs 使用 epsilon=0，先消除 `B(next)>B(s)`；
- 第 101–300 epochs 使用最终 epsilon=0.001；
- history 新增 `active_epsilon`；
- 最终 metrics 同时保存 epsilon=0.001 主验证和 `zero_epsilon_verification`；
- report 会额外打印 epsilon=0 的 violations 和 min margin；
- expanded unsafe、initial、nonnegative、terminal payoff 逻辑保持不变。

### 4.23 low-speed curriculum 结果与 softplus-square 单变量实验

用户已运行 `04_robust_sbc_low_speed_curriculum`：

| 指标 | 结果 |
|---|---:|
| 主验证 epsilon | 0.001 |
| 主 violations | 2576 / 4483（57.46%） |
| 主 min / mean margin | -0.001793 / 0.193518 |
| epsilon=0 violations | 1379 / 4483（30.76%） |
| epsilon=0 min margin | -0.000793 |
| initial violations | 0 / 6400 |
| expanded unsafe violations | 0 / 6669 |
| nonnegative violations | 0 |
| 最坏状态 | `d=7.785m, v=1.177m/s` |

与同一低速终止域上的旧 checkpoint sensitivity 相比：

- epsilon=0 violations 从 1652 降到 1379，少 273 点；min margin 从 -0.006787 改善到 -0.000793；
- epsilon=0.001 violations 从 2588 降到 2576，只少 12 点；min margin 从 -0.007787 改善到 -0.001793；
- initial、expanded unsafe、nonnegative 三类约束全部满足，说明无需再修改证书域或区域权重；
- epsilon=0 阶段从 epoch 1 到 100，训练 violation 约从 36.44% 降到 33.44%；切换为 epsilon=0.001 后，epoch 125–300 长期停在约 57.5%，继续相同训练预计收益很小。

最坏点的 `B(s)=0.032217`、`B(next)=0.033010`，真实增加仅 0.000793。当前 barrier 使用 `softplus(raw)` 保证非负；当输出已接近 0 时，对 raw output 的梯度也很小，使大量微小正差难以继续压低。因此下一步不是调 seed、扫权重或继续删域，而是做一次输出参数化的单变量实验：

```text
old: B(s) = softplus(raw(s))
new: B(s) = softplus(raw(s))^2
```

`softplus_square` 仍从结构上保证 `B>=0`，同时在相同小正值区域比 plain softplus 提供更大的 raw-output 梯度；它也避免直接使用 `raw^2` 在 raw=0 处梯度为零的问题。

`04_robust_sbc_softplus_square` 的运行配置为：

- 从随机初始化开始，不加载旧 checkpoint；
- 仍使用 safety filter、state-conditional semantic endpoints、零 deterministic process residual；
- 仍使用离散 viability domain、`v<=0.5` safe terminal、terminal payoff 0；unsafe 与低速 terminal 重叠时 unsafe 优先；
- 前 100 epochs 使用 epsilon=0，后 200 epochs 使用 epsilon=0.001；
- 只改 `barrier_output_transform=softplus_square`，保持 80×80 verifier、训练样本、loss 权重和 seed 7 不变。

运行前设定的判断标准按优先级为：

1. initial、expanded unsafe、nonnegative violations 必须继续为 0；
2. epsilon=0 violations 应明显低于 1379，判断真实上升点是否被消除；
3. epsilon=0.001 violations 应明显低于 2576；
4. min margin 应向 0 或正值移动。只有 fixed-grid violation count 为 0 才能记为 MVP `verified`。

### 4.24 softplus-square 最终结果：失败并停止继续调参

用户已运行最终单变量实验：

| 指标 | low-speed curriculum | softplus-square | 变化 |
|---|---:|---:|---:|
| epsilon=0.001 violations | 2576 / 4483 | 2556 / 4483 | 少 20 点，改善不足 0.5 个百分点 |
| epsilon=0.001 min margin | -0.001793 | -0.005527 | 变差 |
| epsilon=0 violations | 1379 / 4483 | 2089 / 4483 | 多 710 点，明显变差 |
| epsilon=0 min margin | -0.000793 | -0.004527 | 变差 |
| initial violations | 0 / 6400 | 1 / 6400 | 轻微变差 |
| expanded unsafe violations | 0 / 6669 | 0 / 6669 | 不变 |
| nonnegative violations | 0 | 0 | 不变 |

最终最坏状态为 `d=7.785m, v=1.215m/s`，`B(s)=0.089132`、`B(next)=0.093660`。输出映射仍能保证非负并满足 expanded unsafe，但没有解决 robust decrease，且真实上升点明显增加。因此 `softplus_square` 判定失败，不作为论文推荐模型。

至此最简实验闭环已经完成：semantic contract、safety filter、process uncertainty API、barrier trainer 和 fixed-grid verifier 全部可运行，并留下结构化结果。工程实现“跑通”的完成标准不要求验证器必须返回 `verified`；本次科学结论就是在当前 controller、contract、网络容量和训练目标下没有找到有效 robust SBC。

为避免无止境迭代，从本节起停止：

- 不再修改 barrier 结构或 loss；
- 不再调 epsilon、学习率、权重或 terminal/domain；
- 不跑多 seed、额外 benchmark 或超参数扫描；
- 保留 `low_speed_curriculum` 作为当前 SBC 中相对更好的 checkpoint，因为它的 epsilon=0 violations 和 min margin 更好；
- 论文中将 safety filter 的 0% unsafe 结果作为主要正向改进，将 robust SBC 如实报告为未证成/局限性结果。

## 5. 最简端到端实施步骤

### Step 0：整理统一 MVP 入口

### 要做的修改

新增一个统一入口，例如：

```text
Aebs/mvp/run.py
```

它按顺序调用语义训练、控制器训练、扰动估计和 robust SBC，并统一写入 `results/mvp/`。

新增一份固定配置：

```text
Aebs/mvp/config.json
```

配置中固定：

- `seed=7`；
- 数据路径；
- 模型路径；
- 状态网格大小；
- conformal 参数；
- 扰动分箱；
- barrier 训练轮数；
- verifier 阈值。

### 完成标准

从服务器执行一条命令即可从头开始运行，不需要手工修改源码参数。

### Step 1：固定语义模型与感知契约

### 使用现有实现

输入真实图像，输出距离估计：

$$
E_\phi(o)=\hat d.
$$

后续控制和验证使用：

$$
\hat d\in[d-\delta_d(d),d+\delta_d(d)].
$$

### 本轮不再做的事情

- 不重训多 seed；
- 不比较 global/Mondrian 多种方法；
- 不继续调网络结构。

### 固定输入

直接使用已经生成的：

```text
results/semantic_stage1_alpha05/seed7/semantic_encoder.pt
```

并将其复制或引用为：

```text
results/mvp/01_semantic/semantic_encoder.pt
```

### 完成标准

- 能加载模型；
- 能读取四个状态区间的感知误差半径；
- 能对一批图像输出 point prediction 和 interval；
- 输出一次 `metrics.json`。

### Step 2：训练最简 robust semantic controller

上一版失败的原因是只在边界噪声上做 PPO，导致 nominal performance 明显下降，而且没有改善 worst endpoint。

### 最简修正

训练 observation 使用固定 mixture：

- 50% exact semantic；
- 25% 区间内 uniform semantic error；
- 25% 两个 contract endpoint。

保留 PPO 的 clip/KL 限制，并增加与原策略动作的 MSE：

$$
L_\pi=L_{PPO}+\lambda_{imit}
\mathbb E_s\|\pi(s)-\pi_0(s)\|_2^2.
$$

如果直接修改 Stable-Baselines3 loss 代价较大，MVP 可采用两阶段近似：

1. PPO mixture fine-tuning；
2. 在状态网格上做少量 supervised distillation，使新策略靠近原策略。

### 只运行一个配置

- seed：7；
- PPO 增量步数：20,000；
- learning rate：`3e-5`；
- clip range：0.1；
- target KL：0.01；
- 评估 episode：200。

### 完成标准

- 程序正常完成并保存 controller；
- exact unsafe rate 不高于原 PPO；
- uniform/random-boundary unsafe rate 至少不恶化；
- exact mean return 下降尽量控制在 10% 内；
- worst endpoint 即使仍失败也必须如实记录，不阻塞 MVP 后续模块接线。

这里的目标是先得到一个可供 robust SBC 使用的 controller，不以本阶段单独实现完整形式保证。

### Step 3：建立最简状态相关扰动模型

新建：

```text
Aebs/uncertainty/
├── __init__.py
├── collect.py
└── model.py
```

### 状态划分

只使用粗粒度 `4×4` 状态分箱：

- 距离分 4 箱；
- 速度分 4 箱；
- 总计 16 个 cell。

### 采样

每个 cell：

- 采样 256 个状态；
- 每个状态采样 16 个 semantic errors；
- 使用 Step 2 的 controller；
- 计算名义动作和受扰动作导致的下一状态差异 $w$。

不再使用原 `estimate.py` 的 5000×10000 展开方式。

### 最简 ambiguity set

每个 cell 只保存：

- 样本数；
- $w$ 的经验均值；
- 经验最小值和最大值；
- 均值的 Hoeffding 置信半径；
- 膨胀后的支撑盒。

第一版定义：

$$
\mathcal P_i=
\left\{
Q:\operatorname{supp}(Q)\subseteq[\underline w_i,\overline w_i],
\ \mathbb E_Q[w]\in[\underline m_i,\overline m_i]
\right\}.
$$

### 输出

```text
results/mvp/03_uncertainty/state_dependent_uncertainty.npz
results/mvp/03_uncertainty/metrics.json
```

### 完成标准

- 16 个 cell 都有样本；
- 训练和验证能通过同一个 API 查询当前状态对应的扰动集合；
- 不再在 learner 和 verifier 中分别使用三角/均匀扰动。

### Step 4：实现最简 robust SBC 训练

### Barrier 网络

沿用原 `MLP([2,16,8,1])`，避免同时改变网络容量。

### Robust decrease loss

对每个训练状态：

1. 查询感知误差区间；
2. 计算语义左端点、中心点、右端点三个动作；
3. 查询状态 cell 的扰动支撑；
4. 取扰动盒角点和经验均值点；
5. 对所有组合计算下一状态 barrier；
6. 使用最大值构造 loss。

MVP 近似：

$$
\widehat{\mathcal R}_B(s)=
\max_{\xi\in\{\xi^-,\xi^0,\xi^+\}}
\max_{w\in\mathcal W_{cell}(s)}
B(f(s,\pi(\xi))+w).
$$

训练 loss：

$$
L_{Rdec}=
\mathbb E_s\operatorname{ReLU}
\left(\widehat{\mathcal R}_B(s)-B(s)+\epsilon\right).
$$

同时保留 initial set 和 unsafe set 约束。

### 说明

角点最大化比完整 $\sup_Q\mathbb E_Q$ 更保守，但实现简单，可以先确认感知集合和状态相关扰动确实进入 barrier 训练。后续再替换成更紧的分布鲁棒期望上界。

### 完成标准

- loss 能下降；
- initial/unsafe region loss 被记录；
- robust decrease violations 被记录；
- 保存 barrier checkpoint。

### Step 5：实现最简 robust verifier

第一版继续使用固定 `100×100` 状态网格，不立即实现 adaptive partition。

### 每个网格点检查

1. 当前状态的 barrier 值；
2. 三个 semantic 候选；
3. 当前状态 cell 的 disturbance box；
4. 所有下一状态组合的最大 barrier；
5. state discretization margin；
6. statistical margin。

检查：

$$
B(s)-\widehat{\mathcal R}_B(s)
-K_{state}(s)-\eta_{stat}(s)
\ge\epsilon.
$$

原来的 `k_except_l=1.2` 不再作为最终判断依据。第一版 `K_state` 可以使用 barrier 梯度上界乘 cell 半径；每个 cell 的 Hoeffding 量进入 `eta_stat`。

### verifier 输出

- `verified`：所有检查通过；
- `violated`：找到明确违反点；
- `unknown`：margin 过松，无法确认。

无论结果是哪一种，都保存：

- 违反数量；
- 最小 robust margin；
- 最危险状态；
- 最危险 semantic endpoint；
- 最危险 disturbance corner；
- 验证耗时。

### 完成标准

验证器完整遍历网格并产生结构化 `metrics.json`。MVP 不强制第一次就得到 `verified=true`；能诚实地产生 violated/unknown 和反例即视为端到端跑通。

### Step 6：运行一次完整 MVP 实验

统一命令目标：

```bash
cd /root/work_based_on_spvc/artical-F122
/opt/miniconda3/bin/conda run --no-capture-output -n vt \
  python -u -m Aebs.mvp.run --config Aebs/mvp/config.json
```

最终汇总文件：

```text
results/mvp/summary.json
results/mvp/run.log
```

至少报告：

| 模块 | 指标 |
|---|---|
| Semantic | MAE、RMSE、contract coverage、最大半径 |
| Controller | exact/uniform/boundary/worst unsafe rate、return |
| Uncertainty | cell 数、每 cell 样本数、支撑范围、统计半径 |
| Robust SBC | final loss、训练 violations、region constraints |
| Verifier | verified/violated/unknown、最小 margin、反例、耗时 |

## 6. MVP 跑通后的对照实验

只有 Step 0--6 全部跑通后，再按以下顺序增加实验：

1. 原始感知链路 vs semantic point estimate；
2. semantic point vs semantic contract；
3. 全局扰动 vs state-dependent disturbance；
4. empirical disturbance vs ambiguity set；
5. 固定网格 vs adaptive partition。

## 7. 主要风险

| 风险 | MVP 处理方式 |
|---|---|
| 只有 400 张图像 | 固定已有保守契约，先不继续细分或调参 |
| 某状态区间误差半径 2.776 m | 保留该值，让 robust controller/SBC 面对真实保守性 |
| worst-endpoint controller 仍不安全 | 如实记录；MVP 先保证模块接通并生成反例 |
| ambiguity set 过保守 | 第一版接受 violated/unknown，不为得到 SAT 而缩小集合 |
| 原代码硬编码较多 | 新 MVP 模块独立实现，逐步替换旧调用 |
| 无限时域 conformal 理论不充分 | 把感知区间作为确定性 worst-case contract 使用 |

## 8. 最终执行记录

1. 已完成：新建 `Aebs/mvp/config.json` 和统一目录结构。
2. 已完成：把 seed 7 的 semantic checkpoint 固定为 MVP 输入。
3. 已完成但结果不合格：修改 robust controller 为 exact/uniform/endpoint mixture，并运行 20k steps。
4. 已完成：实现 `Aebs/uncertainty/` 的 `4×4` 状态相关扰动模型。
5. 已完成但结果为 `violated`：实现最简 barrier loss，加入 semantic endpoints 和 disturbance corners。
6. 已完成但结果为 `violated`：实现固定网格 verifier，输出 robust margin 和联合反例。
7. 已完成但未解决：尝试 `decrease_weight=100`，verifier 仍为 `violated`，且 violation count 变差。
8. 已完成但失败：low-speed curriculum 与 softplus-square 最终单变量实验均未证成 robust decrease。
9. 已收口：停止调整 barrier、region、epsilon 和训练超参数，不新增 benchmark、不做多 seed。

当前实验优先级：**先把 teacher→student 的 100% sampled-pass 升级为连续状态盒上的可靠界。暂不重训 PPO/SBC；先完成薄弱区加密、策略输出界和 cell 内误差界，再根据形式化反例决定是否局部再蒸馏。**

## 9. 如何升级为真正的形式安全保证

### 9.1 先明确“形式保证”到底保证什么

当前结果是：在 `80×80` 状态网格中的 `4483` 个可恢复 operational 状态上，每个状态采样 `33` 个语义输入，teacher 和 standalone PPO v2 都没有发现违反。这是很强的 go/no-go 证据，但仍然只是有限点检查。

真正的形式保证必须给出一个可以被逐条审查的命题。例如：

> 对任意初始状态 $s_0\in X_0$，对任意满足感知契约的语义观测 $\hat d_t\in\mathcal C_d(s_t)$，以及任意允许的过程扰动 $w_t\in\mathcal W(s_t)$，由 standalone PPO v2 和车辆动力学产生的闭环轨迹永远不进入碰撞集合 $X_U$。

这里的每个范围都必须写清楚：

- 状态域：距离和速度的连续范围；
- 初始集合 $X_0$；
- 目标集合 $X_G$；
- 不安全集合 $X_U$；
- 感知误差集合 $\mathcal C_d(s)$；
- 过程误差集合 $\mathcal W(s)$；
- PPO 动作范围；
- 时间步长和车辆动力学；
- 保证是确定性的、有限时域概率性的，还是无限时域概率性的。

即使最终形式验证通过，也只能得到“在上述模型和假设内安全”，不能直接写成“真实车辆在任何情况下绝对安全”。真实系统还需要覆盖制动器误差、延迟、坡度、轮胎附着、传感器失效和分布外图像等因素。

### 9.2 当前系统离形式保证还差什么

| 缺口 | 当前做法 | 形式保证需要 |
|---|---|---|
| 连续状态 | `80×80` 网格点 | 覆盖每个连续 state cell |
| 连续感知区间 | 每状态采样 33 点 | 对整个语义输入区间求可靠网络输出界 |
| 策略误差 | 统计 teacher/student 动作差 | 对每个 cell 证明动作上下界 |
| 感知契约 | 有限样本 97.5% coverage | 明确条件事件、风险组合和适用状态分布 |
| 动力学误差 | 当前确定性 residual 为 0 | 明确模型误差为 0 的假设，或加入可靠扰动集合 |
| 数值误差 | 普通浮点计算 | 保守舍入或额外数值裕量 |
| 安全性质 | 一步后仍可恢复 | 对整个连续域证明 robust invariance |
| 任务完成 | rollout 中 100% success | 单独证明有限时间到达或排名函数下降 |

当前最小 sampled margin 只有：

$$
0.003987\text{ m}\approx 4\text{ mm}.
$$

这意味着即使网格点全部通过，只要 cell 内状态变化、网络输出松弛、数值舍入或动力学误差合计超过 4 mm，最差 cell 就不能被证明。因此下一步的关键不是继续增加 rollout 数量，而是计算这些误差的可靠上界。

### 9.3 推荐主路线：解析可恢复域 + 神经策略区间验证

这是当前最快、最适合 AEBS 二维动力学、也最能利用 standalone PPO 正向结果的路线。它仍然属于 barrier/invariant certificate 思想，但不再训练一个任意 neural SBC，而是使用有物理意义的停止距离作为证书。

#### 9.3.1 定义离散停止距离证书

定义安全速度 $v_{safe}=0.5$ m/s、安全距离 $d_{safe}=6$ m、最大制动力 $a_{max}=3$。使用与模拟器相同的离散时间动力学，计算从速度 $v$ 降至 $v_{safe}$ 所需的离散停车距离：

$$
D_{stop}^{disc}(v)
=\Delta t\sum_{k=0}^{N(v)-1}
\max(v-k a_{max}\Delta t,0),
$$

其中：

$$
N(v)=\left\lceil\frac{\max(v-v_{safe},0)}{a_{max}\Delta t}\right\rceil.
$$

定义可恢复余量：

$$
M(d,v)=d-d_{safe}-D_{stop}^{disc}(v).
$$

可恢复安全集合为：

$$
K=\{(d,v):M(d,v)\ge 0\}.
$$

它的物理含义是：如果现在开始使用允许的最大制动力，仍有能力在进入危险区前把速度降到安全阈值。

#### 9.3.2 需要证明的核心不变性条件

对可恢复域中的每个连续状态 cell $C_i$，需要证明：

$$
\inf_{s\in C_i}
\inf_{\hat d\in\mathcal C_d(s)}
\inf_{w\in\mathcal W(s)}
M\left(F(s,\pi_S(\hat d,v),w)\right)
\ge 0.
$$

其中：

- $F$ 是离散车辆动力学；
- $\pi_S$ 是 standalone PPO；
- $\mathcal C_d(s)$ 是该真实状态允许的全部感知距离；
- $\mathcal W(s)$ 是过程扰动集合；
- $pi_S$ 的输出必须在整个输入盒上求界，不能只检查中心和端点。

如果所有 cell 都满足，并且所有初始状态属于 $K$，那么根据归纳法：

1. 初始状态在 $K$；
2. 任意 $s_t\in K$ 的所有允许后继仍在 $K$；
3. 所以轨迹始终留在 $K$；
4. 因为 $K$ 与碰撞集合按定义分离，所以闭环不会进入碰撞集合。

这就是当前项目最直接的确定性安全证明。

#### 9.3.3 对 PPO 输出做可靠区间界

对一个 state cell：

$$
C_i=[d_i^-,d_i^+]\times[v_i^-,v_i^+],
$$

先构造所有可能的 PPO 输入盒。若感知误差半径为 $r(s)$，则：

$$
\hat d\in
[d_i^- - r_i^{max},\ d_i^+ + r_i^{max}].
$$

归一化后输入 PPO，与速度区间组成：

$$
Z_i=
[\hat d_i^-/\sigma_d,\hat d_i^+/\sigma_d]
\times[v_i^-,v_i^+].
$$

使用 `auto_LiRPA`、$\alpha\beta$-CROWN、区间传播或分支定界，计算：

$$
a_i^-\le\pi_S(z)\le a_i^+,
\quad\forall z\in Z_i.
$$

在当前 AEBS 动力学中，较小的制动力通常是碰撞安全的最坏动作，所以可用可靠的动作下界 $a_i^-$ 构造最坏后继。但这个单调性必须针对动作裁剪、速度裁剪和终止条件逐段证明，不能只凭直觉使用。

若一次网络界太松，应细分 cell，而不是缩小真实感知半径或忽略失败点。

#### 9.3.4 连续 cell 后继集合

对于：

$$
d' = d-v\Delta t+w_d,
$$

$$
v' = \operatorname{clip}(v-a\Delta t+w_v,0,3),
$$

结合状态盒、动作界和扰动盒，计算可靠后继区间：

$$
d'\in[d_i'- ,d_i'+],
\qquad
v'\in[v_i'-,v_i'+].
$$

然后直接求该后继盒上的最小可恢复余量：

$$
\underline M_i=inf_{(d',v')\in C_i'}M(d',v').
$$

因为 $M$ 随距离增大而增大、随速度增大而减小，通常最坏角点为：

$$
(d'^-,v'^+).
$$

但离散停车距离是分段函数，应在所有速度分段边界上检查，避免跨越制动步数变化点时漏掉最坏值。

最终 cell 判定为：

- `verified`：$\underline M_i\ge\varepsilon_{num}$；
- `violated`：找到真实输入盒内的明确反例；
- `unknown`：上下界过松，无法确认。

`unknown` 不能算作 `verified`，也不能通过删除 cell 消除。

#### 9.3.5 自适应细分

建议从粗网格开始，只细分以下 cell：

- $\underline M_i<0$，但中心点采样为正；
- 网络动作界宽度过大；
- cell 跨越 semantic contract 分箱边界；
- cell 跨越离散停车步数边界；
- 靠近 $M=0$ 的 viability boundary；
- 当前最差区域 `d≈6.95m, v≈2.35m/s`。

停止条件必须预先设定：

- 最小 cell 尺寸；
- 最大分支数量；
- 数值安全余量；
- 超过预算后返回 `unknown`，而不是强行声称通过。

### 9.4 teacher→student 证书转移的另一种写法

如果希望突出“安全教师编译进 PPO”的论文贡献，可以先证明 teacher 在每个 cell 中具有安全余量：

$$
m_i^T=
\inf_{s,\hat d,w}
M(F(s,\pi_T(\hat d,v),w)).
$$

再验证 student 与 teacher 的局部动作偏差：

$$
\delta_i=
\sup_{z\in Z_i}
|\pi_S(z)-\pi_T(z)|.
$$

若动力学和余量函数对动作的局部敏感度上界为 $L_i$，则可以使用充分条件：

$$
m_i^T-L_i\delta_i-e_i^{cell}-e_i^{num}\ge0.
$$

这里：

- $e_i^{cell}$ 是状态 cell 内近似误差；
- $e_i^{num}$ 是数值计算安全余量；
- 如果已有网络区间传播直接验证 student 后继，则不必再用较松的全局 Lipschitz 常数。

当前最大 teacher/student 欠制动为 `2.291187`，但出现在物理余量较大的低速区域；最差安全点的最大欠制动为 0。这说明局部余量条件可能明显优于全局动作 MAE 或全局最大动作误差。

这条路线的论文重点不是“student 精确模仿 teacher”，而是：

> student 可以在不完全模仿 teacher 的情况下，只要局部策略误差没有耗尽该区域的安全余量，就继承 teacher 的安全性质。

### 9.5 感知契约怎样进入真正的保证

这是最容易被误写的部分。当前 `97.5% coverage` 是有限测试样本上的结果，不等于每一步都以 97.5% 概率正确，更不等于 400 步轨迹以 97.5% 概率安全。

可以选择以下三种严谨口径之一。

#### 方案 A：条件确定性保证

证明：

> 只要每一步感知误差都落在指定契约内，闭环就不会碰撞。

形式为：

$$
\forall t,\quad
|\hat d_t-d_t|\le r(d_t)
\Longrightarrow
s_t\notin X_U.
$$

这是最容易与连续控制验证结合的结论，但它不回答“契约失败的概率是多少”。论文必须把安全结论写成 conditional guarantee。

#### 方案 B：有限时域概率保证

若任务最多运行 $T$ 步，并能为每一步建立有效的误差失败概率 $\alpha_t$，可以使用风险预算：

$$
\Pr\left(\exists t\le T:\hat d_t\notin\mathcal C_d(s_t)\right)
\le\sum_{t=1}^{T}\alpha_t.
$$

再结合“契约内确定性安全”，得到有限时域安全概率下界：

$$
\Pr(\text{safe for }T\text{ steps})
\ge 1-\sum_{t=1}^{T}\alpha_t.
$$

普通 split conformal 的 marginal coverage 未必能直接支持闭环自适应序列中的每个 $\alpha_t$。需要独立校准假设、轨迹级校准，或适合序列数据的 conformal 方法。

#### 方案 C：time-uniform 或轨迹级契约

直接对整条轨迹校准最大标准化误差，例如：

$$
R(\tau)=\max_{t\le T}
\frac{|\hat d_t-d_t|}{\sigma(d_t)}.
$$

使用独立轨迹进行校准，得到一个轨迹级半径。这样可以直接陈述：在校准假设成立时，一条新轨迹的全部感知误差同时落入契约的概率至少为 $1-\alpha$。

这种方法通常比逐步 union bound 更适合有限任务，但需要更多独立轨迹，而当前 400 张单帧图像不足以完成可信的轨迹级校准。

### 9.6 observed-distance 分箱问题必须修正

当前 adaptive teacher 根据观测距离选择半径，但现有 semantic contract 是按真实距离分箱校准的。采样实验可以使用真实状态选择误差范围；在线 teacher 却无法直接知道真实距离。这两种口径不能在形式证明中混用。

可选修正：

1. **最简单、最保守：** 在线使用全局最大半径；
2. **集合一致映射：** 对给定观测 $\hat d$，求所有可能真实距离分箱，并取这些分箱半径的最大值；
3. **按可观测变量重新校准：** 使用独立 calibration set，直接按模型输出或其他在线可观测量分箱；
4. **离线验证专用：** 形式验证时按真实状态 cell 使用相应半径，但明确这只是验证用不确定性模型，不等同于在线 filter 的半径选择逻辑。

若继续研究 filter-free student，推荐第 4 种用于验证 student，同时用第 2 或第 3 种重新定义 teacher，避免教师保证依赖不可观测真值。

### 9.7 安全不等于任务完成

不变性只保证“不撞车”，不能排除车辆停在目标区域之外或无限接近目标却永远不到达。此前 global/adaptive filter 出现 `0% unsafe + 100% timeout`，已经证明这一点。

若论文声称 safe and successful 或 reach-avoid，需要再增加以下一种证明。

#### 排名函数/控制 Lyapunov 条件

构造进度函数 $R(s)\ge0$，在所有非目标状态满足：

$$
R(s_{t+1})\le R(s_t)-\eta,
\qquad\eta>0.
$$

同时保持安全不变性。因为 $R$ 每步至少下降 $\eta$，可以得到有限步到达目标的上界。

#### 有限时域 reach-avoid 动态规划

在有限抽象上计算：

$$
V_t(s)=Pr(\text{在 }T-t\text{ 步内到达目标且此前不碰撞}).
$$

对转移区间取最坏情况，可得到保守的 reach-avoid 概率下界。这比强行让一个 SBC 同时承担安全和进度通常更清楚。

#### 形式化 rollout 上界

对二维确定性 AEBS，可以从整个初始盒传播可达集合，证明所有轨迹在不超过 $T$ 步时进入目标集。若集合膨胀过大，再做分支或按时间重分割。

### 9.8 其他可选形式验证路线

#### 路线 B：正式验证 neural SBC

保留原论文框架，训练 barrier $B(s)$，再用 $\alpha\beta$-CROWN、MILP、SMT 或 branch-and-bound 验证：

$$
B(s)\ge0,
$$

$$
B(s)\le1,\quad s\in X_0,
$$

$$
B(s)\ge\frac{1}{1-p},\quad s\in X_U,
$$

$$
B(s)\ge
\sup_{\hat d,w}B(F(s,\pi(\hat d,v),w))+\epsilon.
$$

优点：与原论文理论最接近，可以给出概率安全下界。

缺点：当前多版 neural SBC 已经失败；严格下降条件可能与循环、低速行为及终止区处理冲突；验证 barrier 与 PPO 的组合网络也更难。它适合作为对照或长期路线，不是当前最快路线。

如果继续使用 SBC，必须满足严格正的 $\epsilon$ 或改用与 $\epsilon=0$ 相匹配的定理，不能把 non-increase 直接套进原无限时域结论。

#### 路线 C：有限抽象 / IMDP / 模型检测

把连续状态划分为 cells，对每个 cell 和感知集合计算所有可能后继 cells：

$$
C_i\rightarrow\{C_j\}.
$$

确定性 worst-case 情况可构造非确定转移系统；有统计转移概率时可构造 interval MDP。然后用模型检测计算：

- 不变安全；
- 有限时域安全概率；
- reach-avoid 概率；
- 最坏到达时间。

优点：安全和任务完成都能自然表达；LP 可以作为动态规划后端；反例是可解释的 cell 路径。

缺点：二维 AEBS 可行，但扩展到高维会发生状态爆炸；仍需可靠的策略输出界和连续 cell 后继界。

这条路线比“只求一个 barrier LP”更完整。单纯固定网格 LP 通过仍然不是连续形式证明，必须加入 abstraction error 或可靠后继覆盖。

#### 路线 D：MILP/SMT 精确验证 PPO

若策略网络使用 ReLU 或可转化为分段线性网络，可以把：

- 网络激活；
- 动作裁剪；
- 一步车辆动力学；
- 不安全后继条件；

编码成 MILP 或 SMT，直接搜索反例：

$$
\exists s,\hat d,w:
s\in K,
\quad
F(s,\pi(\hat d,v),w)\notin K.
$$

求解器返回 `UNSAT` 时，在编码和数值假设正确的前提下即可证明一步不变性。

优点：结论清楚，能给具体反例。

缺点：SB3 PPO 常使用 `tanh` 隐层或非线性分布参数，无法直接获得简单的精确 MILP；可能需要可靠分段逼近、非线性 SMT 或先蒸馏成 ReLU policy。网络和 cell 较多时计算成本高。

#### 路线 E：闭环可达性分析

从初始集合开始，用 zonotope、polytope、interval、Taylor model 或神经网络可达性工具逐步传播：

$$
R_{t+1}=F(R_t,\pi(\mathcal C_d(R_t)),\mathcal W(R_t)).
$$

若对所有 $t\le T$ 都有：

$$
R_t\cap X_U=\varnothing,
$$

则得到有限时域安全保证；若最终 $R_T\subseteq X_G$，还能证明有限时间成功。

优点：直接对应完整闭环轨迹，不需要另训练 barrier。

缺点：可达集合随时间快速膨胀；通常只能证明有限时域；需要分割初始集合和控制集合。

#### 路线 F：保留运行时 safety filter

把经过形式验证的 filter 保留在部署闭环中：

$$
u_t=\operatorname{Filter}(s_t,\pi_S(o_t)).
$$

只要 filter 对所有输入都强制下一状态留在不变集，就可以不证明 PPO 本身安全。

优点：最容易得到强安全保证；student 出错时仍有最后保护层。

缺点：不再是纯 standalone PPO；增加运行时计算；如果 filter 过保守仍可能 timeout。

这是工程上最稳妥的备选，但不符合当前“去掉运行时 filter”的主要目标。可以保留为安全基线和实际部署版本。

#### 路线 G：安全结构化或单调策略

重新设计策略，使输出天然满足解析安全下界：

$$
\pi_{safe}(s)=a_{required}(s)+\operatorname{softplus}(g_\theta(s)),
$$

再用 anti-stall 上界限制过度制动。这样策略结构本身保证制动力不低于安全需求。

优点：证明简单，避免验证一个完全自由的 PPO。

缺点：相当于把 filter 逻辑嵌入网络结构；需要重新训练；需要妥善解决感知误差、动作饱和与 liveness；创新点也更接近 safe-by-construction policy，而不是原 PPO 证书转移。

#### 路线 H：PAC / scenario / statistical certificate

从指定状态分布采样大量独立状态和误差，使用场景优化、PAC bound 或统计模型检查给出：

> 在指定分布下，违反概率以至少 $1-\beta$ 的置信度不超过 $\epsilon$。

优点：比完整连续验证容易，能覆盖复杂感知模型。

缺点：这是分布相关的统计保证，不是“对所有状态和所有误差”的确定性形式保证；闭环相关样本和分布漂移需要额外处理。

### 9.9 各路线对当前项目的适配度

| 路线 | 保证类型 | 是否保留 standalone PPO | 当前实现难度 | 推荐度 |
|---|---|---:|---:|---:|
| 解析可恢复域 + 网络区间界 | 连续域 robust invariance | 是 | 中 | **最高** |
| teacher→student 局部余量转移 | 连续域继承保证 | 是 | 中 | **最高，适合作为论文主线** |
| 有限抽象/IMDP | 安全与 reach-avoid | 是 | 中高 | 高 |
| 闭环可达性 | 有限时域安全/到达 | 是 | 中高 | 高 |
| 正式 neural SBC | 概率或鲁棒 barrier | 是 | 高 | 中低 |
| MILP/SMT | 一步不变性或有限性质 | 是，网络需适配 | 高 | 中 |
| 运行时 safety filter | 在线确定性保护 | 否 | 低到中 | 工程备选最高 |
| 安全结构化策略 | 结构性安全 | 是，但需重训 | 中 | 备选 |
| PAC/scenario | 分布相关概率保证 | 是 | 中 | 补充证据 |

### 9.10 推荐的最小实现顺序

#### Formal Step 0：冻结被验证对象

- 固定 `standalone_ppo_distilled_v2/standalone_ppo.zip`；
- 记录模型哈希、semantic checkpoint 哈希和配置；
- 固定动力学、动作裁剪、时间步长、目标/危险定义；
- 禁止在验证后悄悄替换 checkpoint。

#### Formal Step 1：薄弱区加密诊断

- 在 `d∈[6.7,7.2]m`、`v∈[2.1,2.6]m/s` 加密真实状态；
- 增加语义采样密度；
- 检查 4 mm 余量是否稳定；
- 这一步仍是诊断，不是证明。

#### Formal Step 2：实现 PPO 输入盒动作界

- 优先复用项目已有 `auto_LiRPA`；
- 对每个 state/semantic input box 输出可靠 $a^-,a^+$；
- 用随机样本检查 bound soundness，但随机样本不能替代 bound；
- 保存 bound 宽度和最差 cell。

#### Formal Step 3：实现连续后继余量 verifier

- 从状态盒、动作界和扰动盒计算后继盒；
- 计算后继盒最小 $M$；
- 输出 `verified/violated/unknown`；
- 对 unknown 自动细分。

#### Formal Step 4：处理感知概率口径

- MVP 先给“契约成立条件下的确定性安全”；
- 后续补独立轨迹 calibration，形成有限时域概率结论；
- 在完成序列校准前，不把 97.5% 单样本 coverage 写成整条轨迹安全概率。

#### Formal Step 5：补任务完成证明

- 最简先报告已有闭环 success；
- 论文强结论再增加 reach-avoid abstraction 或 ranking condition；
- safety 与 liveness 分开验证和报告。

### 9.11 形式验证的验收标准

只有同时满足以下条件，才能在论文中写“formally verified”：

- [ ] 验证对象 checkpoint、配置和哈希固定；
- [ ] 初始、目标、不安全、终止和可恢复集合定义一致；
- [ ] 每个连续 state cell 被覆盖，不存在未说明的空洞；
- [ ] 整个 semantic interval 被可靠网络界覆盖，而不是只采样端点；
- [ ] 过程扰动为可靠集合，或明确假设为零；
- [ ] 所有 cell 结果均为 `verified`，或 theorem 只覆盖明确列出的 verified 子域；
- [ ] `unknown` 不计为通过；
- [ ] 数值舍入和 solver tolerance 被纳入安全余量；
- [ ] 感知 coverage 与轨迹风险的关系表述正确；
- [ ] safety 与 success/liveness 不混为一谈；
- [ ] 独立脚本能够复现实验证书和最差 margin；
- [ ] 论文中明确声明模型假设和保证边界。

### 9.12 当前结果对应的准确结论

目前可以写：

> 在 `4483` 个离散可恢复 operational 状态和每状态 `33` 个语义输入组成的 `147939` 个检查对上，standalone PPO v2 的一步后继均保持离散可恢复，未发现违反；最小 sampled recoverability margin 为 `0.003987m`。

目前不能写：

> standalone PPO v2 已在连续状态空间中被形式证明安全。

下一阶段如果连续 verifier 全部通过，可以升级为：

> 在指定连续初始域、状态条件感知契约和动力学扰动集合内，standalone PPO 的闭环后继保持在 robust recoverable invariant set 中，因此在模型假设成立且感知误差位于契约内时不会进入碰撞集合。

若再完成轨迹级校准和 reach-avoid 检查，才可以进一步给出任务时域内的概率安全与成功到达结论。

### 9.13 面向实际实现的逐步路线：每一步做什么、改什么、为什么

下面把后续工作拆成可独立验收的步骤。主线目标不是立刻重新训练 SBC，而是先证明现有 standalone PPO 在连续状态域中的安全性。只有主线证明完成或明确失败后，才决定是否增加 neural SBC。

整体流程为：

```text
冻结当前模型和定义
        ↓
统一安全问题口径
        ↓
修正感知契约的使用方式
        ↓
建立物理可恢复证书
        ↓
把 PPO 转换成可验证 actor
        ↓
计算连续输入盒上的动作界
        ↓
计算连续后继盒和最小安全余量
        ↓
对无法判断的区域自动细分
        ↓
得到条件确定性安全保证
        ↓
再增加轨迹概率与任务完成保证
        ↓
可选：把结果进一步写成 neural SBC
```

#### Step F0：冻结当前实验对象

**通俗解释**

先把“到底验证哪个模型”固定下来。否则今天验证的是一个 PPO，明天重新训练后覆盖了 checkpoint，之前的证明就失效了。

**需要做什么**

1. 固定当前 standalone PPO v2；
2. 固定 semantic encoder；
3. 固定 semantic contract；
4. 固定动力学、时间步长、动作范围和安全定义；
5. 记录所有输入文件的 SHA256 哈希；
6. 复制一份只读配置清单。

**建议新增**

```text
Aebs/formal/freeze_manifest.py
results/formal/00_manifest/manifest.json
```

`manifest.json` 至少保存：

```text
PPO checkpoint 路径和哈希
semantic checkpoint 路径和哈希
filter JSON 路径和哈希
distance scale
dt=0.05
action range=[-3,3]
safety distance=6.0m
safety speed=0.5m/s
状态域、初始域、目标域、不安全域
git commit 或代码文件哈希
```

**为什么要做**

形式证明只对一个确定的系统成立。模型、配置或动力学改变后必须重新验证。

**输入**

- `standalone_ppo.zip`；
- `semantic_encoder.pt`；
- 当前配置文件和动力学代码。

**输出**

- 一份不可歧义的验证对象清单。

**通过标准**

- 后续所有 verifier 都读取同一份 manifest；
- 每次运行先检查哈希；
- 哈希不一致时立即停止，不继续使用旧证书。

#### Step F1：统一安全问题的数学定义

**通俗解释**

把“安全”“成功”“停车”“超时”和“物理上已经救不回来”分别定义清楚。之前反复修改 barrier，主要原因之一就是这些集合在不同代码中口径不一致。

**需要做什么**

统一定义：

```text
X      ：完整状态域
X0     ：允许的初始状态
XG     ：安全到达的目标状态
XU     ：已经发生危险的状态
XI     ：即使最大制动也无法恢复的 inevitable 状态
K      ：仍可安全恢复的状态
XT     ：无需再要求下降的终止状态
```

建议定义：

$$
X_U=\{(d,v):d\le6, v>0.5\},
$$

$$
X_G=\{(d,v):d\le6, v\le0.5\},
$$

$$
K=\{(d,v):M(d,v)\ge0\}.
$$

**建议新增或修改**

```text
Aebs/formal/spec.py
```

让环境、rollout、LP、SBC 和新 verifier 全部调用同一组集合判断函数，禁止各文件重复硬编码 `6.0` 和 `0.5`。

**为什么要做**

如果一个模块把低速状态当终止，另一个模块仍要求其严格下降，就会产生人为矛盾。统一定义是所有后续证明的基础。

**输出**

- 一套共享的集合判断 API；
- 每个集合的网格计数；
- 集合之间是否重叠的自动检查。

**通过标准**

- `XG` 与 `XU` 不重叠；
- operational、goal、unsafe、inevitable 分类互斥且覆盖验证域；
- 环境和 verifier 对同一状态给出相同分类。

#### Step F2：修正感知契约的形式化口径

**通俗解释**

当前误差半径是根据真实距离分箱得到的，但在线控制器只能看到估计距离。形式验证必须明确：半径到底由真实状态选择，还是由在线观测选择。

**需要做什么**

先选择最小实现：验证时对每个真实状态 cell 使用其允许的最大误差半径；若 cell 跨越多个分箱，就取这些分箱半径的最大值，或者直接沿分箱边界切开 cell。

对于状态 cell：

$$
d\in[d^-,d^+],
$$

构造可靠观测区间：

$$
\hat d\in
[d^- - r_{cell},d^+ + r_{cell}],
$$

其中：

$$
r_{cell}=\max_{d\in[d^-,d^+]}r(d).
$$

**建议新增**

```text
Aebs/formal/perception_contract.py
```

包含：

- 查询一个状态 cell 涉及哪些 contract bins；
- 返回可靠最大半径；
- 返回完整观测区间；
- 检查区间是否超出训练支持范围；
- 标记 cell 是否跨分箱边界。

**为什么要做**

如果只使用 cell 中心的半径，会漏掉同一 cell 内更大的感知误差。若继续用 observed-distance bin，却引用 true-state calibration coverage，概率保证也不成立。

**输出**

- 每个 cell 的观测输入区间；
- 最大感知半径；
- 跨分箱 cell 列表。

**通过标准**

- 任意允许真实状态和误差产生的观测都包含在输出区间内；
- verifier 不再只枚举 `-1/0/+1` 三个误差点；
- 论文明确区分“契约内确定性安全”和“契约成立概率”。

#### Step F3：建立物理可恢复证书

**通俗解释**

不再让神经网络自己猜什么状态危险，而是直接使用车辆是否还来得及刹车作为安全证书。

**需要做什么**

实现离散停车距离：

$$
D_{stop}^{disc}(v),
$$

以及可恢复余量：

$$
M(d,v)=d-6-D_{stop}^{disc}(v).
$$

含义：

- $M>0$：还有额外停车距离；
- $M=0$：位于可恢复边界；
- $M<0$：即使最大制动也可能来不及。

**建议新增或迁移**

```text
Aebs/formal/viability.py
```

把目前散落在 `robust_sbc.py`、`analyze_domain.py` 和 transfer check 中的停止距离逻辑收敛到一个共享实现。

**为什么要做**

这个函数具有物理含义、容易解释，而且当前离散诊断已经证明它与 rollout unsafe 状态吻合。它可以替代难以训练的自由 neural barrier，或作为 neural SBC 的骨架。

**输出**

- 任意状态的 $M(d,v)$；
- 可恢复域边界；
- 初始域最小余量；
- 与 rollout 结果的一致性报告。

**通过标准**

- 所有离散 rollout-unsafe operational 状态均不被错误归为可恢复；
- 使用与环境完全相同的离散动力学；
- 速度裁剪和终止条件处理一致。

#### Step F4：只做薄弱区域的加密诊断

**通俗解释**

先用便宜的密集采样判断当前 4 mm 最小余量附近是否存在明显反例。它仍然不是证明，只是为了避免在明显失败的控制器上直接投入昂贵形式验证。

**需要做什么**

重点加密：

```text
distance ∈ [6.7, 7.2] m
speed    ∈ [2.1, 2.6] m/s
```

对每个状态增加更多 semantic samples，例如 257 或 1025 个，并记录：

- student 最小下一状态余量；
- teacher 最小下一状态余量；
- 最差感知输入；
- 最差动作；
- 最差状态是否落在原网格之间。

**修改位置**

```text
Aebs/mvp/check_certificate_transfer.py
```

增加局部范围参数和加密采样模式，不训练任何模型。

**为什么要做**

如果密集采样马上找到负 margin，说明当前 student 真的存在局部问题，应先修策略；如果仍然为正，才值得进入网络区间证明。

**通过标准**

- 没有发现负 margin；
- 最差点和 margin 被结构化保存；
- 不能因为加密通过就声称形式安全。

**失败后怎么办**

- 若 student 失败而 teacher 通过：只在失败小区域局部再蒸馏 student；
- 若 teacher 也失败：先修 teacher 或调整有物理依据的保证域；
- 不重新扫描全局 PPO 参数。

#### Step F5：把 PPO actor 转换为可验证网络

**通俗解释**

SB3 的 `.zip` 包含训练、价值网络、动作分布等很多内容。形式验证只需要确定性 actor：输入距离和速度，输出一个动作。需要把它抽出来，并确认输出与原 PPO 完全一致。

**需要做什么**

构造一个只包含以下部分的 PyTorch 模型：

```text
observation
   ↓
policy feature/MLP layers
   ↓
action_net
   ↓
action clipping
   ↓
deterministic action
```

**建议新增**

```text
Aebs/formal/export_actor.py
results/formal/01_actor/verified_actor.pt
results/formal/01_actor/equivalence.json
```

**为什么要做**

验证工具需要一个明确、无随机采样的前向网络。不能把 PPO 的 value network、随机动作采样或训练状态混入证明。

**一致性检查**

在大量随机输入和边界输入上比较：

$$
|\pi_{SB3}(z)-\pi_{export}(z)|.
$$

**通过标准**

- 最大差异低于预设数值容差；
- 激活函数、归一化和动作裁剪完全保留；
- 导出模型哈希写入 manifest；
- 如果导出不等价，不进入下一步。

#### Step F6：计算整个输入盒上的 PPO 动作界

**通俗解释**

采样33个或1000个点仍可能漏掉中间的最坏动作。这一步要求证明：对输入小盒里的所有距离和速度，PPO 输出一定在某个范围内。

**需要做什么**

对每个 cell 输入盒 $Z_i$，使用 `auto_LiRPA` 或其他可靠验证器计算：

$$
a_i^-\le\pi_S(z)\le a_i^+,
\qquad\forall z\in Z_i.
$$

对于安全制动，首先关注动作下界 $a_i^-$，因为较小制动力会产生更高的下一时刻速度。

**建议新增**

```text
Aebs/formal/policy_bounds.py
results/formal/02_policy_bounds/bounds.npz
results/formal/02_policy_bounds/metrics.json
```

**加入的功能**

- IBP 快速初始界；
- CROWN 或 $\alpha$-CROWN 收紧；
- bound 太宽时返回 `unknown`；
- 保存每个 cell 的动作上下界和 bound width；
- 随机点只能用于检查工具实现，不能替代形式界。

**为什么要做**

这是从有限语义采样升级到完整连续感知区间的关键步骤。

**通过标准**

- 所有抽样动作都位于形式上下界内；
- 验证器明确报告 sound bound；
- 不支持的算子不能静默近似；
- bound width 过大的 cell 被标为 `unknown`。

**失败后怎么办**

- 先细分输入盒；
- 再换更紧的 bound 方法；
- 只有确认存在真实欠制动反例时才修改 PPO。

#### Step F7：计算连续状态 cell 的所有后继

**通俗解释**

得到 PPO 动作范围后，把状态范围、动作范围、感知范围和过程误差一起送入动力学，计算所有可能的下一状态，而不是只算一个网格点的下一状态。

**需要做什么**

对：

$$
C_i=[d_i^-,d_i^+]\times[v_i^-,v_i^+]
$$

和动作界：

$$
a\in[a_i^-,a_i^+],
$$

计算后继盒：

$$
C_i'=[d_i'^-,d_i'^+]\times[v_i'^-,v_i'^+].
$$

如果过程扰动为零，必须在配置里明确写出 `W={0}`；将来有真实动力学误差时，再加入：

$$
w_d\in[w_d^-,w_d^+],
\quad
w_v\in[w_v^-,w_v^+].
$$

然后计算：

$$
\underline M_i=inf_{s'\in C_i'}M(s').
$$

**建议新增**

```text
Aebs/formal/continuous_verifier.py
results/formal/03_continuous_verifier/metrics.json
results/formal/03_continuous_verifier/cells.npz
```

**为什么要做**

这一项直接回答：任意连续状态、任意允许感知误差和任意允许过程扰动下，下一状态是否仍然来得及安全停车。

**cell 分类**

- `verified`：可靠下界 $\underline M_i$ 大于数值安全余量；
- `violated`：存在经过回代确认的真实负 margin 状态；
- `unknown`：当前 bound 无法判断。

**通过标准**

- 所有需要声明保证的 cell 均为 `verified`；
- `unknown` 数量必须为0，或者从论文保证域中明确排除并解释；
- excluded cell 必须有物理依据，不能为了通过而随意删除；
- 保存全局最小形式 margin 和对应 cell。

#### Step F8：对 unknown cell 自适应细分

**通俗解释**

有些区域实际上安全，但因为输入盒太大，网络界太松，验证器看不清。此时把该区域切小再检查，而不是重新训练模型。

**需要做什么**

优先细分：

1. 距离或速度跨度较大的维度；
2. PPO bound 最宽的维度；
3. 跨 contract bin 的 cell；
4. 跨停车步数边界的 cell；
5. 靠近 $M=0$ 的 cell。

**建议新增**

```text
Aebs/formal/refine.py
results/formal/04_refinement/tree.json
```

`tree.json` 保存每个父 cell、子 cell、细分原因和最终状态，保证验证覆盖没有空洞。

**为什么要做**

全域统一使用极细网格成本高；自适应细分把算力集中到真正困难的边界区域。

**停止规则**

- 最大深度；
- 最小 cell 宽度；
- 最大 cell 数；
- 最大运行时间。

达到限制仍不能判断时必须返回 `unknown`。

#### Step F9：根据验证结果决定是否修 PPO

**通俗解释**

形式验证失败有两种完全不同的原因：工具界太松，或者 PPO 真的会在某些连续状态下制动不足。两者不能使用同一种处理方法。

**情况 A：只有 unknown，没有真实反例**

处理顺序：

1. 细分 cell；
2. 使用更紧的网络界；
3. 利用动作对安全余量的单调性；
4. 改善数值精度。

不修改 PPO。

**情况 B：找到真实 violated 状态**

仅在反例邻域生成 teacher 数据，并加入安全余量 loss：

$$
L_{safe}=
\operatorname{ReLU}
(a_{required}(s)-\pi_S(\hat d,v)+m_a).
$$

同时保留原策略：

$$
L_{keep}=\|\pi_{new}-\pi_{old}\|^2.
$$

建议输出新 checkpoint `standalone_ppo_formal_repair_v1`，禁止覆盖 v2。

**为什么要做**

只修形式反例区域，可以最大限度保留当前四种模式100% success的行为，避免再次出现全局重训后性能倒退。

**修复完成标准**

- 原四种 rollout 结果不退化；
- 原反例被消除；
- 完整连续 verifier 重新运行；
- 不能只在训练反例上通过。

#### Step F10：形成第一层正式结论——契约内确定性安全

完成 F0–F9 后，首先形成最简单、最可靠的定理：

> 对任意属于指定初始域的状态，只要每一步感知误差位于 semantic contract 内、过程扰动位于指定集合内，standalone PPO 的闭环后继始终留在 robust recoverable invariant set 中，因此不会进入碰撞集合。

**需要保存的证明产物**

```text
manifest.json
完整 cell 覆盖树
每个 cell 的 PPO 动作界
每个 cell 的后继盒
每个 cell 的最小余量
全局最小形式 margin
verified/violated/unknown 计数
验证器版本和运行时间
```

**为什么先做这一层**

它不需要立刻解决闭环序列 conformal 的复杂概率问题，而且能直接证明控制器在契约内的最坏情况安全。

#### Step F11：增加第二层结论——感知失败概率

**通俗解释**

第一层结论假设感知误差始终在契约内。第二层需要回答：一整段任务中，这个假设有多大概率成立。

**需要增加什么**

收集独立轨迹，而不只是零散图像。每条轨迹计算：

$$
R(\tau)=\max_{t\le T}
\frac{|\hat d_t-d_t|}{\sigma(d_t)}.
$$

使用训练数据训练 encoder、独立 calibration trajectories 校准轨迹分数、独立 test trajectories 评估。

**建议新增**

```text
Aebs/formal/trajectory_contract.py
results/formal/05_trajectory_contract/metrics.json
```

**为什么要做**

单帧97.5%覆盖率不能直接变成400步任务的97.5%安全率。轨迹级校准直接约束“整条轨迹是否全部落在误差带内”。

**输出结论**

若轨迹级契约覆盖率为 $1-\alpha$，且契约内确定性验证通过，可以得到对应任务时域内的概率安全下界。

**当前限制**

现有400张单帧数据不足以可靠完成这一步，因此它属于论文完整版，而不是当前最小实现。

#### Step F12：增加第三层结论——不仅安全，而且能完成任务

**通俗解释**

证明车辆不会撞车以后，还要证明不会永远停在目标外。

**最简实现选择**

构造有限抽象，对每个连续 cell 建立后继 cells，并计算所有路径是否在最多400步内到达目标。

**建议新增**

```text
Aebs/formal/reach_avoid.py
results/formal/06_reach_avoid/metrics.json
```

**检查内容**

- 所有后继路径是否避开 unsafe；
- 是否存在一直不进入 goal 的循环；
- 最大保证到达步数；
- 无法保证到达的初始 cells。

**通过标准**

- 指定初始域所有路径均不进入 unsafe；
- 所有路径在指定步数内进入 goal；
- 或给出保守 reach-avoid 概率下界。

**为什么单独做**

安全不变性与任务完成是两个不同性质。分开验证可以避免再次把 timeout 当成安全成功。

#### Step F13：可选——重新引入 neural SBC

这一阶段不是主线必需项。只有在论文必须与原 SafePVC 的 stochastic barrier 理论保持强联系时才做。

**推荐方式**

1. 使用 F3 的物理证书或有限抽象 value 作为监督标签；
2. 不从随机 MLP 开始；
3. 优先使用有限时域 $B_t(s)$；
4. 使用 LP/动态规划先确认离散约束有解；
5. neural SBC 只压缩离散证书；
6. 最终仍需使用连续 verifier 检查。

**建议新增**

```text
Aebs/formal/finite_horizon_sbc.py
Aebs/formal/train_sbc_from_value.py
Aebs/formal/verify_sbc.py
```

**为什么比旧方法更可能成功**

- 证明域已经合理；
- terminal/goal/unsafe 定义一致；
- 已有可行的离散 value；
- 网络不再从零猜测安全边界；
- 使用有限时域条件避免全域固定严格下降冲突。

**停止条件**

- 离散 LP/DP 不可行时，不训练 neural SBC；
- 连续验证长期为 unknown 时，不将训练 loss 当作证明；
- neural SBC 不得破坏已经得到的解析不变集保证。

#### Step F14：统一生成论文结果

**需要比较的对象**

| 方法 | 作用 |
|---|---|
| 原始 PPO | 展示感知误差下的安全缺陷 |
| PPO + runtime filter | 安全教师和工程安全基线 |
| standalone PPO v2 | 普通蒸馏结果 |
| formal-repair PPO（若需要） | 局部反例修复结果 |
| 原 neural SBC | 展示原路线的证成困难 |
| 解析 invariant verifier | 主要形式安全结果 |
| finite abstraction/reach-avoid | 任务完成结果 |
| 可选 finite-horizon neural SBC | 与原论文理论连接 |

**统一报告指标**

```text
success / unsafe / timeout
return / steps
teacher-student action error
sampled minimum margin
formal minimum margin
verified / violated / unknown cells
verified initial-set volume
验证时间和 cell 数量
感知契约覆盖率
有限时域安全或 reach-avoid 概率下界
运行时是否需要 filter
```

**为什么要统一报告**

论文需要同时回答三个问题：

1. 控制器实际是否工作；
2. 安全结论是否经过形式验证；
3. 为安全付出了多少性能和计算代价。

### 9.14 主线与可选路线的边界

为了避免再次同时推进太多方向，后续工作分为三层。

#### 必做主线

```text
F0 冻结对象
F1 统一集合定义
F2 形式化感知区间
F3 物理可恢复证书
F4 薄弱区加密诊断
F5 导出确定性 actor
F6 PPO 连续输入界
F7 连续后继验证
F8 自适应细分
F9 必要时局部修复
F10 条件确定性安全结论
```

#### 论文完整版增加

```text
F11 轨迹级感知概率
F12 reach-avoid 任务完成证明
F14 完整对照和消融
```

#### 仅在确实需要保留原 SBC 主线时增加

```text
F13 finite-horizon / physics-guided neural SBC
```

在 F10 完成以前，不开始 F11–F13。这样可以确保每一步都解决一个明确问题，不再出现同时修改 controller、contract、barrier、domain 和 verifier 后无法判断结果来源的情况。

### 9.15 每一步最终解决的疑问

| 步骤 | 最终回答的问题 |
|---|---|
| F0 | 我们到底在证明哪个模型？ |
| F1 | 什么算安全、成功、危险和不可恢复？ |
| F2 | 所有可能感知输入是否被覆盖？ |
| F3 | 什么状态在物理上还来得及刹车？ |
| F4 | 当前薄弱区域是否有明显采样反例？ |
| F5 | 被验证的 actor 是否与部署 PPO 相同？ |
| F6 | 两个采样点之间的 PPO 动作会不会更危险？ |
| F7 | 连续状态和所有误差下，下一状态是否仍安全？ |
| F8 | 失败是系统真不安全，还是验证界太松？ |
| F9 | 如果有真实反例，怎样只修必要区域？ |
| F10 | 能否得到契约内的确定性形式安全保证？ |
| F11 | 整条任务中感知契约有多大概率成立？ |
| F12 | 系统是否不仅安全，而且一定能到达目标？ |
| F13 | 如何继续保留并改进原论文的 SBC 形式？ |
| F14 | 新方法相较原文究竟改善了什么、代价是什么？ |
